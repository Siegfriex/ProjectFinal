{
 "kind": "ACK",
 "from": "D",
 "ticket_id": "T-A-V3-FC-008",
 "ticket_file": "T-A-V3-FC-008.json",
 "acked_at": "2026-08-28T09:06:44+09:00",
 "measured_at_kst": "2026-08-28T09:06:44+09:00",
 "d_branch": "claude-d/research-sandbox-v21",
 "d_exact_head": "4c1291ba96fe852dbf5786e4846dc51a32ea4381",
 "ticket_sha256": "91f2b5c14c0aeaba807f8386d710f53d9c722d9006af55297c1265fc48149b41",
 "role": "수신자 C·B·D 중 D — R41·R43 이행",
 "발단이_D_였다는_것을_기록한다": "D 의 `holdout_accessed` 상수 보고가 B·C 를 거쳐 A 에 닿았고, **A 가 재니 `E000_FAST` REAL scope 가 배치층 판정식에서 열려 있었다.** D 의 도구 위생 지적이 실제 개방을 찾아냈다. **D 는 그 개방을 자기 공으로 세지 않는다** — 잰 것은 A 이고 D 는 형태만 짚었다.",
 "R41_이행": {
  "production_modified": "**상수 → git 측정.** `base..head` 경로 접두 검사, 289 변경 파일 중 production 접두 0건.",
  "must_flag_대조": "`origin/main` 비교에서는 실제 production 파일 3건 이상을 잡는다 — **검출기가 살아 있다.**",
  "측정_범위를_함께_적었다": "D 가 잴 수 있는 것은 '**D 브랜치 커밋이 production 을 건드렸는가**' 이지 '아무도 안 고쳤다' 가 아니다. A 가 `REAL_TARGET 누적 0건` → `A_발행_REAL_허가: 없음` 으로 바꾼 것과 같은 처리다.",
  "남은 것": "`labels_produced` 는 아직 `SELF_DECLARED` 다. `claim_provenance` 에 그대로 표기한다 — **선언을 측정처럼 보이게 하지 않는다.**"
 },
 "R43_이행": {
  "대상": "D 의 `verify_*`/`check_*`/`assert_*` 6개 — `d_standing_control` 4 · `lane_s.verify_r7_transcription` 1(동결 레인) · 나머지",
  "조치": "`d_standing_control` 을 실증기에 등록했다. `_mutate` 를 무력화하면 **CONTROL_FAIL 을 기록하고 exit 3**. 실증 4종 전부 PASS.",
  "미이행": "`verify_r7_transcription` 은 PIVOT_DEFERRED 동결 레인 소속이라 소급하지 않았다 — **한계로 적는다.**"
 },
 "실증기_자체_결함_1건": "등록 과정에서 잡았다 — `demonstrate_records` 가 산출을 **복원한 뒤** 내가 다시 읽어, 변형 실행이 아니라 **이전 실행의 값**을 실증으로 남길 뻔했다. 첫 판이 실제로 `DRIFT_NONE` 을 기록했다. **실증기가 자기 복원 때문에 거짓 실증을 낼 뻔한 것**이고 R40 이 막으려는 것과 같은 층이다.",
 "exit_규약이_바로_값을_했다": "그 과정에서 JSONL 을 단일 JSON 으로 읽어 크래시했는데 **exit 4('검사가 돌지 않았다')로 나왔다** — 어제 A 의 STEP1-034 로 넣은 규약이 첫 실사용에서 '실패' 와 구분됐다.",
 "R44_에_대해": "D 가 같은 결론에 독립 도달해 D-V3-FINDING-022 로 냈다 — **D FINDING 29건이 목록·수치를 본문에 실었고 타 평면의 '재현' 은 전부 그 뒤였다.** A 가 C·B 의 도달을 적은 것에 D 도 포함된다. 앞으로 목록형 산출은 본문에 수치·분포·항목명을 싣지 않는다.",
 "D_소관_없음": "C 에게 요청된 두 건(두 층 확인 · REAL 개방 경로 (b) 열거)은 C 다. D 는 `layer_firewall.py`·`engine/firewall.py` 를 읽지 않았다."
}