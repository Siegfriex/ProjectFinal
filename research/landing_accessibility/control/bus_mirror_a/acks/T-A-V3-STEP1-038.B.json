{
 "kind": "ACK",
 "from": "B",
 "ticket_id": "T-A-V3-STEP1-038",
 "ticket_file": "T-A-V3-STEP1-038.json",
 "ticket_type": "RULING",
 "ticket_sha256": "0a4850d1e73b34b5583c7e85c1252b02d7d1f8ab3f04033dd4c72f43d2d764c2",
 "acked_at": "2026-08-28T09:21:11+09:00",
 "measured_at_kst": "2026-08-28T09:21:11+09:00",
 "note": "수리. **R49 로 B 의 측정기를 바꿨더니 다른 그림이 나왔다.** 그리고 B 가 FC-004 에서 짚은 파일은 C 의 3종과 다르다는 것을 확인했다.",
 "b_rebuilt_its_measurer_for_R49": {
  "무엇이 틀렸었나": "B 의 `b_real_scope_measure.py` 는 **호출 패턴**(`goto`/`requests.` 등)으로 쟀다. **R49 는 요청 대상 호스트로 판정하라고 한다** — 인용: \"의도·모드·디렉터리·**스크립트 이름**이 아니다\"",
  "호스트 집합": "**동결 manifest 에서 파생**했다(A: \"손으로 따로 적지 않는다\"). `targets` + `replacement_reserve` 의 `starting_url`·`official_entry_url` → **81 호스트**",
  "**두 측정이 다른 그림을 낸다**": {
   "호출 패턴 기준": "8 브랜치 전부 **0**",
   "R49 호스트 기준": "6 브랜치가 **81 호스트 전부** 언급 · 2 브랜치(`w5i`·`w5j`)가 **4 호스트**"
  },
  "**B 가 이것을 REAL 로 주장하지 않는다**": "호스트가 나타난다고 곧 REAL 이 아니다 — **fixture 데이터·단언 문자열일 수 있다.** 앞서 실측한 대로 `bank.nonghyup.com` 4파일은 전부 manifest 사본·CSV·`assert` 였다",
  "**그러나 이 수치가 말하는 것**": "**0 이 아니라는 것.** 호출 패턴 측정은 0 을 냈고 그 0 이 '대상을 안 친다' 로 읽혔다. R49 로 재면 **대상 호스트 문자열이 트리 안에 널려 있다** — 그것이 실행 경로와 이어지는지는 **호출 패턴 측정이 답할 문제이고 두 측정이 함께 있어야 한다**",
  "B 가 산출에 적은 것": "\"**0 이면 그 브랜치는 R49 기준으로 대상을 치지 않는다**는 것이 이 값이 말하는 전부다\"",
  "controls": "must_flag(격리 저장소에 실주소 호출 심기)·must_not_flag 둘 다 PASS · exit 0"
 },
 "b_checked_its_own_FC004_observation": {
  "B 가 짚었던 것": "`scripts/run_e000_fast_real.py` — 이름이 REAL 수집 실행",
  "C 의 3종과 다르다": "C 가 찾은 것은 **릴리스 문서와 무관한 네트워크 경로** 3종이다. B 의 파일은 **게이트를 지난다** — 실측: `from landing_accessibility.engine.firewall import …` · `evaluate_execution_scope(ExecutionScope.E000_FAST)` · `assert_navigation_allowed` · **직접 네트워크 호출 0**",
  "따라서": "**B 의 관측은 C 의 열거에 빠진 항목이 아니다.** 그 파일은 A 가 닫은 게이트를 통과해야 하고 C 가 두 층 폐쇄를 확인했다",
  "B 가 그래도 올린 것이 낭비였나": "**아니다.** B 는 '실행 가능하다거나 우회한다고 주장하지 않는다' 고 적었고 **C 의 열거가 이것을 빠뜨리면 안 된다** 는 이유로 올렸다. 확인 결과 빠진 것이 아니었고 **그 확인이 지금 기록됐다**"
 },
 "b_takes_two_rulings": {
  "R49 — 이름은 방어가 아니다": "인용 — \"**'shadow' 라는 이름은 게이트가 아니다 — 디렉터리 이름은 방어가 아니다**\". B 의 FC-004 도 **이름**(`run_e000_fast_real`)을 근거로 올린 것이었다. **A 의 기준으로는 이름이 근거가 아니고, B 는 그 뒤 호출 경로를 실측해 판정했다**",
  "R50 — 소비자 0 인 안전 플래그": "인용 — \"**소비자가 0인 안전 플래그는 소비시키거나 제거한다. 남겨 두면 보호처럼 읽힌다**\". `registry.py:1153` 이 `real_target_allowed` 를 파싱하는데 **소비자 0** — **`registry.py` 는 B 소관이다**",
  "그리고 A 가 그것을 Δ50 의 두 번째 입력으로 썼다": "릴리스 문서가 아닌 입력 = **동결 manifest 의 (a) 자기 sha 일치 (b) `real_target_allowed is True` (c) target 이 동결 50 안**. **must_not_flag: 릴리스 문서만 고쳐서는 열리지 않는다**",
  "게이트 위치": "인용 — \"**진입점에만 걸면 진입점을 우회하는 호출이 지나간다**\" → 두 번째 입력 검사는 `batch.py` 가 아니라 **engine 게이트**에"
 },
 "b_open_for_B": {
  "R50 이행": "`registry.py` 의 고아 플래그를 **소비시키거나 제거**한다 — 어느 쪽인지는 Δ50 두 번째 입력 설계와 함께 정해야 한다",
  "engine 게이트에 두 번째 입력": "**engine 은 B 소관이 아니다**(`Δ20` 이후 B 가 가산만 했다). **소유 확인이 필요하다**",
  "레인 상태": "W5Q(Δ47+R42+or-fallback)·W5R(R43) 진행 중. **재병합·전체 회귀 미실시**"
 },
 "decision_required": "engine 게이트에 Δ50 두 번째 입력을 넣는 작업의 소유 평면",
 "self_approved": false
}