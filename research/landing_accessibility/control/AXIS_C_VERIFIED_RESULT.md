# 축 C(초기 화면 방해요소) — 검증된 관측 결과

**ID** `LA-AXC-20260827`
**작성** Claude A (Analysis Governor), 2026-08-27 14:46 KST
**표본** E001 전수 59건 중 **L0 산출물 보유 56건**
**검증** **3중 독립 계수 일치** — B(mart 경로) · C(배치 raw `detail.l0.interrupts[]`) · A(동일 경로, 독립 실행). 세 시점·세 실행이 전건 동일.

> **오늘 유일하게 실측된 축이다.** 축 A 는 평가기 부재로 미평가, 축 B 는 0/59 산출.
> 그러나 축 C 도 **"완전히 측정됨" 이 아니다** — §3 참조.

---

## 1. 규모

```
L0 산출물 보유 관측   56 / 59
interrupt 총          235
관측당 평균           4.2
```

## 2. 방해요소 유형 분포 — **UNKNOWN 이 최대 범주다**

| final_label | n | % |
|---|---|---|
| **UNKNOWN** | **110** | **46.8%** |
| BANNER | 88 | 37.4% |
| LOGIN_PROMPT | 13 | 5.5% |
| PROMOTION_MODAL | 11 | 4.7% |
| COOKIE_CONSENT | 4 | 1.7% |
| APP_INSTALL_PROMPT | 3 | 1.3% |
| BLOCKING_MODAL | 2 | 0.9% |
| CHAT_WIDGET | 2 | 0.9% |
| ADVERTISEMENT | 2 | 0.9% |

**이 표를 UNKNOWN 없이 인용하는 것을 금지한다.** 그렇게 하면 "BANNER 가 최다(70%)" 로 읽히는데
실제로는 **분류된 것의 70%** 이고 전체의 37% 다. 절반이 미분류인 상태를 숨기는 서술이 된다.

## 3. **축 C 도 완전히 측정되지 않았다**

`final_label` 분류기는 **결정론 규칙만 돌고 semantic 판정 단계가 없다.**
그 결과 46.8% 가 `UNKNOWN` 이다.

> **축 A(평가기 부재) · 축 B(gate 판별 실패) · 축 C(47% 미분류) — 세 축 전부에서 같은 구조다.**
> **수집기는 만들어졌고 판정기는 만들어지지 않았다.** 이것이 오늘의 통합적 발견이다.

**정확한 상태 서술:** `축 C — raw 는 실측(235건), 분류는 47% 미분류.`
**금지:** `축 C — 측정됨`

## 4. 겹침 정도 — **양극 분포다. median 단독 인용은 오도한다**

```
max_overlay_coverage (관측당 최대값, n=56)
  q1      0.0664
  median  0.1281
  q3      1.0000     ← 사분위가 극단으로 갈린다
```

**분해하면 median 이 무엇도 대표하지 않는다는 것이 드러난다:**

| 구간 | n | 비율 | median |
|---|---|---|---|
| **전면 덮음 (= 1.0)** | **22** | **39.3%** | 1.0 |
| 겹침 없음 (= 0) | 6 | 10.7% | 0 |
| 그 사이 | 28 | 50.0% | **0.0723** |

**즉 실제 분포는 "화면을 완전히 덮는 것 22건 · 없는 것 6건 · 작은 배너류 28건" 이고,
전체 median 0.1281 은 이 셋 중 어느 것도 아니다.**

**서술 규칙:** `median 단독 인용 금지.` 반드시 **세 구간으로 분해**해서 적는다.

## 5. 닫기 경로 — 4조합 전부 보고한다

| `dismiss_control_exists` | `dismiss_succeeded` | n | 의미 |
|---|---|---|---|
| 0 | 1 | **102** | 닫기 컨트롤 미탐지 상태에서 ESC/배경클릭으로 닫힘 |
| 1 | 1 | 64 | 컨트롤 존재 · 닫힘 |
| 1 | 0 | **38** | 컨트롤 존재 · **닫기 실패** |
| 0 | 0 | 30 | 컨트롤 미탐지 · 닫히지 않음 |
| 1 | null | 1 | 미기록 |

**102 만 강조하지 않는다.** `(1,0) = 38` — **컨트롤이 있는데도 닫히지 않은 경우**가 같은 비중으로 보고돼야
한쪽만 보이지 않는다.

### 5.1 서술 제약 — 축 B 와 동일한 구분

```
✗ 고령자가 이 방해요소를 닫지 못한다
✗ 닫을 수 없는 방해요소가 102건이다
✗ 닫기 수단이 없는 방해요소가 102건이다
○ 시각적 닫기 컨트롤이 **탐지되지 않은** 상태에서 ESC/배경클릭으로 닫힌 경우가 102건이다
○ 닫기 컨트롤이 탐지됐으나 닫기가 실패한 경우가 38건이다
```

**우리가 관측한 것은 자동화 도구의 dismissal 결과이지 사용자 행동이 아니다.**
그리고 **"탐지되지 않았다" 는 "없다" 가 아니다** — 탐지기의 한계일 수 있다.
축 B 에서 "가드가 단어를 탐지했지 gate 뒤인지 확인하지 않았다" 와 같은 구분이다.

## 6. 인용 가능 여부

**§1·§2·§4·§5 의 수치는 A 가 직접 계산했고 B·C 와 3중 일치했다. 확정 인용 가능하다.**

`ANALYSIS_FRAME_FREEZE` 의 축 B 수치도 동일하게 3중 검증됐다.
**단, 이 문서 밖의 파생 수치는 인용 전에 출처를 확인한다.**

## 7. 변경이력

| 시각 | 내용 | 데이터 관측 이후인가 |
|---|---|---|
| 2026-08-27 14:46 | 최초 기록 | **예 — 수집 완료 후.** 이 문서는 **분석 규칙을 정하지 않는다.** 관측된 값을 기록하고 서술 제약을 붙일 뿐이며, 서술 제약(median 분해 · UNKNOWN 병기 · 4조합 · 도구/사용자 구분)은 전부 **오도를 막는 방향**이지 결과를 특정 방향으로 만드는 것이 아니다. |
