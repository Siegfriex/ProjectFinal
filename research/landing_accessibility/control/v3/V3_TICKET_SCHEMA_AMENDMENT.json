{
 "document_id": "V3_TICKET_SCHEMA_AMENDMENT",
 "version": 1,
 "issued_by": "A / Authority Plane",
 "issued_at_kst": "2026-08-28T14:31:03+0900",
 "amends": "SSOTV3/15_TICKET_PROTOCOL_SCHEMA_v3.0.json",
 "원본_수정": "**하지 않는다.** 동결 SSOT 는 그대로 두고 이 문서가 개정을 싣는다",
 "검사기_규약": "티켓 스키마 검사기는 **원본 + 이 개정문서**를 함께 읽는다. D 의 `d_retractions.py` 가 철회 정본을 파싱하는 방식과 같다",
 "AMEND_1_type_enum": {
  "추가": [
   "ADDENDUM",
   "STATUS"
  ],
  "ADDENDUM": "**이미 발행된 티켓에 사실을 덧붙인다.** 원 티켓의 판정·수치를 바꾸지 않는다 — 소급 수정이 아니라 **추가 기록**이다",
  "STATUS": "**판정이나 발견 없이 진행 상태만 보고한다.** 수치를 실을 수 있으나 그 수치로 주장하지 않는다",
  "근거": "D 발행분 각 4·6건이 이 두 어휘로 이미 나갔다. **스키마가 그 용례를 예상하지 못한 것이지 D 가 틀린 것이 아니다**"
 },
 "AMEND_2_평면_E": {
  "추가": "`from`·`to`·`cc` enum 에 **`E`**",
  "왜_더_큰_공백인가": "**원본 스키마에 `E` 가 아예 없다.** E 는 이 census 의 **유일한 REAL 수집 평면**이었고 probe v2·v3·v4 를 전부 수행했는데 **스키마상 존재하지 않는 발신자**였다",
  "어떻게_드러났나": "D 가 `type` enum 10건을 짚어 A 가 스키마를 열어보고서야 봤다 — **10건짜리 문제를 보러 갔다가 평면 하나가 통째로 빠진 것을 봤다**"
 },
 "AMEND_3_적용_시점": {
  "규칙": "**소급 적용하지 않는다.** 이전 발행분을 위반으로 세지 않는다",
  "근거": "철회 효력 시점 규약(`R170`)과 같다"
 },
 "해소되지_않는_것": "`missing:status` **49** · `missing:scope` **30** — 이미 발행된 것이라 고치지 않는다. 필드 부재는 이 개정의 대상이 아니다"
}