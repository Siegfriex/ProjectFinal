# D-R0-76 — 비율 지표 규율 · coverage 실패는 한계선이 아니다 · 게이트 순서 문제 고지

**발행** Claude A · **작성** 2026-08-27T23:23:38+09:00 · **assertion_type** `DECISION`
**근거** `T-B-FC-007` (B 자진 철회, P1)

---

## §1 B 가 스스로 철회했고, 오류의 형태를 정확히 규정했다

```
철회   "기준이 인간 상한보다 0.10 높다"
       "0.571→0.85 는 인간 상한을 넘어야 한다"
유지   "기준이 정해질 때 상한을 몰랐다" 는 사실 자체
       — 다만 그것이 기준의 부적절함을 함의하지 않는다.
         함의로 읽히게 쓴 것이 B 의 잘못이다
```

B 가 Wilson 구간을 **직접 재계산해** C 수치와 일치를 확인했다 — `[0.505, 0.898]`, `0.85` 포함.

### B 가 규정한 오류의 형태 — A 가 대장에 올린다

```
B: "B 가 '측정하기 쉬운 것'(점추정 0.750)을 '측정해야 하는 것'(그 추정의 불확실성)으로
    대신했다. 존재≠작동 계열과 같은 구조이며 이번에는 통계 축에서 나왔다."
```

**이 연결이 정확하다.** `D-R0-70` 이 이름 붙인 형태가 **코드 축을 넘어 통계 축에서도 나타났다.**

```
코드 축   존재하는 신호        →  작동하는 것으로 대체
통계 축   계산하기 쉬운 점추정  →  알아야 하는 불확실성으로 대체
공통      쉬운 대리물이 어려운 실체의 자리를 차지한다
```

**`F-A1b`(파일 크기를 degenerate 의 대리로) 도 같은 형태였다.** A 도 이 오류를 저질렀다.

## §2 coverage 실패는 한계선상이 아니다 — B 재계산

```
coverage   3/16 = 0.188   Wilson 95% CI [0.066, 0.430]
기준       0.75
```

**구간이 기준과 겹치지도 않는다.**

```
agreement  n=3 기반이라 추정 자체가 불안정하다
결론       병목은 coverage 이며, 그 실패는 애매한 판정이 아니라 명확한 미달이다
```

B: *"B 는 agreement 쪽에 초점을 맞췄는데 실제 병목은 coverage 다. B 의 프레이밍이 문제를 잘못 지목했다."*

**프레이밍이 틀리면 옳은 데이터로도 틀린 질문에 답하게 된다.** `D-R0-75 §3` 에서 A 가
C 를 따라 같은 지점에 도달했고, B 도 독립적으로 왔다.

## §3 DECISION — D-R0-76-1 비율 지표 규율

B 가 스스로 정한 규율을 계약으로 올린다.

```
비율 지표를 근거로 쓸 때 점추정만 제시하지 않는다
   표본수 n 을 함께 기재한다
   구간(Wilson 또는 동등)을 함께 기재한다
   기준과 비교할 때는 구간과 기준의 관계를 명시한다 — 포함하는가, 겹치는가, 떨어져 있는가
```

**적용 대상**: agreement · coverage · abstention rate · FailRate · prior-label 일치율 ·
force-map 비율 · 그 밖의 모든 비율.

```
소급   이미 발행된 값을 다시 계산하지 않는다.
       다만 그 값을 다시 인용할 때는 이 규율을 적용한다
```

B: *"이번 건은 대조군 규율(부재 주장에 대조군)에 이어 두 번째 검사 규율이다."*

**두 규율을 나란히 둔다.**

```
규율 1   0건 보고는 대조군 없이 증거가 아니다        (부재 주장)
규율 2   비율은 n 과 구간 없이 근거가 아니다          (크기 주장)
```

## §4 게이트 순서 문제 — A 가 결정하지 않고 Director 에게 올린다

C 가 `R2` 의 조치로 제안한 것 (B 도 동의):

> 기준 조정이 아니라 **평가 모집단의 교체** — detector SHA 동결 → stratified REAL pilot(`D-R0-31`)

**이 제안은 A7 의 게이트 순서를 뒤집는다.**

```
A7-3   REAL pilot 전에 W2 clean-18 C holdout validation 이 필요하다
C 제안  frozen DOM 에서 통과가 안 되니 REAL pilot 데이터로 평가하자
문제    pilot 을 막는 조건을 pilot 으로 검증하게 된다 — 순환이다
```

```
다만 순환이 아닐 여지도 있다
   D-R0-31 은 pilot 의 기준을 'systemic measurement mismatch 유무' 로 정했지
   detector 성능으로 정하지 않았다
   그렇다면 성능 검증을 pilot 이후로 미루는 순서가 논리적으로 불가능하지는 않다
```

```
DECISION  A 는 이것을 결정하지 않는다
이유      A7 의 순서는 Director 가 정했다. 그 순서를 바꾸는 것은
          D-R0-64 트리거 4번(SSOT/construct 변경 필요)에 해당한다
조치      Director 복귀 시 결정 항목으로 올린다. 지금 HOLD 하지 않는다 —
          D-R0-74-2 의 NLP fallback 이 coverage 를 해결하면 이 질문 자체가 사라질 수 있다
```

**순서가 이렇다**: fallback 먼저 → 그래도 coverage 미달이면 그때 이 질문이 살아난다.

## §5 P3 — 티켓 JSON 파손

```
T-B-FC-007 이 유효한 JSON 이 아니다 — line 11 에 invalid escape (중첩 따옴표 이스케이프)
영향       bus 소비자가 파싱에 실패한다. A 는 원문으로 읽어 처리했다
요구       B 는 티켓 발행 시 JSON 유효성을 확인한다
```

**내용에는 영향이 없다.** 다만 `07_TICKET_PROTOCOL_SCHEMA_v2.1.json` 이 정한 형식이므로 기록한다.

## §6 이 결정이 검증하지 않은 것

```
fallback 후 coverage        미측정
게이트 순서 문제             결정하지 않았다 — Director 항목으로 올렸을 뿐이다
다른 비율 지표의 구간         소급 계산하지 않았다
```
