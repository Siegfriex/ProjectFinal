# Landing Accessibility v2 — V2-C005 Adversarial Audit

| 항목 | 값 |
|---|---|
| Auditor | adversarial (Claude), 전용 워크트리 `.agent_worktrees/landing_adversarial` · 브랜치 `audit/landing-adversarial` |
| Phase / Gate | `P0_V2_REFREEZE` / `V2_SSOT_FROZEN` |
| Cycle | `V2-C005` |
| Exact audited SHA | `684c792452af670caf5bbf2646cf41bd39a1ce99` (`agent/landing-v2-exec`) |
| Previous target | `d5f1da5652953542d5c8be377026cc3293f2075a` |
| Authoritative main | `5a9015d1e95b15304aaf53a73efb475934610b82` |
| Pilot baseline | `32460b87334a67f6a74823ac55f85ca80a9f8980` |
| Control target | `e7de68413077d28130e3924f1e83b53fb1a9400b` |
| 직전 adversarial (Codex 대체 수행) | `1d96a48665c4c5d81625982eaba351b0f0d38968` @ `audit/landing-adversarial-codex-c004` |
| 직전 ssot | `0ca2f636736dc1e16295f296c6c1e794e4cf3d51` @ `audit/landing-ssot` |
| Verdict | **FAIL** |
| Finding count | **P0 0 / P1 1 / P2 4** (blocking **2**) |
| Gate | `V2_SSOT_FROZEN = FAIL` — OPEN 유지 |

---

## 1. 결론

**시정은 실효가 있었다.** V2-C004 blocking 4건 중 **3건이 닫혔고**, 내 V2-C003 잔여 2건 중 1건도 닫혔다.
배너 우회는 committed 변조로 5개 권위문서 전건을 직접 공격해 전부 차단됨을 확인했고,
승격 reconciliation 가드는 4가지 닫힘조건을 dry-run 으로 하나씩 깨뜨려 보려다 전부 막혔다.
`open_blocking_total = 12` 는 두 경로로 독립 재계산해 정확하며 **이번 사이클 시정 주장분은 한 건도 몰래 빠지지 않았다.**

그럼에도 `FAIL` 이다. 두 가지 때문이다.

1. **재수집 선행 앵커(RC-6)는 "사전선언의 소급 작성"을 닫았지만 "결과를 보고 고르는 것"을 닫지 못한다.**
   A-3·A-5 의 **부재 증명은 공격자가 커밋 순서를 통제하므로 자명하게 만족**시킬 수 있고, 실질적으로 하중을
   받는 유일한 검사인 A-6 은 `planned_evidence_run_id` 를 **수집 시작 전에 결정론적으로 알 수 있게** 만든다.
   따라서 앵커된 그 하나의 id 로 **로컬에서 K 번 실행한 뒤 마음에 드는 실행분만 커밋**하면
   A-1~A-6 과 RC-7 양방향 대조가 전부 통과한다. A2 §1.11.2 의 닫힘 논거표(§1.11.2 :1334)가
   "은닉은 정본 선택에 아무 이득을 주지 못한다"고 단언하는데, **동일 앵커 재실행에 대해서는 그 단언이 거짓이다.**
   `ADV-C004-01` 은 **크게 좁혀졌으나 여전히 OPEN** 이다. `P1 / blocking`.
2. **배너 정책을 잠근다고 신설한 회귀 테스트 5건이 공허하게 통과한다.** 검증 블록을 **통째로 삭제해도**,
   `BANNER_ALLOWED` 에 5개 권위문서를 **전부 추가해도** `pytest -q` 는 7 passed 다 (두 경우 모두 실측).
   테스트가 워킹트리를 더럽히므로 `returncode != 0` 이 배너 검사와 **무관하게** 항상 성립하기 때문이다.
   Codex 닫힘조건 ③(committed mutation regression test)이 이행되지 않았고, 정본 검사를 지키는 장치가 없다.
   `P2 / blocking / V2_SSOT_FROZEN`. **한 줄 수정으로 닫힌다.**

나머지 3건(P2)은 non-blocking 이며 다음 사이클 이전에 닫히면 충분하다.

**shadow lane 3개는 금지된 real-target measurement 를 하지 않았다.** `PHASE_GATES §4.6` 에 따른 P0 finding 은 **0건**이다.

---

## 2. 감사 독립성과 경계

전용 워크트리 `/home/sieg/projects-wsl/ProjectFinal/.agent_worktrees/landing_adversarial`(`audit/landing-adversarial`)
에서 수행했다. 공격은 전부 **일회용 clone**(`--shared`)과 **dangling commit-object**로만 했고,
어떤 브랜치 ref 도 이동시키지 않았으며 exec·control·shadow 워크트리와 Pilot 을 수정하지 않았다.
`git fetch --all --prune` 후 `ls-remote` 로 원격 SHA 를 직접 대조했다.

> **직전 사이클 감사 주체 명시.** `V2-C004` adversarial 은 Claude 세션 한도 중단으로 독립 감사자 **Codex** 가
> `audit/landing-adversarial-codex-c004` (`1d96a48`)에서 수행했다.
> 그것이 V2-C004 의 **정본 보고서**다. 내 워크트리에는 중단된 세션의 `V2_C004.{md,json}` 초안이
> **untracked 로 남아 있으나 읽지도 채택하지도 않았고 커밋하지도 않았다** — 같은 브랜치에 경쟁 `V2_C004.json`
> 이 커밋되면 `[DEBT_RECOMPUTE]` 가 원장 선언 브랜치에서 잘못된 보고서를 읽을 수 있기 때문이다(§6.1 관찰 5).

### 2.1 불변조건 실측

| 경계 | 결과 |
|---|---|
| Pilot `research/refcohort/**` 수정 | `git diff --stat 32460b8 684c792 -- research/refcohort` = **0** |
| root `CLAUDE.md` 수정 | `git diff --stat 5a9015d 684c792 -- CLAUDE.md` = **0** |
| `state/*.parquet` 수정 | **0** |
| C013 혼입 | main→target diff 19파일 전부 v2 docs · verifier · tests · `.gitignore` — C013 경로 **0** |
| target tree 내 evidence | `git ls-tree -r 684c792 \| grep evidence/` = **0** |
| 원본 pack 바이트 | 저장소 밖 `SSOT/` 와 `cmp` — `00`~`05` **6/6 IDENTICAL** + `MANIFEST.json` IDENTICAL |
| 자기승인 / 미감사 깊이 | `unaudited_cycle_depth = 0 ≤ MAX = 1`, exec·control·audit 브랜치 분리 유지 |
| 정지점 | `E001_V2_STARTED = false` · `FULL_COLLECTION_STARTED = NO` (EXECUTION_AUTHORITY:19-20) |

---

## 3. 실행한 공격

### 3.1 배너 우회 — committed 변조 12케이스

일회용 clone 에서 파일을 고치고 `INSTALL_MANIFEST.json` 의 `bytes`/`sha256`(필요시 `banner_sha256`)을
공격본과 맞춘 뒤 **commit** 하고 `verify_v2_docs.py` 를 돌렸다.

| # | 공격 | 결과 |
|---|---|---|
| A1 | `A1` 최상단 배너 + 매니페스트 동기화 (**Codex 가 통과시킨 정확한 경로**) | **차단** `BANNER … 배너 마커가 있다` |
| A2 | `A2` 동일 | **차단** |
| A3 | `EXECUTION_AUTHORITY` 동일 | **차단** |
| A4 | `PHASE_GATES` 동일 | **차단** |
| A5 | `00_SSOT_v2.0.md` 동일 (V2-C003 경로) | **차단** |
| B | `A1` **본문 중간**에 배너 마커 쌍 삽입 | **차단** (최상단 검사 이전에 허용목록에서 죽는다) |
| C2 | 마커를 줄바꿈으로 **쪼갬** (`<!-- INSTALLED-BANNER-\nSTART -->`) | **차단** (END 마커가 남아 탐지) |
| D | **허용 파일** `README.md` 의 배너 내용 위조 + `banner_sha256` 갱신 | 통과 — **설계상 정상**(§6.1 관찰 1) |
| D2 | **허용 파일** `CLAUDE.md` 의 배너 내용 위조 + `banner_sha256` 갱신 | 통과 — **설계상 정상** |
| E | `verify_v2_docs.py` 의 `BANNER_ALLOWED` 에 `A1` 추가 + `A1` 주입 | 통과 — **ADV-C005-03** |
| F | `INSTALL_MANIFEST` 에서 `CLAUDE.md` / `A1` / `PHASE_GATES` 항목 **삭제** | **차단** (`UNINSTALLED` / `UNDECLARED`) |
| F2·G | `docs/INDEX.md` 항목 삭제 (+ 그 파일에 배너 주입) | 통과 — **ADV-C005-02** |

`docs/v2/` 아래 파일 삭제는 `V2.rglob` 커버리지가, pack 파생 파일 삭제는 `for name in source` 루프가 잡는다.
**`source_pack_name = null` 이면서 `docs/v2/` 밖인 파일(현재 `docs/INDEX.md` 하나)만 두 그물 사이로 빠진다.**

`BANNER_ALLOWED` 3파일은 `EXECUTION_AUTHORITY.md:59-62` 의 선언과 정확히 일치한다.

### 3.2 회귀 테스트 공허성 — 두 번 실측

`tests/test_v2_docs_integrity.py:58-80` 이 신설한 5건은 `assert proc.returncode != 0` **하나만** 본다.
테스트가 대상 파일과 매니페스트를 **워킹트리에 쓰므로** `git_tracked_and_clean` 이 항상 `ANCHOR … 워킹트리 오염`
으로 실패한다 — 배너 검사가 없어도 `returncode != 0` 이다.

```text
(a) verify_v2_docs.py:105-118 배너 블록 717바이트 전체 삭제 → pytest -q ⇒ 7 passed
(b) BANNER_ALLOWED 에 00_SSOT · A1 · A2 · EXECUTION_AUTHORITY · PHASE_GATES 5건 전부 추가 → 7 passed
(c) 테스트가 만드는 시나리오의 실제 stderr:
      ANCHOR    docs/v2/A1_….md: 워킹트리 오염: M …
      ANCHOR    docs/v2/INSTALL_MANIFEST.json: 워킹트리 오염: M …
      V2_DOCS_VERIFY: FAIL          ← "BANNER" 문자열이 없다
```

정상 상태(clean clone)에서 같은 주입을 하면 `BANNER    docs/v2/A1_….md: 배너 마커가 있다` 가 나온다.
**즉 검사는 살아 있고 테스트만 그것을 보지 않는다.**

### 3.3 재수집 선행 앵커(RC-6) — 설계 공격

구현은 P-C/P-D 배정이므로 **문면을 공격했다.**

| 공격 | 결과 |
|---|---|
| `anchor.prereg_commit_sha = NOT_A_COMMIT` / 빈 문자열 / 7자리 prefix | **차단** — A-1 fail-closed (I-30) |
| 원장 과거 레코드 개작 후 history rewrite | **차단** — `prev_record_sha256` 체인 + 기존 SHA resolve 실패 (A-2 · I-32) |
| 다른 id 로 로컬 수집한 run 을 선언에 재부착 | **차단** — A-6 id 불일치, evidence·`manifest.jsonl` 해시 전량 재생성 필요 (I-37) |
| control 등재 없는 / evidence 커밋 이후의 countersign | **차단** — A-5 (I-33). control state 의 `recollection_prereg_anchors.entries = []` 는 **fail-open 이 아니다** — 명시적으로 "빈 배열은 참조 가능한 앵커가 0" 이며 앵커를 참조하는 run 은 전부 차단이라고 선언했고, invariants 6항이 그것을 못박는다 |
| **A-3 부재 증명 우회** — evidence 를 커밋하지 않은 채 사전선언을 커밋한다 | **통과** — evidence 를 언제 커밋할지는 수집자가 정한다. A-3 은 "부주의하게 evidence 를 먼저 커밋한 경우"만 잡는다 |
| **A-5 부재 증명 우회** — countersign 은 control 브랜치에 있고 exec 의 evidence 커밋은 그 조상에 **원래 없다** | **통과** — 브랜치가 다르므로 조건이 자명하게 성립한다 |
| **동일 앵커 재실행** — 앵커를 정상 취득한 뒤 `planned_evidence_run_id` 로 로컬 K회 실행, 최적 1회만 커밋 | **통과** — 아래 |
| RC-7 `ABORTED_*` 우회 — observation 행을 물질화하지 않고 evidence 만 보고 판단한 뒤 `ABORTED_OPERATOR` | **통과** — 가드가 *물질화된 행*을 보고 *정보*를 보지 않는다 (동일 잔여의 변형) |
| RC-1 상한 1 이 "한 번은 결과를 보고 쓸 수 있다"인가 | **아니다** — 최초 run 과 재수집 run 사이 선택은 RC-3 이 `expected_evidence` 산출 여부로만 정하고 `verdict_state` 는 어느 검사에도 입력되지 않는다 |

**동일 앵커 재실행이 왜 통과하는가.** A-6 은 `planned_evidence_run_id` 를 사전선언 레코드의 `record_sha256`
에서 **결정론적으로 유도**한다. 따라서 수집자는 수집을 시작하기 **전에** 그 id 를 안다. 같은 id 로 K 번 실행하면
K 개의 산출물이 나오지만 커밋되는 것은 하나이고, 원장에는 `PREREGISTRATION` 1건 + `DISPOSITION` 1건만 남는다.
RC-7 양방향 대조(원장→run 1:1, run→원장 1:1)도, `attempt_index`·`ledger_seq` 조밀성도, 해시체인도 전부 통과한다.
버려진 K−1 회는 저장소에 아무 흔적을 남기지 않는다. **선택 기준이 `verdict_state` 였는지 여부를 볼 수 있는 것이 없다.**

### 3.4 승격 가드 dry-run (push 없음)

전부 `--dry-run` 으로 실제 `promote_landing_main.sh` 를 실행했다.

| 입력 | 결과 |
|---|---|
| `reconciliation_sha = NOT_A_COMMIT` | **차단 `[REC_RESOLVE]`** — Codex 가 7검사를 통과시킨 값이 첫 층에서 죽는다 |
| 유효하나 control 계보 밖(Pilot SHA `32460b8`) | **차단** — `state.json` 부재. (조상 판정 자체는 통과한다: `32460b8` 은 control tip 의 조상이다. 그러나 `REC_SHA`≠tip 경로는 **tip 의 state.json 과 바이트 동일**을 요구하므로 실질 fail-closed) |
| **push 되지 않은 로컬 커밋** (control tip 위에 dangling commit-tree 로 생성, `open_blocking_total = 0` 으로 조작) | **차단 `[REC_REMOTE_AUTHORITY]`** — `ls-remote` 실시간 tip 의 조상이 아니다 |
| 같은 로컬 커밋을 `--orch-sha=` 로 | **차단 `[SHA_RESOLVE]`** — control 워크트리 HEAD 불일치 |
| 정상 입력 (`rec = e7de684`) | `[REC_RESOLVE]`·`[REC_REMOTE_AUTHORITY]`·`[PILOT_IMMUTABLE]`·`[EXEC_TREE]`·`[ORCH_TREE]` 통과 후 **`[REC_STATE_BIND]` 에서 차단** — `V2_C004_RECONCILIATION.json` 의 `target_exec_sha` 가 `d5f1da5` 라 승격 대상과 다르다 (정상. V2-C005 는 아직 reconcile 되지 않았다) |

`[DEBT_RECOMPUTE]` 파이썬 블록은 주석이 초대한 대로 **떼어내 argv 만으로 독립 실행**했다.

| 원장 조작 | 결과 |
|---|---|
| 스칼라 `open_blocking_total.total` 만 12→0 | **차단** — 원장 항목 재계산 12 ≠ 선언 0 |
| 전 finding `state=CLOSED` + total 0 | **차단** — pinned 보고서 findings[] 에서 센 blocking adv 4 / ssot 1 ≠ 0 |
| V2-C004 원장 finding 1건 삭제 + total 11 | **차단** — 원장 등재수 3 ≠ 보고서 4 |
| `counted_as_open=false` 를 `duplicate_of`/`superseded_by` 없이 부여 | **차단** — 근거 없는 제외 |
| **v1 `debt_inheritance.items` 1건 삭제 + total 12→11** | **통과** (재계산과 선언이 함께 내려가 정합) — **ADV-C005-04** |

`[AUDIT_ANCESTRY]` 브랜치 선언 블록도 분리 실행했다.

| 입력 | 결과 |
|---|---|
| `latest_adversarial_audit_branch` 부재 | **차단** (fail-closed) |
| `agent/landing-v2-exec` (네임스페이스 밖) | **차단** |
| `audit/landing-adversarial-selfmade` (네임스페이스 안 임의 이름) | 통과 — **finding 으로 올리지 않는다**(§6.1 관찰 3) |

---

## 4. Blocking findings

### ADV-C005-P1-01 — `recollection-escape-path-unbounded-and-conclusion-prioritized`

**P1 / blocking(`V2_SSOT_FROZEN`) / 승계 OPEN (V2-C003 → V2-C004 ADV-C004-01 → V2-C005)**

`research/landing_accessibility/docs/v2/A2_VOCABULARY_AND_SCHEMA_BINDING.md:1281-1340` (규칙 RC-6, 검사 A-1~A-6),
`:1334` (닫힘 논거표 "은닉한 run" 행), `:1341-1372` (규칙 RC-7).

**닫힌 것.** Codex 가 §3.4 에서 실행으로 재현한 경로 — *결과를 본 뒤 `preregistered_at` 을 소급해 사전선언을
지어내는 것* — 은 닫혔다. 원장·해시체인·push·control countersign 이 선언 자체의 순서를 run 바깥에 고정한다.
Codex 닫힘조건 ①②③(선행 앵커 존재 · 앵커 종류 · run manifest 의 앵커 참조)은 이행됐다.

**닫히지 않은 것.** 닫힘조건 ④(*시도·실패·폐기된 모든 run 을 열거할 수 있어야 한다*)가 이행되지 않았다.
A-3·A-5 의 부재 증명은 커밋 순서를 수집자가 정하므로 자명하게 만족되고(§3.3), 하중을 받는 A-6 은
`planned_evidence_run_id` 를 수집 **전에** 결정론적으로 노출한다. 그 결과 **하나의 정상 앵커 아래에서
같은 run id 로 K 번 실행하고 원하는 1회만 커밋**하는 경로가 열려 있다. 이때
A-1~A-6 · RC-7 양방향 대조 · 조밀성 · 해시체인이 **전부 통과**한다.

A2 `:1334` 는 이 지점에서 사실과 다른 단언을 한다 — "은닉된 run 은 원장에 레코드가 없어 A-2·A-6 을
통과할 수 없다". 은닉된 run 이 **다른** id 를 쓸 때만 참이다. 앵커된 id 를 **공유**하면 A-2·A-6 은
은닉분과 채택분을 구별하지 않는다.

**닫힘 조건(제안).**
- 앵커 1건이 허가하는 **실행 횟수를 1로 못박고**, 실행마다 `kind = EXECUTION` 레코드를
  시작 전에 append·commit·push 하게 한다(A-3 부재 증명을 그 레코드에 건다).
  그러면 K 회 실행은 K 개 레코드로 `ledger_seq` 조밀성·체인에 드러난다.
- 또는 `evidence_run_id` 유도 입력에 **수집자가 미리 알 수 없는 값**(control countersign 커밋 SHA 등)을 넣어
  수집 시작 전 id 예측을 막는다.
- `:1334` 의 단언을 **동일 앵커 재실행에 대해 정정**한다. 남는 잔여가 절차적이라면 절차적이라고 적는다.
- RC-7 `ABORTED_*` 조건을 "observation 행 0건"이 아니라 "evidence 산출물 0건"으로 좁힌다.

### ADV-C005-P2-01 — `banner-policy-regression-tests-pass-vacuously`

**P2 / blocking(`V2_SSOT_FROZEN`) / 신규**

`tests/test_v2_docs_integrity.py:40-80` (`_forge_banner` · `test_banner_injection_is_rejected` 5 파라미터).

테스트가 대상 파일과 `INSTALL_MANIFEST.json` 을 **워킹트리에 쓰기 때문에** `verify_v2_docs.py` 는
배너 검사와 무관하게 `ANCHOR … 워킹트리 오염` 으로 실패한다. 유일한 단언 `assert proc.returncode != 0` 은
그 오염 하나로 항상 성립한다. 실측(§3.2):

- 배너 검증 블록(`verify_v2_docs.py:105-118`)을 **통째로 삭제** → `7 passed`
- `BANNER_ALLOWED` 에 `00_SSOT`·`A1`·`A2`·`EXECUTION_AUTHORITY`·`PHASE_GATES` **5건 전부 추가** → `7 passed`

두 번째가 결정적이다 — 그것이 이 테스트가 막으려고 존재하는 **바로 그 회귀**다.
Codex 닫힘조건 ③("A1, A2, EXECUTION_AUTHORITY, PHASE_GATES 각각에 committed mutation regression test") 미이행이며,
`EXECUTION_AUTHORITY.md:64-69` 의 "이 정책은 문장이 아니라 코드가 강제한다" 는 **코드를 지키는 장치 없이** 서 있다.

**닫힘 조건.** 둘 중 하나로 충분하다.
- `assert "BANNER" in proc.stderr and rel in proc.stderr` 를 추가한다 (한 줄).
- 또는 `tmp_path` 에 clone 을 만들어 **커밋한 뒤** 검증한다 (Codex 문면 그대로의 committed mutation).

---

## 5. Non-blocking findings (P2)

### ADV-C005-P2-02 — `install-manifest-coverage-boundary-leaves-non-pack-docs-outside-v2-droppable`
`research/landing_accessibility/scripts/verify_v2_docs.py:184-194` · `docs/v2/INSTALL_MANIFEST.json`

커버리지는 두 그물이다 — ① `for name in source` (pack 파생 전건 설치 확인), ② `V2.rglob` (`docs/v2/` 아래 등재 확인).
**`source_pack_name = null` 이면서 `docs/v2/` 밖인 파일은 두 그물 밖이다.** 현재 해당하는 것은
`docs/INDEX.md`(authority `DOCUMENT_INDEX`) 하나이며, 매니페스트에서 항목을 지우면 검증이 `PASS` 하고
그 뒤 그 파일에 배너를 주입해도 `PASS` 한다(실측 F2·G).
또한 A2 §0 이 **권위 10위**로 열거하고 RC-6 앵커 digest 결속의 근거가 되는
`docs/07_EVIDENCE_MANIFEST_CONTRACT.md` 는 `INSTALL_MANIFEST.json` 에 **애초에 없다**.

non-blocking 근거 — 매니페스트는 git 앵커이므로 항목 삭제는 감사 대상 diff 에 그대로 드러나고,
파일 자체는 여전히 git 추적 상태다. 현 사이클에 실현된 우회는 아니다.
**닫힘 조건**: 커버리지 루프를 `ROOT/docs` + 명시 목록으로 넓히고 `07_EVIDENCE_MANIFEST_CONTRACT.md` 를 등재한다.

### ADV-C005-P2-03 — `verify-script-and-its-tests-are-not-in-the-install-manifest`
`research/landing_accessibility/scripts/verify_v2_docs.py:43-52` · `tests/test_v2_docs_integrity.py`

`BANNER_ALLOWED` 는 코드 상수인데 그 코드를 앵커하는 것이 없다. 스크립트에 `A1` 을 허용목록에 추가하고
`A1` 에 배너를 주입한 뒤 커밋하면 `V2_DOCS_VERIFY: PASS` 다(실측 E). `[INSTALL_INTEGRITY]` 는 스크립트를
**exec 워크트리에서** 실행하므로 "공격자가 자기 검증기를 돌리는" 구조다.
Codex 가 잔여 부채 ③으로 기록한 것을 **구체 재현으로 재확인**한다.

non-blocking 근거 — `[EXEC_TREE]` 가 exec SHA 를 감사받은 SHA 에 핀하므로 감사 후 교체는 불가능하고,
스크립트 변경은 사이클 diff 에 드러난다. 실질 앵커는 감사 그 자체다.
**닫힘 조건**: 두 파일을 `INSTALL_MANIFEST.json` 에 `anchor: git` 으로 등재하고,
`BANNER_ALLOWED` 가 `EXECUTION_AUTHORITY.md` 선언 3파일과 같은지 테스트로 대조한다. ADV-C005-P2-01 과 함께 닫힌다.

### ADV-C005-P2-04 — `debt-recompute-has-no-second-source-for-v1-inherited-items`
`control@e7de684:research/landing_accessibility/scripts/promote_landing_main.sh:673-706`

`[DEBT_RECOMPUTE]` 의 (A)는 pinned 감사 보고서에서, (B)는 원장 항목에서 센다. v2 사이클 finding 은
(A)↔(B) 교차대조가 되므로 삭제가 잡힌다(실측: 1건 삭제 → 차단). 그러나 **v1 `debt_inheritance.items`
(현재 blocking 6건)에는 두 번째 원천이 없다.** 항목 1건을 지우고 `total` 을 함께 1 내리면 재계산과 선언이
정합해 통과한다(실측 T3b). 원장에 `previous_total = 19` · `delta = -7` 이 있으나 **아무도 검증하지 않는다.**

non-blocking 근거 — 현재 total 이 0 이 아니라 `!= 0` 에서 먼저 죽고, 조작은 control 브랜치 diff 에 드러난다.
**닫힘 조건**: `total == previous_total + delta` 를 단언하고, 직전 사이클 `open_blocking_ids` 의 각 id 가
현 원장에 (열려 있거나 CLOSED 근거와 함께) **존재**함을 요구한다.

---

## 6. 이전 finding 판정

### 6.0 Codex `V2-C004` 4건 + 내 `V2-C003` 잔여 2건

| # | id | 출처 | 판정 | 근거 |
|---|---|---|---|---|
| 1 | `install-manifest-banner-allowlist-skips-repo-authored-authority-docs` | Codex ADV-C004-03 (P2) | **CLOSED** | committed 변조로 `A1`·`A2`·`EXECUTION_AUTHORITY`·`PHASE_GATES`·`00_SSOT` **5건 전건 차단** 실측. 본문 중간 삽입·마커 분할도 차단. 검사가 `source_pack_name` 분기 **앞**으로 이동했고 쌍 개수·최상단 위치까지 본다. (닫힘조건 ③의 회귀 테스트는 별건 ADV-C005-P2-01 로 신규 등재) |
| 2 | `recollection-escape-path-unbounded-and-conclusion-prioritized` | Codex ADV-C004-01 (P1) | **OPEN (대폭 좁혀짐)** | 사전선언 소급 작성 = 닫힘. 동일 앵커 재실행에 의한 결과 선택 = OPEN. §4 ADV-C005-P1-01 |
| 3 | `a1-0-3-jurisdiction-declaration-drifts-from-a2-and-collides-with-existing-section` | Codex ADV-C004-02 + ssot V2-C004 (P2) | **CLOSED** | 두 표를 기계 파싱해 대조: **7행 전건 라우팅 동일**(A1 §0.3 L55-61 ↔ A2 §0 4항 L45-51). A1 중복 `### 0.3` 해소 — §0.1~§0.5 연속·유일. A2 의 stale `A1 §0.2` backlink **4곳 전부** 갱신 + 절번호 이동 2곳 반영. A1→A2 인용 13/13 · A1 §1.8~§6.1 10/10 · A2 §1.5.1~§6.3 11/11 전부 실재. 잔여는 §6.1 관찰 2 |
| 4 | `promotion-reconciliation-sha-is-dead-and-control-state-is-only-local-single-source` | Codex ADV-C004-04 (P2) | **CLOSED** | 닫힘조건 4가지를 dry-run 으로 각각 공격해 전부 차단(§3.4). `NOT_A_COMMIT` → `[REC_RESOLVE]`, 로컬 미push 커밋 → `[REC_REMOTE_AUTHORITY]`, 원장 미bind → `[REC_STATE_BIND]`, 스칼라·항목·근거없는 제외 → `[DEBT_RECOMPUTE]`. 잔여 1건은 ADV-C005-P2-04 로 신규 등재 |
| 5 | `ned-ied-path-minimality-not-operationalized` | 내 V2-C002 (P2, `E001_V2`-blocking) | **OPEN — 이번 사이클 미착수, 현 P0 non-blocking** | A1 diff 는 §0.3 관할표 + §0.4/§0.5 절번호 이동 뿐(23+/15−). §1.3·§1.7 의 NED/IED 경로 최소성은 손대지 않았다. P-C/P-D 귀속이며 그 Gate 와 `E001_V2` 전에는 닫혀야 한다 |
| 6 | `installed-banner-region-is-unanchored-injection-surface` | 내 V2-C002/C003 (P2, `POST_E001_DEBT`) | **CLOSED** | 배너 영역이 이제 네 겹으로 묶인다 — (a) 허용 3파일 밖은 **앵커 종류와 무관하게** 코드가 거부, (b) 정확히 한 쌍, (c) 최상단 강제, (d) 내용 자체가 `banner_sha256` 으로 git 앵커된 매니페스트에 고정. 허용 3파일의 배너 내용을 위조하면 통과하지만(실측 D·D2) 그 텍스트는 **정의상 executor 저작**이고 외부 앵커가 존재하지 않는 종류다 — 앵커는 pinned exec SHA 에 대한 감사이며, 그것은 `A1`/`A2`/`PHASE_GATES` 와 동일한 수준의 앵커다. 원장도 blocking=false 로 등재해 왔다 |

### 6.1 finding 으로 올리지 않은 관찰 (과탐 회피)

1. **허용 3파일의 배너 내용 위조**(D·D2)는 통과하지만 finding 이 아니다. 배너 텍스트에 외부 앵커가 존재할 수 없고,
   같은 논리로 `A1`·`A2`·`PHASE_GATES` 본문도 executor 가 임의로 커밋할 수 있다. 앵커는 감사다.
2. **A2 `:160` 의 세 번째 열거**(`값 도메인 등재 대상: A1 §1.8 / §2.2 / §3.2~§3.4 / §4.2~§4.4 / §5.1 / §6.1`)가
   `d5f1da5` 에서 바이트 무변경으로 남아 있고 `§1.5` 를 빠뜨린다. A1 §0.3 이 닫았다고 적은 세 drift 중 하나다.
   또 A2 `:140` 의 "A1 §0.3과 **글자 그대로 동일**" 은 문면상 거짓이다(헤더 + 2·7행 3열이 A1 쪽에만 부연을 갖는다).
   **라우팅은 7행 전건 대칭이고 구현자가 다른 권위를 고를 여지가 없으므로** blocking 으로 올리지 않는다.
   한 줄 삭제 + "정확히 같은 목록" 으로의 문구 정정으로 끝난다.
3. **`[AUDIT_ANCESTRY]` 의 브랜치 선언이 새 공격면인가 — 아니다.** 원장(state.json)은 control 브랜치에 있고
   reconciliation 커밋에 bind 되며 원격 control 계보 위에서만 성립한다. 즉 선언 주체는 오케스트레이터다.
   **이전 버전의 하드코딩도 `promote_landing_main.sh`(같은 control 브랜치) 안의 상수였다** — 권한 경계가 동일하다.
   `audit/landing-*` 네임스페이스 안에서 임의 이름을 인정시킬 수 있다는 것은 사실이나 그것은 하드코딩 시절에도
   상수 한 줄 수정으로 가능했다. 권한 상승이 없으므로 finding 이 아니다. 오히려 하드코딩은 Codex 보고서에
   **도달조차 못 하게** 만들었고 그 결함이 이번에 실제로 닫혔다.
4. **마커 변형 `<!--INSTALLED-BANNER-START-->`(공백 없음)** 은 `A1` 에 넣어도 탐지되지 않는다(실측 C).
   그러나 그 블록이 주는 권한은 "repo-authored 문서에 임의 텍스트를 커밋한다" 이상이 아니며, 이는 마커 없이도
   가능하다. 마커 정규화는 값싼 강화이므로 적어 두되 finding 으로 올리지 않는다.
5. 내 워크트리에 중단 세션의 `audit/adversarial/V2_C004.{md,json}` 초안이 untracked 로 남아 있다.
   읽지도 커밋하지도 않았다. 브랜치 선언이 데이터가 된 지금, 같은 브랜치에 경쟁 `V2_C004.json` 이
   커밋되면 `[DEBT_RECOMPUTE]` 가 잘못된 보고서를 읽을 수 있으므로 **삭제를 권한다**.
6. `promote_landing_main.sh:424` 의 인용 `(PHASE_GATES §4: 두 감사 PASS + reconciliation 후 Gate close)` 는
   A0 amendment 의 절 이동으로 이제 `§5` 를 가리켜야 한다. 저장소 전체에서 이 한 건뿐이며 오류 메시지 문구다.
7. `SSOT/` 는 primary 워크트리에 untracked 로 있고 `.gitignore` 대상이 아니다. 감사 입력이 아니므로 무해하나
   원본 pack 이 추적 여부에 대해 아무 선언 없이 놓여 있는 상태다.

---

## 7. A0 SHADOW amendment (`PHASE_GATES §4`) 검증

문서 스스로 "이 A0 amendment 자체도 다음 P0 audit 의 검증 대상" 이라 선언했으므로 전항 검증했다.

### 7.1 §4.1 금지 10항 vs `00 §15` · `00 §14` · `05 §6` — **모순 없음**

- `00 §15` 는 **본수집(E001) 전** 충족 목록이고 §4.1 은 **P0 종료 전** 금지 목록이다. §4.1 은 promotion·E000 authoritative·
  E001·result claim 을 전부 금지하므로 `00 §15` 의 순서를 완화하지 않는다. §4.7 이 자동승격을 금지하고
  deterministic rerun + 재감사를 요구하므로 shadow 에서 "validated" 를 상속할 수도 없다.
- `00 §14` Claim Boundary 는 §4.1 10항("article / result claim 생성 금지")이 그대로 흡수한다.
- `05 §6` "본수집 전 evidence 생성 = gate 위반" 과 §4.2 의 fixture 기반 수집기 허용은 **충돌이 아니다.**
  `00 §15` 자신이 `E000_V2 smoke PASS` 를 `READY_FOR_E001_V2` 의 선행조건으로 두므로 `05 §6` 의 문면을
  "모든 evidence" 로 읽으면 `00`(권위 1위)과 충돌한다. §4.1 2항이 그것을 **real-target** 으로 좁힌 것은
  기존 충돌의 정합적 해소이지 새 이탈이 아니다.
- `05 §6` 의 dual audit · exact same target SHA · self-approval 금지 · `MAX_UNAUDITED_EXEC_CYCLES` 는
  §4.8 이 "batching 만 허용, 감사 삭제 금지, Gate close 시 exact promoted candidate SHA 에 대한 required audit 유지"
  로 명시 보존한다.

### 7.2 §4.5 REAL-TARGET FIREWALL — 문장뿐인가

**세 lane 의 실제 코드에서 강제된다.** LANE C 엔진은 `engine/firewall.py` 에 `ALLOWED = {FIXTURE, SHADOW_DRY_RUN}`
과 `FIXTURE_URL_SCHEMES = {"file"}` 를 두고 `l0_collector.py` 가 `page.goto` **이전에** `assert_navigation_allowed`
를 호출한다. `failure_injection.py` 는 `REAL_TARGET` 을 시도하는 주입 케이스 7건(문자열 우회 포함)을 태워 hard fail 을 요구한다.
LANE B 프로브는 `_FORBIDDEN_KEYS` 17개(verdict/kwcag/mpfed/ned/ied/dom_path/ax_path/screenshot_path/…)를 매 실행 assert 한다.
**다만 lane 을 가로질러 강제하는 저장소 차원의 게이트는 없다** — cross-lane 강제 수단은 감사다.
현 Phase(문서 동결)에서는 비례하며 finding 으로 올리지 않는다.

### 7.3 shadow lane 이 real-target measurement 를 했는가 — **하지 않았다 (P0 finding 0건)**

| 검사 | 결과 |
|---|---|
| KWCAG verdict / `fact_criterion_result` 행 | **0** |
| popup·obstruction 결과 | **0** |
| MPFED / NED / IED 결과 | **0** |
| evidence run · `manifest.jsonl` · DOM/AX/screenshot 산출물 | 저장소 전체에 **0** (`evidence/` 디렉터리 자체가 없다) |
| `execution_mode = REAL_TARGET` 선언 | **0** — 모든 출현이 가드 코드 또는 음성 테스트 |
| authoritative E000 / E001 / article claim | **0** |
| `research/landing-accessibility-main` 또는 `main` 으로의 merge | **0** (`git branch -a --contains` 로 9브랜치 전건 확인) |
| authoritative `state/*.parquet` write | **0** |

lane tip: `agent/landing-pa-shadow = 0f46203` (1커밋, 28파일 전부 `analysis/` 아래),
`agent/landing-pb-prework = 9999857` (1커밋), `agent/landing-pc-fixture = d5f1da5` (미커밋 fixture 엔진 36항목).
`claude-b/*` · `codex/*` 6브랜치는 커밋 0.

**LANE B 는 실제 서비스 URL 358건에 라이브 HTTP 요청을 했다**(2026-08-26 16:52–17:03 UTC, mobile+desktop,
1.2s 지연, 직렬, 연구용 UA + 연락처). 이것은 **§4.2 의 `official URL research` · `web eligibility` ·
`web target grouping` 과 §4.5 의 명시적 carve-out("URL availability/eligibility probe 는 measurement 가 아니라
target-preparation")에 정확히 해당**하며, 그 carve-out 의 조건(accessibility verdict 미생성)도 지켜졌다 —
저장된 필드는 status/final_url/redirect/title/lang/viewport-meta/app-store-signal/length/elapsed 뿐이고
본문 HTML·DOM·AX·screenshot 은 한 바이트도 남기지 않았다. `PHASE_GATES §4.6` 마지막 항의 P0 finding 요건에
해당하지 않는다.

다만 사실관계로 적어 둔다 — LANE B 산출물의 `real_target_measurement: false` 는 정책 정의상 옳지만
**그 lane 이 실제 타겟에 네트워크로 접촉했다**는 사실은 그 플래그만 보고는 알 수 없다.
`§4.7` reconciliation 시 이 구분이 유지되어야 한다.

### 7.4 §4.3 provenance — 대체로 붙어 있으나 두 결손

LANE A `analysis/SHADOW_MANIFEST.json` 과 LANE B `shadow/lane_b/state/LANE_B_SHADOW_MANIFEST.json` 은
필수 6필드 + 선택 필드를 모두 갖는다. LANE B 는 4개 출력 표에 `_status`·`_base_sha`·… 8컬럼을 **행마다** 박았다.

**§4.7 reconciliation 시점으로 이월할 결손 2건** (P0 finding 아님 — §4.6 "shadow artifact 의 존재 자체를
P0 blocker 로 올리지 않는다"):
- LANE A `SHADOW_MANIFEST.json` 의 `artifact_sha256` 27건 중 **1건 불일치** —
  `analysis/out/pilot/mapping_run_manifest.json` 기록 `daf9ee21…` vs 실제 `6f0f384a…`.
  그 파일은 wall-clock 타임스탬프를 품고 있어 **§4.7 의 deterministic rerun 으로 해시가 재생될 수 없다.**
- LANE A 하위 산출물 11건(EDA 리포트·스크립트·`out/eda0*`·`figs/*.png`·`pilot_mapping.jsonl`)이
  in-file `status = SHADOW_PREPARATORY` 를 갖지 않고 위 매니페스트에만 의존한다.

### 7.5 정책표 복제 — **없음**

target tree 전체에서 SHADOW 정책 문자열은 `PHASE_GATES.md` **한 파일에만** 있다.
control 쪽 `state.json:v2_transition.a0_shadow_parallelization` 과 `cycles/V2_C005_orchestrator.json` 은
`definition_of_record = "…PHASE_GATES.md §4"` 를 명시하고 lane 인벤토리만 담는다 — 정책표 복제가 아니다.

---

## 8. A2 199KB — 신규 규칙이 기존 규칙을 무력화하는가

`UNDETERMINED → PASS` 를 executor 코드를 쓰지 않고 A2 §1.11.1 표만 읽어 다시 열거했다.

```text
논리 조합 = verdict_state(4) x ai_review_required(2) x adjudication(4)  = 32
  ai_review_required = 0 -> adjudication 행 없음 -> 4조합 (표 1~4행)
  ai_review_required = 1 -> 16조합, 표 5~20행이 1:1 (RESOLVED 는 확정 label 로 재분기)
매핑되지 않은 조합 = 0
UNDETERMINED -> PASS 경로 = 0
UNDETERMINED -> FAIL / NA 경로 = 0   (T-8, X-11 · X-13)
NA -> 그 외 경로 = 0                 (T-6 > T-8 > T-7 · T-4 · T-9, 19·20행, X-15)
```

신규 규칙의 방향은 **전부 조임**이다. RC-6·RC-7 은 *어느 run 이 정본인가*만 다루고 전이표에 입력을 주지 않는다.
RC-3 에 추가된 "A-1~A-6 중 하나라도 실패한 run 은 `UNDECLARED`", RC-4 의 "블록 없이 = 세 경우",
§4.2 의 "정본이 되려면 선행 앵커가 있어야 한다" 는 모두 분자·분모를 좁힌다. X-14 는 ⑤⑥⑦ 추가로 금지를 넓힌다.
V-9 는 원장을 `state/*.parquet` **밖**에 두어 규칙 V-1·V-4·V-5 와 정합한다.
EXC-4 는 §0 5항의 예외를 1→2 로 늘리면서 그 사실을 표로 명시했다 — 7항 예외 등재 형식을 지켰고
`02` 의 바이트를 건드리지 않았다. **기존 규칙을 무력화하는 신규 규칙은 발견되지 않았다.**

---

## 9. `open_blocking_total = 12` 검증

두 경로로 독립 재계산했다 — (i) 감사자 자작 스크립트, (ii) `promote_landing_main.sh` 의
`[DEBT_RECOMPUTE]` 블록을 떼어내 argv 로 직접 실행. **둘 다 12.**

```text
v1=6, V2-C001=0, V2-C002=1, V2-C003=0, V2-C004=4, orchestrator=1   합계 12
19 - 1(C002 ssot CLOSED) - 5(C003 adv CLOSED) - 2(C003 adv -> C004 승계) - 3(C003 ssot CLOSED) + 4(C004 신규) = 12
open_blocking_ids 항목 수 = 12  (합계와 일치)
counted_as_open=false 3건 전부 duplicate_of/superseded_by 근거 보유
pinned 보고서 counts.blocking: adversarial 4 / ssot 1 — findings[] 직접 계수와 일치
```

**차감의 정당성.** 빠진 11건은 전부 *감사가 CLOSED 판정한 것*이다 — Codex V2-C004 §5.2 가 CLOSED 로 판정한
C003 adversarial 5건, ssot V2-C004 가 CLOSED 로 판정한 C003 ssot 3건 + C002 ssot 1건, 그리고 C003 의
2건은 삭제가 아니라 `superseded_by` 로 C004 쪽에서 계속 세고 있다.
**시정 주장분은 한 건도 빠지지 않았다** — `⑨`·`⑪` 이 명시하듯 V2-C005 가 시정을 주장한 5건
(ADV-C004-01/02/03/04 + 파생)은 전부 `REMEDIATION_CLAIMED_PENDING_AUDIT` 로 **계상 유지**되어
`open_blocking_ids` 에 그대로 남아 있다. 자기승인 금지가 지켜졌다.

**이 감사 반영 시 다음 원장 값** (오케스트레이터 참고): ADV-C004-02·03·04 CLOSED → −3,
ADV-C005-P2-01 신규 blocking → +1. `12 → 10` 이 된다 (ADV-C004-01 은 OPEN 유지).

---

## 10. Gate verdict

```text
VERDICT              = FAIL
P0 / P1 / P2         = 0 / 1 / 4
BLOCKING             = 2   (P1 recollection · P2 배너 회귀 테스트 공허)
V2_SSOT_FROZEN       = FAIL (OPEN 유지)
PROMOTION_AUTHORIZED = false
P_A_AUTHORIZED       = false   (LANE A shadow 는 §4 아래 계속 허용)
E001_V2_AUTHORIZED   = false
```

이 감사에서 하지 않은 것: 승격, P-A 착수, `E001_V2` 실행, Pilot 수정, root `CLAUDE.md` 수정,
`state/*.parquet` 수정, target 안 evidence 생성, 어떤 브랜치 ref 의 이동, 어떤 push (감사 브랜치 제외).
