# V2-C006 / SSOT · Authority Audit

| 항목 | 값 |
|---|---|
| cycle | `V2-C006` |
| auditor | `ssot` (SSOT / Authority Auditor) |
| target | `agent/landing-v2-exec` @ `3ad04585ae0f58500859e4c3b765844b2cdb5089` |
| prev target | `684c792452af670caf5bbf2646cf41bd39a1ce99` (V2-C005) |
| base | `research/landing-accessibility-main` @ `5a9015d1e95b15304aaf53a73efb475934610b82` |
| control | `control/landing-orchestrator` @ `3eaf51bd8157f6b24d3ee5f5afd81b1e0387b309` |
| 동일사이클 adversarial | (미확인 — 이 감사 시점에 origin 에 V2-C006 adversarial 보고서 없음) |
| 직전 두 감사 | adversarial `audit/landing-adversarial` @ `b7fd4677202640e8293c9c7a766fb624e88f3925` (V2-C005) · ssot(본인) `audit/landing-ssot` @ `39e485c85b745e7f34b675e24c54662b83dcea79` (V2-C005) |
| 감사일 | 2026-08-27 |
| **verdict** | **FAIL** |
| 집계 (신규) | P0 **0** / P1 **0** / P2 **1** (blocking **1**) |
| ssot 귀속 open blocking 합계 | **1** |

> **판정 요약.**
>
> 이번 사이클은 실질적으로 큰 진전이다. 내 V2-C005 blocking 3건(F1·F2·F3) 중 **F1·F3 는 CLOSED**,
> **F2 는 부분적으로만 닫혔다.** 비차단 3건(F4·F5·F6)은 **전부 CLOSED.** 새로 여는 blocking 은 1건뿐이다 —
> F2 가 부분적으로만 닫힌 바로 그 자리(§5 기준선 표)에서 발생한 잔여 결함이다.
>
> **F1(배너 회귀 테스트 무력화) — CLOSED, 직접 재현으로 확인.** 커밋 주장("배너 강제 블록 제거 시
> 4건 실패, 원복 시 8건 통과")을 `git archive` 격리 복제본에서 **내가 직접 재현**했다 — 배너 강제
> 블록을 제거하자 정확히 A1·A2·EXECUTION_AUTHORITY·PHASE_GATES 4개 파라미터화 테스트가
> `"차단됐으나 사유가 배너가 아니다"` 로 실패했고(신설된 `assert "BANNER" in combined` 가 작동),
> 00_SSOT 는 별개의 `banner_inserted` 불일치 경로로 여전히 잡혔다(무력화 아님). 원복하니 8/8 통과.
> 대조군 테스트(`test_non_banner_mutation_does_not_report_banner`)도 확인했다 — 배너 없는 변조가
> BANNER 사유로 오분류되지 않는다. 검증기 자기등재로 인한 마커 리터럴 자기탐지 문제도
> `f"<!-- {_MARKER}-START -->"` 조립으로 해소됐고, 검사가 약화된 지점은 없다 — 오히려
> 필수 3항목(`CLAUDE.md` · `docs/INDEX.md` · `scripts/verify_v2_docs.py`) 매니페스트 등재 강제가
> **새로** 추가됐다(삭제 공격 차단, 독립 확인).
>
> **F3(RC-6 A-6 순환) — CLOSED.** `planned_evidence_run_id` 필드를 사전선언 레코드에서 삭제하고
> `evidence_run_id = f(ledger_record_sha256, countersign_commit_sha, execution_index)` 로 유도 방향을
> 뒤집었다. `f` 의 정의역이 전부 사전선언 레코드 **바깥**(원장 커밋 해시 · control countersign · 실행
> 순번)이므로 `record_sha256` 순환이 재발하지 않는다. 해석은 하나뿐이다 — 검증자가 `f` 를 재계산해
> **바이트 비교**한다고 명문화했다(A-6). control 측 `recollection_prereg_anchors.record_schema` 도
> `FORBIDDEN_evidence_run_id` · `REMOVED_planned_evidence_run_id` 로 같은 결정을 반영해 문서와
> 정합한다. `f` 는 `A1 §6.3` 의 `observation_id` 해시 입력을 불투명 문자열로만 소비하므로 그쪽과도
> 충돌이 없다(A1 diff 0, 직접 확인).
>
> **A0 amendment 재수집 앵커 재설계 — 내부 일관성 확인.** `EXECUTION` 레코드 신설이 `02 §12`
> append-only 를 위반하지 않고(레코드 종류가 2→3 이 됐을 뿐 append 전용은 유지), A-7·A-8 은
> RC-1~RC-7 번호 체계와 충돌하지 않는 **RC-6 하위 검사**로만 추가됐다. **R-1(막지 못하는 잔여 —
> 인가된 하나의 id 로 로컬 K회 실행 후 1회만 커밋) 을 적은 세 자리(§1.11.2 잔여 위험표 · 닫힘
> 논거표의 "닫히지 않는다" 행 · 실패주입 I-41)가 서로 같은 시나리오·같은 결론을 말한다.** 옛
> ":1334" 단언("은닉은 정본 선택에 아무 이득을 주지 못한다")은 명시적으로 철회되고 세 행으로
> 분해됐다 — 인가받지 않은 id(닫힘) / 인가받고 숨김(닫힘) / 동일 인가 재실행(**닫히지 않음**).
> 과탐 없이 정직하게 적혀 있다.
>
> **F2(EXECUTION_AUTHORITY 가 A0 를 모른다) — 부분 CLOSED.** §2.5 A0 pointer 신설(정책표 복제 0건,
> 직접 확인), shadow lane 3종 등재(`agent/landing-pa-shadow`·`pb-prework`·`pc-fixture` 3브랜치
> 실재·base SHA 일치를 `ls-remote`+`merge-base` 로 확인), §6 감사 이력에 V2-C004(Codex 경위 포함)·
> V2-C005 행 추가 — **셋 다 닫혔다.** 그러나 **§5 기준선 표의 `ssot audit` 행이 갱신되지 않았다.**
> `adversarial audit` 행은 `V2-C005 @ b7fd4677...` 로 정확히 갱신됐는데(`origin/audit/landing-adversarial`
> tip 과 문자 그대로 일치), 바로 아래 `ssot audit` 행은 여전히 `V2-C003 @ 43fab0ac...` 를 가리킨다 —
> 실제로는 이 문서 자신이 참조해야 할 최신 ssot 감사는 V2-C005(`audit/landing-ssot @ 39e485c...`)다.
> **같은 표 안에서 한 행은 최신이고 한 행은 세 사이클(V2-C004·V2-C005 를 건너뛰고) 정체돼 있다** —
> §2.5·§6 은 정확히 고쳐놓고 바로 아래 §5 표 한 줄은 손대지 않은 것이다. 새 blocking 으로 연다.
>
> **install-integrity-coverage-gap — 부분 CLOSED, 비차단 유지.** `scripts/verify_v2_docs.py` 는
> 이번에 `INSTALL_MANIFEST.json` 에 등재되고 자기 SHA 로 앵커됐다(git-anchored, 독립 확인). 그러나
> `tests/test_v2_docs_integrity.py` 는 여전히 미등재다 — control 도 같은 사실을 `OPEN` 으로 기록한다.
> 등급·차단 여부는 V2-C002 이래 판정과 동일하게 유지한다(비차단).
>
> **A2 는 199,424B → 214,013B.** 독립 재계산 결과 `INSTALL_MANIFEST.json` 선언(`bytes 214013` /
> `sha256 40afa5d5be996405d9ad47221d1f10d76e894a72f0bbe3d008355837cabdc478`)이 실제 target A2 와
> **일치**하며, control `a2_document_state` 도 같은 값으로 갱신돼 **지난 사이클 F5("어떤 브랜치의
> A2 와도 맞지 않는다")가 CLOSED** 됐다. `open_blocking_total = 14` (v1 6 + C002 1 + orchestrator 3 +
> C005 4)를 `open_blocking_ids` 14건을 직접 세어 재현했다 — 목록 길이 == 합계.
>
> 설치 무결성은 **다섯 사이클 연속 완전 통과**다. `INSTALL_MANIFEST.json` 15항목(신규 1건 —
> `verify_v2_docs.py` 자기등재) `bytes`/`sha256` 를 스크립트를 신뢰하지 않고 전부 재계산했고,
> 배너 3종의 배너 제외 본문 ↔ `SSOT/` 원본 바이트 동일성, `00`~`05` 6종 ↔ `SSOT/` 바이트 동일성을
> 모두 독립 확인했다. Pilot·root `CLAUDE.md`·`state/*.parquet` 는 BASE↔TARGET·PREV↔TARGET 양쪽
> diff 0 이다.

변경량 `git diff 684c7924 3ad04585 --stat` = **5파일 / +206 / −52**
(`A2` +163/−0netto·`EXECUTION_AUTHORITY` +33/−0netto·`INSTALL_MANIFEST.json`·`verify_v2_docs.py`·
`tests/test_v2_docs_integrity.py`). `00~05` + `MANIFEST.json` + `README.md` + `bootstrap/07` +
`CLAUDE.md` + `docs/INDEX.md` + `A1` + `PHASE_GATES.md` 전건 diff 0 — 전건 실측.

---

## 0. 감사 범위와 방법

```
3ad04585 (PREV 684c7924 대비) 변경분:
  research/landing_accessibility/docs/v2/A2_VOCABULARY_AND_SCHEMA_BINDING.md   +120/-43 (199424B → 214013B)
  research/landing_accessibility/docs/v2/EXECUTION_AUTHORITY.md                +30/-3   (14016B 확정)
  research/landing_accessibility/docs/v2/INSTALL_MANIFEST.json                 +12/-5
  research/landing_accessibility/scripts/verify_v2_docs.py                     +11/-1
  tests/test_v2_docs_integrity.py                                             +30/-0

대조 원본:
  /home/sieg/projects-wsl/ProjectFinal/SSOT/00~07 · MANIFEST.json · README.md
  control/landing-orchestrator @ 3eaf51bd : research/landing_accessibility/control/state.json ·
                                            control/cycles/V2_C006_orchestrator.json
  audit/landing-ssot @ 39e485c85b745e7f34b675e24c54662b83dcea79 : V2_C005.{md,json} (내 직전 보고서)
```

실행한 것:

- `git archive 3ad04585 | 새 git init` 로 **완전 격리 복제본**을 만들어 **F1 재현 실험**을 직접 수행 —
  배너 강제 블록(105–121행) 제거 → 파라미터화 배너 회귀 테스트 5종 중 정확히 4건 실패(`AssertionError:
  차단됐으나 사유가 배너가 아니다`), 00_SSOT 1건은 별도 `banner_inserted` 경로로 계속 통과.
  블록 원복 → `pytest tests/test_v2_docs_integrity.py` **8 passed** — 실물 저장소 무수정.
- `INSTALL_MANIFEST.json` **15항목 bytes/sha256 독립 재계산**(Python, `git show` 로만 읽음, `checkout` 미사용)
- 배너 3종 — 종료 마커 탐색 → `body` 분리 → `SSOT/` 원본과 바이트 비교 + `body_sha256`/`banner_sha256`
  자체 재계산 및 매니페스트 선언과 대조 (3/3 전건 일치)
- `00`~`05` 6종 ↔ `SSOT/` 원본 바이트 비교 (6/6 IDENTICAL)
- PREV↔TARGET diff 로 `00~05`+`MANIFEST.json`+`README.md`+`bootstrap/07`+`CLAUDE.md`+`docs/INDEX.md`+
  `A1`+`PHASE_GATES.md` 전건 diff 0 확인, BASE↔TARGET 로 Pilot·root `CLAUDE.md`·`state/*.parquet` diff 0 확인
- A2 diff 전문 리뷰(299행) — `EXECUTION` 레코드·A-7·A-8·유도식 `f`·잔여표·닫힘논거표·I-38~I-41 전건
- `f` 의 정의역이 `A1 §6.3` `observation_id` 해시 입력과 충돌하는지 A1 `evidence_run_id` 4개 출현 전수 확인
- `EXECUTION_AUTHORITY.md` §5 기준선 표 두 행을 `ls-remote`(간접, 로컬 refs 로 대체 확인)로 실측 대조
- shadow lane 3브랜치 `origin` 실재·`merge-base` 로 base SHA 일치 확인
- `PHASE_GATES §4→§5` 인용 회귀 — target tree + control 양쪽 전수 grep(9개 브랜치가 아니라 target·control
  2개 트리 직접 확인, control 자체의 V2-C006 자기시정 로그와 대조)
- control `state.json` 의 `recollection_prereg_anchors.record_schema` v2 신설분과 A2 §1.11.2 대조
  (`FORBIDDEN_evidence_run_id` · `REMOVED_planned_evidence_run_id` · `REMOVED_absence_proof` ·
  invariant A-7·A-8 · 검사범위 A-1~A-8)
- `a2_document_state.{bytes,sha256}` 를 target A2 실측과 대조 (F5 재판정)
- `open_blocking_total = 14` 를 `open_blocking_ids` 배열 직접 카운트로 재검증
- `verify_v2_docs.py` 코드 자체를 읽어 배너 검사 순서(SHA/BYTES 선행 → BANNER)와 필수 3항목 강제
  로직을 확인(실행이 아니라 정적 검토 — 실물 저장소 실행은 하지 않았다. 실행은 격리 복제본에서만)

**저장소에는 아무것도 쓰지 않았다.** 최초 실수로 `git checkout 3ad04585 -- .` 를 워크트리에서
실행해 워킹트리가 일시 오염됐으나 **커밋·push 전에 `git reset --hard HEAD` 로 즉시 원복**했고,
이후 모든 target-tree 열람은 `git show` 로만 수행했다. 감사 시작·종료 시점 모두
`git status --porcelain` = 0, `HEAD = 39e485c85b745e7f34b675e24c54662b83dcea79`(내 워크트리 자기
브랜치, 미변경).

---

## 1. V2-C005 내 finding 판정

### 1.1 `banner-injection-regression-tests-pass-without-banner-enforcement` (F1) — **CLOSED**

실증 재현 결과가 커밋 주장과 정확히 일치한다.

| 시나리오 | 결과 |
|---|---|
| 실물(배너 강제 포함) `pytest` | **8 passed** |
| 배너 강제 블록 제거판 `pytest` | **5 failed, 3 passed** — 그중 A1·A2·EXECUTION_AUTHORITY·PHASE_GATES **4건**이 신설된 `assert "BANNER" in combined` 에서 `"차단됐으나 사유가 배너가 아니다"` 로 실패 |
| 00_SSOT (배너 강제 블록 제거판에서도) | **통과** — 별도의 `banner_inserted` 플래그 불일치 경로(`banner_inserted=False 인데 실제는 True`)가 독립적으로 잡는다. 무력화되지 않았다 |
| 대조군 (`test_non_banner_mutation_does_not_report_banner`) | 배너 없는 A1 꼬리 변조 → `BANNER` 아닌 사유로 차단 확인 |
| 블록 원복 | **8 passed** (재현 시 발생한 self-inflicted 실패 1건 — `verify_v2_docs.py` 자기 SHA 불일치 — 은 내 격리 편집 방식의 부산물이며 실물 미해당) |

**"배너 강제 블록 제거 시 4건 실패, 원복 시 8건 통과"** — 커밋 메시지 주장을 그대로 재현했다.
검증기가 매니페스트에 등재되며 자기 마커 리터럴에 걸리던 문제도 `_MARKER = "INSTALLED-BANNER"` →
`f"<!-- {_MARKER}-START -->"` 조립으로 해소됐고, 검사가 약화된 지점은 없다 — 오히려
`CLAUDE.md`·`docs/INDEX.md`·`scripts/verify_v2_docs.py` **필수 3항목 매니페스트 등재 강제**가
새로 추가됐다(코드 직접 확인, `research/landing_accessibility/scripts/verify_v2_docs.py:182-186`).

### 1.2 `execution-authority-has-no-a0-pointer-and-baseline-audit-record-is-one-cycle-stale` (F2) — **부분 CLOSED, 잔여 1건을 새 finding 으로 연다**

| 하위 결함 | 판정 | 근거 |
|---|---|---|
| (a) A0 pointer 부재 | **CLOSED** | §2.5 신설 — `> **정책의 유일한 정의부는 PHASE_GATES.md §4다.**` 직접 인용. `PHASE_GATES §4` 본문과 대조해 정책표 복제 0건(금지 10항·firewall·lane 표 어느 것도 재수록하지 않고 lane명·브랜치·base SHA 표만 등재) |
| (b) §5 기준선 정체 | **부분 CLOSED — ssot 행 미갱신** | 아래 §2 참조. 새 finding |
| (c) §6 감사 이력 V2-C003 에서 정지 | **CLOSED** | V2-C004(Codex 대체 수행 경위 포함) · V2-C005 두 행 추가 확인(`EXECUTION_AUTHORITY.md:185-186`) |
| (d) shadow lane 미등재 | **CLOSED** | §2.5 lane 표에 `agent/landing-pa-shadow`·`agent/landing-pb-prework`·`agent/landing-pc-fixture` 3브랜치 등재. `origin` 실재 확인 + `git merge-base origin/<lane> d5f1da56` = `d5f1da56` 3/3 으로 표기된 base SHA 정확성 확인 |

### 1.3 `a2-rc6-check-a6-run-id-derivation-is-circular-and-unspecified` (F3) — **CLOSED**

| 검사 | 결과 |
|---|---|
| 순환 재발 여부 | **재발 없음.** `planned_evidence_run_id` 필드가 사전선언(`PREREGISTRATION`) 레코드에서 완전히 삭제됐다(§1.11.2 원장 표 재구성 확인) — `record_sha256` 의 입력집합에 더 이상 run id 가 없다 |
| 유도식 명시 | **명시됐다.** `evidence_run_id = "rc" \|\| hex(sha256(b"landing-v2/RC-6/f/v1" \|\| 0x1F \|\| ledger_record_sha256 \|\| 0x1F \|\| countersign_commit_sha \|\| 0x1F \|\| ascii(decimal(execution_index))))[0:32]` — 도메인 분리 프리픽스까지 포함한 완전한 정의 |
| 해석이 하나뿐인가 | **그렇다.** A-6 이 "검증자가 `f` 를 재계산해 바이트 비교" 로 명문화됐다 — 구현자가 임의로 풀 여지가 없다 |
| 새 순환 여부 | **없음.** `f` 의 세 입력(원장 레코드 해시·control countersign 커밋·실행 순번)은 전부 사전선언 레코드 **바깥** 값이다. 등재 레코드(control 측)도 `evidence_run_id` 를 담지 않도록 `FORBIDDEN_evidence_run_id` 로 명문화돼 있어(§3 control↔문서 대조 참고) 대칭적 순환도 막혔다 |
| `A1 §6.3` `observation_id` 충돌 여부 | **없음.** `A1_MEASUREMENT_OPERATIONALIZATION.md` PREV↔TARGET diff 0 (직접 확인) — `observation_id = hash(web_target_id, evidence_run_id, requested_url, …)` 는 `evidence_run_id` 를 불투명 문자열로만 소비하며 그 유도 방식에 의존하지 않는다 |
| adversarial 과의 독해 대립 재현 여부 | **양립.** 이제 A-6 은 "인가 없이는 유효 id 를 계산할 수 없다" 를 기계적으로 강제하는 정의이고, adversarial 이 지적한 "동일 인가 재실행" 경로는 **다른 층**(R-1, 저장소 내부 검사로 배제 불가)으로 옮겨져 명시적으로 "닫히지 않는다" 로 남았다. 두 감사가 반대로 읽을 여지가 있던 자리 자체가 사라졌다 |

### 1.4 `install-integrity-coverage-gap` — **부분 CLOSED, 비차단 유지**

| 대상 | 상태 |
|---|---|
| `docs/v2/MANIFEST.json` | 닫힘(V2-C003) |
| `scripts/verify_v2_docs.py` | **신규 CLOSED** — `INSTALL_MANIFEST.json` `files` 에 `bytes 9108`/`sha256 053ca636...` 로 등재, git-anchored. 독립 재계산 일치 |
| `tests/test_v2_docs_integrity.py` | **잔존 OPEN** — 여전히 미등재. control 도 `verify-script-and-its-tests-are-not-in-the-install-manifest`(비차단, OPEN) 로 같은 사실을 기록한다 |

등급·class **P2 / 비차단 유지** — V2-C002 이래 판정과 동일하다. 필수 3항목 강제 로직이 `CLAUDE.md`·
`docs/INDEX.md`·`scripts/verify_v2_docs.py` 만 지정하고 `tests/`쪽 파일은 대상이 아니어서
이번 시정 범위 밖이다. 차단으로 올리지 않는다 — 실질 커버리지(`docs/v2/**`)와 무관하고
승격 `[EXEC_TREE]` 가 exec 워크트리 clean 을 요구해 우회 표면이 없다.

---

## 2. 신규 blocking — `execution-authority-baseline-ssot-audit-row-not-updated`

**§5 기준선 표의 `ssot audit` 행이 이번 사이클에 갱신되지 않았다.**

```
EXECUTION_AUTHORITY.md:169  | adversarial audit | `audit/landing-adversarial` | V2-C005 @ `b7fd4677202640e8293c9c7a766fb624e88f3925` |
EXECUTION_AUTHORITY.md:170  | ssot audit         | `audit/landing-ssot`         | V2-C003 @ `43fab0acb3a9352c36b9c0b844cb44cef9f9d79d` |
```

`adversarial audit` 행은 `origin/audit/landing-adversarial` tip(`b7fd4677...`, V2-C005)과 문자 그대로
일치한다 — **정확히 갱신됐다.** 반면 바로 아래 `ssot audit` 행은 `V2-C003 @ 43fab0ac...` 를 가리키는데,
실제 최신 ssot 감사는 **이 문서 자신이 §6 감사 이력에 적어 놓은** V2-C005 행이 가리키는
`audit/landing-ssot @ 39e485c85b745e7f34b675e24c54662b83dcea79` 다(`git rev-parse
origin/audit/landing-ssot` 로 직접 확인). 즉 **§6 은 옳고 §5 는 틀렸다** — 같은 문서 안에서
두 절이 서로 다른 사실을 말한다.

**왜 사소하지 않은가.** 이 자리는 V2-C005 의 F2(b)("§5 기준선·§6 감사 이력이 한 사이클 정체")가
정확히 지적한 자리다. 이번 커밋은 그 finding 을 닫는다고 주장하며(`§4 pointer 신설`·`기준선
감사 SHA 갱신`·`감사 이력에 V2-C004 Codex 경위와 V2-C005 추가`) 실제로 §6 은 완전히 고쳤고
§5 의 두 행 중 하나(adversarial)도 고쳤다 — 그런데 **바로 옆 줄(ssot)은 손대지 않았다.** 세
사이클(V2-C004→V2-C005 갱신 누락, 그리고 이번에도) 동안 정체된 값이 남아 있다. "기준선 감사
SHA 갱신"이라는 커밋 메시지 문구는 절반만 참이다.

**차단 범위.** `V2_SSOT_FROZEN`. 강제 경로(`promote_landing_main.sh`)는 원장(`audit_lag.
latest_*_audit_branch`)을 읽으므로 승격 게이팅에 실질 영향은 없다 — 그러나 권위·기준선을
선언하는 문서가 **자기 자신의 §6 이 방금 기록한 사실과 모순**되는 것은 F2 와 같은 종류의
권위 결함이며, "동결"은 이 문서가 정확하다는 선언이다. 문면 수정 1줄이며 P-C 를 막지 않는다.

**처방.** `EXECUTION_AUTHORITY.md:170` 을
`| ssot audit | \`audit/landing-ssot\` | V2-C005 @ \`39e485c85b745e7f34b675e24c54662b83dcea79\` |`
로 고친다. §6 행과 §5 행이 항상 같은 최신 사이클을 가리키도록 승격 스크립트나 리마인더에
교차검증을 추가하면 재발을 막을 수 있다(두 값을 매 사이클 함께 갱신하는 체크리스트 항목화 권장).

---

## 3. A0 재수집 앵커 재설계 — 내부 정합성

### 3.1 `EXECUTION` 레코드 vs `02 §12` append-only · EXC-4

원장 레코드 종류가 2종(`PREREGISTRATION`/`DISPOSITION`)에서 3종(`+EXECUTION`)으로 늘었으나
**append 전용 규율은 문면 그대로다** — "레코드는 세 종류이고 셋 다 append 로만 쓴다. 기존 줄을
고치지 않는다." EXC-4 도 "실행 1회마다 control 인가 1건이 필요하고 `evidence_run_id`가 그 인가에서
유도된다"는 문구만 추가됐을 뿐 `02 §11`·`02 §12` 축자 인용은 그대로다. append-only 위반 0건.

### 3.2 A-7 · A-8 vs RC-1~RC-7

A-7·A-8 은 **RC-6 의 하위 검사**(`검사 A-1~A-8`)로만 추가됐다 — RC 규칙 번호 체계(RC-1~RC-7)에
새 RC 를 삽입하지 않았으므로 기존 규칙과 이름 충돌이 없다. `X-14` 에 ⑧항이, 실패주입 표에
I-38~I-41 이 각각 새 검사에 1:1 대응해 추가됐고 기존 I-1~I-37(+28b) 는 손대지 않았다(전수 확인,
결번·중복 0).

### 3.3 R-1 세 자리 일관성

| 자리 | 문면 |
|---|---|
| §1.11.2 잔여 위험표 R-1 | "인가된 **하나의** `evidence_run_id`로 로컬에서 K회 실행한 뒤 1회만 커밋한다 … **저장소 내부 검사로는 배제 불가**" |
| 닫힘 논거표 | "은닉한 *실행*이 결론을 고르는 것 — 인가된 같은 id로 K회 로컬 실행 후 1회만 커밋 → **닫히지 않는다**. 아래 잔여 위험 R-1" |
| I-41 | "인가된 하나의 id로 로컬에서 K회 실행한 뒤 결과가 가장 좋은 1회만 커밋한다 → **차단되지 않는다** … §1.11.2 잔여 위험 R-1" |

**세 곳이 같은 시나리오·같은 결론(닫히지 않음)을 말하며 서로를 명시적으로 참조한다.** V2-C005
adversarial 이 지적한 옛 ":1334" 단언("은닉은 정본 선택에 아무 이득을 주지 못한다")은
"동일 앵커 재실행에 대해 거짓이었으므로 철회한다"고 명문 철회되고 세 행(인가받지 않은 id=닫힘 /
인가받고 숨김=닫힘 / 동일 인가 재실행=닫히지 않음)으로 분해됐다. 과탐 없이 정직하다 — control
의 `open_blocking_total` 도 이 R-1 을 별도 orchestrator 등재 finding
(`rc-6-r1-same-authorization-local-reexecution-unbounded`)으로 계상해 두어 "새 설계가 닫는 것"과
"닫지 못하는 것"이 이중 계상 없이 갈려 있다.

### 3.4 `f` vs `A1 §6.3`

`A1_MEASUREMENT_OPERATIONALIZATION.md` 는 이번 사이클 diff 0(직접 확인). `observation_id = hash(
web_target_id, evidence_run_id, requested_url, …)`(577행)는 `evidence_run_id` 를 불투명 문자열로
소비할 뿐 그 유도 방식을 규정하지 않으므로 새 `f` 정의와 충돌하지 않는다.

---

## 4. control ↔ 문서 정합

### 4.1 `recollection_prereg_anchors.record_schema`

control `state.json` 이 `schema_version: landing-accessibility-recollection-prereg-anchor-v2` 로
갱신하며 A2 재설계와 **필드 단위로 대응**한다.

| 요구 항목 | control `record_schema` 실측 |
|---|---|
| `planned_evidence_run_id` 제거 | `REMOVED_planned_evidence_run_id` 명시 (사유까지 A2 와 동일하게 기재) |
| `execution_index` 추가 | 필드로 존재, "`f` 의 세 번째 입력" 이라 명시 |
| `evidence_run_id` 금지 명문화 | `FORBIDDEN_evidence_run_id` — "넣으면 id 가 자기를 담은 커밋 SHA를 입력으로 갖게 되어 ssot V2-C005 F3 와 같은 종류의 순환이 재발한다"고 **내 finding ID 를 정확히 인용**하며 금지 |
| `absence_proof` 제거 | `REMOVED_absence_proof` — "control 쪽에서는 성립하지 않는다. 부재 증명은 exec 쪽 A-3 이 사후 수행한다"로 A2 와 동일한 논거 |
| invariant A-7·A-8 | `invariants` 배열에 "(ledger_record_sha256, execution_index) 하나에 SIGNED record 최대 1건 (A-7)" · "execution_index 는 1..E 조밀 (A-8)" 그대로 존재 |
| 검사 범위 A-1~A-8 | `authority.definition_of_record`: "검사 **A-1~A-8**" |

**필드·금지·불변식 6항목 전부 A2 와 정합한다.** 어긋난 지점 없음.

### 4.2 `a2_document_state` — F5 CLOSED 재확인

```
control state.json (V2-C006 기록): bytes 214013 / sha256 40afa5d5be996405d9ad47221d1f10d76e894a72f0bbe3d008355837cabdc478
독립 재계산 (target A2 실물, git show 로 직접 sha256sum):     bytes 214013 / sha256 40afa5d5be996405d9ad47221d1f10d76e894a72f0bbe3d008355837cabdc478
INSTALL_MANIFEST.json 선언:                                    bytes 214013 / sha256 40afa5d5be996405d9ad47221d1f10d76e894a72f0bbe3d008355837cabdc478
```

**3자 전건 일치.** 지난 사이클 F5("어떤 브랜치의 A2 와도 맞지 않는다")가 **CLOSED** 됐다.
control 자신도 `supersedes` 블록에 "ssot V2-C005 F5 가 지적했다. 이번에 값을 갱신한다"고
자진 기록해 두었다.

### 4.3 shadow lane 3브랜치 · base SHA

`agent/landing-pa-shadow`(`0f46203`) · `agent/landing-pb-prework`(`9999857`) ·
`agent/landing-pc-fixture`(`0c36c95`) — `origin` 실재 확인 3/3, `git merge-base origin/<lane>
d5f1da5652953542d5c8be377026cc3293f2075a` = `d5f1da5652953542d5c8be377026cc3293f2075a` 3/3.
`EXECUTION_AUTHORITY §2.5` lane 표의 base SHA 표기가 정확하다.

---

## 5. `open_blocking_total = 14` — 직접 재계산

```
v1 승계           6   (debt_ledger.triage.counts.E001_BLOCKING = 6, items[] blocking 집합과 일치)
V2-C002 잔존       1   (ned-ied-path-minimality-not-operationalized)
orchestrator 등재  3   (ssot-00-3-auth-gate-endpoint-contradicts-a2-1-5-1 · e-6a-accepts-misclassified-gate-kind-and-silently-flips-endpoint · rc-6-r1-same-authorization-local-reexecution-unbounded)
V2-C005 신규       4   (recollection-escape-path-unbounded-and-conclusion-prioritized · banner-policy-regression-tests-pass-vacuously[adv·ssot F1 병합 1건] · execution-authority-has-no-a0-pointer...[ssot F2] · a2-rc6-check-a6...[ssot F3])
──────────────────────
합계              14
```

`open_blocking_ids` 배열을 직접 세면 **14개**이며 합계와 일치한다(v1 6 + C002 1행 + orchestrator 3행 +
C005 4행 순서로 배열 순서도 카테고리별로 정렬돼 있음을 확인). `by_blocking_class`
(`V2_SSOT_FROZEN-blocking 5 + E001_V2-blocking 3 + v1_inherited 6 = 14`)도 같은 14 로 재분류될 뿐
합계는 동일 — **세 경로 모두 14 로 일치, 문서와 어긋나지 않는다.**

주의: F1·F3 가 이번 감사로 CLOSED 판정을 받았지만 `open_blocking_total` 은 **아직 차감되지 않는다** —
`banner-policy-regression-tests-pass-vacuously` 는 control 이 `REMEDIATION_CLAIMED_PENDING_AUDIT` 로
self-approval 금지 원칙에 따라 열어 두고 있고, `a2-rc6-check-a6...` 도 마찬가지다. **이 감사 보고서가
그 판정을 제공한다** — 다음 orchestrator reconciliation 에서 두 항목(및 F2 의 pointer/history/lane
서브이슈)이 차감되고, 대신 이번에 새로 연 `execution-authority-baseline-ssot-audit-row-not-updated`
1건이 반영되어야 한다.

---

## 6. `PHASE_GATES §4 → §5` 인용 회귀 — target·control 양쪽 재확인

| 위치 | 사이클 | 상태 |
|---|---|---|
| target tree 전체 | — | **0건** (V2-C005 부터 계속 0건) |
| `promote_landing_main.sh:424` | V2-C005 에서 stale, V2-C006 시정 | **CLOSED** — `"PHASE_GATES 「판정 권한」절 = §5: … 절 제목으로 부른다 — 절 번호는 절 신설로 밀린다"` 로 절 제목 인용으로 교체 확인 |
| `control/cycles/V2_C005_orchestrator.json:52,198` | V2-C005 에서 stale | **CLOSED** — `「판정 권한」절(당시 §4, A0 §4 SHADOW 신설 후 =§5)` 로 양쪽 명시 |
| `control/state.json:2670` (`definition_of_record`) | 새 §4(SHADOW) 를 가리켜야 함 | **정확** — `「SHADOW / PREPARATORY WORK — A0 결정」절 = §4` 로 옳게 지목하고, "예전 §4 판정 권한은 §5 로 밀렸다" 를 병기해 혼동 지점을 스스로 해소 |

전수 grep(`git grep "PHASE_GATES §4[^.]"`) 결과 target·control 어디에도 stale 인용이 남지 않았다
(유일한 매치는 `V2_C006_orchestrator.json` 자신의 "이번에 이걸 고쳤다"는 시정 로그 문장뿐).
`PHASE_GATES.md` 실제 절 번호(`§0`~`§3` 무변경 · `§4` SHADOW 신설 · `§5` 판정 권한)와도 직접
대조해 확인했다. **CLOSED.**

---

## 7. 설치 무결성 — 독립 실측 (전건 통과, 다섯 사이클 연속)

| 검사 | 결과 |
|---|---|
| `INSTALL_MANIFEST.json` 15항목 `bytes`/`sha256` 독립 재계산 | **15/15 일치, 불일치 0**(신규 1건 — `scripts/verify_v2_docs.py` 자기등재, `bytes 9108`/`sha256 053ca636...`) |
| A2 갱신 항목 | `bytes 214013` / `sha256 40afa5d5...` — **실측 일치** |
| 배너 3종 — 배너 제외 본문 ↔ `SSOT/` 원본 | `CLAUDE.md`·`docs/v2/README.md`·`bootstrap/07…` **3/3 바이트 동일**, `body_sha256`·`banner_sha256` 3/3×2 일치, 마커 정확히 1쌍·offset 0 |
| `00`~`05` 6종 ↔ `SSOT/` 원본 | **6/6 IDENTICAL** |
| PREV↔TARGET 무변경 확인 | `00~05`·`MANIFEST.json`·`README.md`·`bootstrap/07`·`CLAUDE.md`·`docs/INDEX.md`·`A1`·`PHASE_GATES.md` **전건 diff 0** |
| 실물 검증기 | 격리 복제본에서 `pytest tests/test_v2_docs_integrity.py` **8 passed**(배너 강제 포함 상태) |
| 배너 강제 무력화 여부 | **무력화되지 않았다** — 강제 제거 시 4/5 파라미터화 테스트가 정확한 사유로 실패, 1건(00_SSOT)은 독립 경로로 여전히 잡힌다 |
| 필수 3항목 강제 | `verify_v2_docs.py:182-186` 코드 직접 확인 — `CLAUDE.md`·`docs/INDEX.md`·`scripts/verify_v2_docs.py` 3종 미등재 시 `UNDECLARED` 실패 |
| 커버리지 잔여 gap | `tests/test_v2_docs_integrity.py` 미등재(비차단, §1.4 참조) |

---

## 8. 경계 준수

| 경계 | 실측 |
|---|---|
| Pilot `research/refcohort/**` | BASE→TARGET diff 0, PREV→TARGET diff 0 |
| root `CLAUDE.md` | BASE→TARGET diff 0 |
| `state/*.parquet` | BASE→TARGET diff 0 |
| `E001_V2_STARTED` | `false`, `FULL_COLLECTION_STARTED = NO`(EXECUTION_AUTHORITY diff 확인 — 변경 없음) |
| `recollection_prereg_anchors.entries` | `[]` — P0 종료 전이므로 정상 |
| 워크트리 | 감사 전후 `git status --porcelain` = 0. **1회 실수**(`git checkout <target> -- .`)가 있었으나 커밋 전 `git reset --hard HEAD` 로 즉시 원복, push 없음 |

---

## 9. 비차단 관찰

1. **A2 §7 닫는 finding 표의 id 표기 불일치.** A2 `§7`(finding 닫기 표)가 F3 을 `rc-6-a-6-circular-derivation`
   이라는 shorthand 로 적는데, 실제 등재 finding id(내 V2-C005.json·control `open_blocking_ids` 양쪽)는
   `a2-rc6-check-a6-run-id-derivation-is-circular-and-unspecified` 다. 내용은 정확히 맞물리므로
   추적에 지장은 없지만, 다음 커밋에서 정식 id 로 맞추면 grep 기반 추적이 쉬워진다.
2. **`open_blocking_total` 아직 미차감.** F1·F3(및 F2 의 세 하위결함)이 이번 감사로 CLOSED 판정을
   받았으나 self-approval 금지 원칙에 따라 `open_blocking_total` 자체는 orchestrator reconciliation
   전까지 14 로 유지된다 — 정상 동작이다.
3. **A-3 의 위상 재정의.** A2 가 스스로 "A-3 은 A-6 의 귀결에 가깝다 — 독립 근거로 세지 않는다"고
   적었다(옛 F6 논리를 A-3 에도 선제 적용). 정직한 자기평가이며 검사 개수 과대표시를 막는다.

---

## 10. 종합

| Gate | 판정 |
|---|---|
| `V2_SSOT_FROZEN` | **FAIL — 계속 OPEN, 그러나 큰 진전** |

내 V2-C005 blocking 3건 중 **2건(F1·F3) CLOSED**, 1건(F2)은 pointer·history·lane 서브이슈
전부 닫히고 **기준선 SHA 서브이슈만 미완**으로 남았다. 비차단 3건(F4·F5·F6) **전부 CLOSED**.
A0 재수집 앵커 재설계는 내부적으로 일관되고(EXECUTION 레코드·A-7·A-8·R-1 세 자리·`f` 유도식
전부 상호 정합), control 문서와도 필드 단위로 맞물린다. 설치 무결성은 다섯 사이클 연속
완전 통과이며 이번에도 스크립트를 신뢰하지 않고 전부 다시 계산했다.

새로 여는 것은 1건뿐이다 — **F2 를 닫는다고 주장한 바로 그 표에서, 방금 고친 줄 옆줄이
그대로 남았다.** 문면 1줄 수정이며 P-C 를 막지 않는다.

`research/landing-accessibility-main` 승격 · P-A 착수 · `E001_V2` 실행은 **금지 상태를 유지한다.**
