# V2-C001 / SSOT · Authority Audit

| 항목 | 값 |
|---|---|
| cycle | `V2-C001` |
| auditor | `ssot` (SSOT / Authority Auditor) |
| target | `agent/landing-v2-exec` @ `eb36d173182a582e8d7499f29170a83363f9d560` |
| base | `research/landing-accessibility-main` @ `5a9015d1e95b15304aaf53a73efb475934610b82` |
| 감사일 | 2026-08-26 |
| **verdict** | **FAIL** |
| 집계 | P0 **0** / P1 **2** / P2 **11** (blocking 9 · non-blocking 2 · P1 포함 총 blocking 11) |

> **FAIL 사유.** 권위 붕괴(P0)는 없다. 설치 무결성·기준선 SHA·v1/v2 권위선언은 모두 정확하다.
> 다만 `01`/`02`가 `FROZEN FOR IMPLEMENTATION`으로 선언된 상태에서 **Axis B의 핵심 변수인
> NED/IED가 측정명세만으로는 산출 불가능**하고(F1), **이번 단계가 닫아야 할 다음 gate 이름
> `V2_SSOT_FROZEN`이 비권위 문서에만 존재**한다(F8). 두 건 모두 P1 정의로 "이 단계에서 반드시
> 해결"에 해당하므로 동결·승격 전에 시정이 필요하다.

변경 전량은 **순수 추가 14파일 / +2432행 / 삭제·수정 0**이다.
`git diff 5a9015d1 eb36d17 --stat` 확인. root `CLAUDE.md` diff 공집합, `research/refcohort/**` 무변경.

---

## 0. 감사 범위와 방법

`git fetch --all --prune` 후 `git show eb36d17:<path>`로 다음 14개 설치물 전량을 통독했다.

```
research/landing_accessibility/CLAUDE.md
research/landing_accessibility/docs/INDEX.md
research/landing_accessibility/docs/v2/{00..05}_*.md
research/landing_accessibility/docs/v2/{README,EXECUTION_AUTHORITY}.md
research/landing_accessibility/docs/v2/{MANIFEST,INSTALL_MANIFEST}.json
research/landing_accessibility/docs/v2/bootstrap/07_CLAUDE_FIRST_SESSION_PROMPT_v2.0.md
research/landing_accessibility/scripts/verify_v2_docs.py
```

추가로 현행 유효로 선언된 v1 산물 `docs/07_EVIDENCE_MANIFEST_CONTRACT.md` 본문을 통독하고,
`state/*.csv` 5종의 실제 행수·컬럼과 remote 8개 브랜치 HEAD를 직접 확인했다.

---

## 1. 검사 1 — v2 문서 내부 일관성 : **FAIL**

### 1.1 통과 항목

| 검사 | 결과 | 근거 |
|---|---|---|
| `PASS/FAIL/UNDETERMINED/NA` 어휘 | PASS | 00 §4 · 01 §11 · 04 — 동일 4값, 동일 의미. 02 §14 실패주입에 `UNDETERMINED→PASS 시도` 차단이 명시돼 laundering 금지도 일관 |
| archetype 7종 | PASS | 00 §6 · CLAUDE.md(간접) · bootstrap A4 모두 `QUERY / CONTENT_OPEN / ITEM_DETAIL / PLACE_LOOKUP / COMMUNICATION_ENTRY / FINANCIAL_ACTION_ENTRY / UTILITY_ENTRY` 7종 동일. 03에는 목록이 없으나 `Interaction Archetype`을 00 참조로만 쓰므로 모순 아님 |
| `older_relevance` 4태그 | PASS | 00 §4와 01 §7이 `VISION / MOTOR / COGNITIVE_NAVIGATION / OTHER`로 완전 일치 |
| MPFED/NED/IED/ExcessDepth **정의** | PASS | 00 §7 `MPFED = NED + IED`, `ExcessDepth = MPFED - 같은 archetype의 중앙값` / 04 `MPFED\|NED + IED` / CLAUDE.md `ExcessDepth = MPFED - archetype median` — 수식·의미 동일 |
| `HUMAN_FINAL_REVIEW_MAX = 5` | PASS | 00 §9 `최대 5건` · 02 §1 `인간 최대 5건` · 03 Phase5 `human escalation ≤5` · 04 `최대 5건` · CLAUDE.md `= 5` · EXECUTION_AUTHORITY §1 `= 5`. 6개 문서 동일 값·동일 의미 |
| 수집 상수 단일 정의 | PASS | `390×844` · `ko-KR` · `Asia/Seoul`은 **02 §2에만** 존재. 01 §4의 `viewport_width`/`viewport_height`는 관측별 기록 컬럼이므로 중복 정의가 아니다. 충돌 0 |

`ExcessDepth` 관련해 00 §7의 `Depth >= 3 = 나쁨 같은 기준은 만들지 않는다`와 CLAUDE.md의
`depth >= N = bad 같은 임의 threshold 금지`도 동일 규범이다.

### 1.2 02 §14 smoke 부분집합 판정 — **정상, finding 아님**

02 §14는 7개 archetype 중 5개(`QUERY / CONTENT_OPEN / ITEM_DETAIL / PLACE_LOOKUP /
FINANCIAL_ACTION_ENTRY`)만 열거하고 `COMMUNICATION_ENTRY`·`UTILITY_ENTRY`를 뺐다.

**판정: 정상.** §14는 `본수집 전에 8~12개 서비스로 반드시 검증한다`로 표본 상한을 스스로
제한하고, 목록은 archetype 전수가 아니라 **위험차원 커버리지**(popup-heavy / motion-heavy /
auth gate / ID collision / certified 혼합)로 구성돼 있다. 전수 커버리지를 8~12개 서비스에
요구하면 위험차원 커버리지가 희생된다. 부분집합은 설계상 정당하며 상위문서와 모순되지 않는다.

다만 `AUTH_GATE_REACHED` 종료경로는 00 §3의 **금융과 커뮤니티 두 유형**에서 발생하는데
smoke는 금융만 태운다. 권고(비-finding): §14에 `COMMUNICATION_ENTRY` 1건 추가 또는
"미포함 archetype은 의도적 연기"라는 한 줄 명시. 강제사항으로 올리지 않는다.

### 1.3 FAIL 사유 — F1, F6, F7 (아래 §7 상세)

상태값 어휘가 **문서 간에는** 일치하나, **컬럼별 허용값 집합이 어디에도 열거돼 있지 않다.**
`measurement_status`(01 §4)의 값 집합을 01 §11은 `MEASUREMENT_FAILED` 하나로, 02 §13은
`ACCESS_BLOCKED` 외 4조건으로 서로 다르게 말한다.

---

## 2. 검사 2 — Data Spec ↔ Collection Spec 연결 : **FAIL**

지시받은 5개 대조를 전부 수행했다.

### 2.1 `fact_interrupt_element`(01 §5) ↔ 02 §5

29개 필드 중 **23개는 연결된다.**

| 필드군 | 02 대응 |
|---|---|
| `selector` `candidate_source` `z_index` `position_type` `aria_modal` `role_dialog` `backdrop_detected` `body_scroll_lock` | §5 1차 후보 (`<dialog>` / `role=dialog` / `aria-modal` / fixed·sticky / 고 z-index / backdrop / scroll lock / pointer interception / focus containment) |
| `bbox_*` `viewport_intersection_area` `overlay_coverage` | §5 2차 공간검사 + §3 bounding box |
| `blocks_primary_action` `primary_action_occlusion` | §5 3차 blocking 여부 |
| `interrupt_type` `classification_status` `ai_review_status` `final_label` | §5 4차 의미분류 + §10 evidence package |

**고아 필드 6개** → F2. `dismiss_control_exists` / `dismiss_control_visible` /
`dismiss_control_accessible_name` / `dismiss_control_width` / `dismiss_control_height` /
`dismiss_succeeded`.

### 2.2 `fact_task_entry` · `fact_task_step`(01 §6) ↔ 02 §7~§9

`fact_task_step` **15필드 전건 연결 확인.** `state_before_id`↔`DOM state key`,
`control_*`↔§7 `clicked control`+§6 추출항목, `action_type`↔§9 activation 예시,
`screenshot_before/after`↔§11 `Task step도 before/after evidence가 trace와 연결되어야 한다`,
`modal_encountered`↔`popup`, `auth_gate_detected`↔`auth gate`,
`endpoint_signal_detected`↔`endpoint signal`. 고아 없음.

`fact_task_entry` — `endpoint_status`는 02 §7의 7개 종료상태와 정확히 대응하고,
`path_manifest_path`는 §8 `task manifest`, `auth_gate_before_endpoint`·`redirect_count`도 연결된다.
**고아 필드 4개**: `NED` `IED`(→ F1), `text_input_episode_count` `scroll_episode_count`(→ F3).

### 2.3 `fact_criterion_result`(01 §7) ↔ 02 §4

`applicable_count` / `pass_count` / `fail_count` / `undetermined_count`는 02 §4의
`probe는 판정하지 않고 raw feature만 저장한다` + 03 M1 `raw browser feature → criterion
opportunity → verdict` 경로로 산출 가능하다. **연결 OK.**
`criterion_id` 목록(KWCAG subset)이 아직 없는 것은 00 §15·03 P-C가 명시적으로 미래 작업으로
지정한 것이므로 결함이 아니다.
**고아 1개**: `automation_grade` — 전 문서에서 01 §7 단 한 번만 등장, 정의·값집합 없음(→ F7).

### 2.4 `fact_ai_adjudication`(01 §9) ↔ 02 §10

14필드 전건 연결. `deterministic_label`→`semantic_model_label`→`reviewer_a/b_label`→
`arbiter_label`→`human_required` 사슬이 00 §9 cascade 6단계와 순서까지 일치하고,
`evidence_package_id`는 02 §10 `항상 evidence package만 전달한다`에 대응한다.
`출력은 JSON classification only. 자유로운 새 기준 생성 금지`도 `arbiter_label` 값 통제와 정합.
**고아 없음.** 단 00 §9가 규정한 `ABSTAIN`을 담을 컬럼이 지정돼 있지 않다(→ F7).

### 2.5 evidence 경로 필드(01 §4) ↔ 02 §11~§12

`dom_path` / `ax_path` / `probe_path` / `manifest_path`는 02 §11의
`한 observation은 정확히 다음과 대응해야 한다 — DOM / AX / screenshot / probe / manifest`와
1:1 대응한다. `hash-based observation id 사용` 및 §12 append-only도 01 §4 grain
(`한 행 = 한 웹 타겟의 한 수집회차`)과 모순 없다. **연결 OK.**

`screenshot_path`만 단수라 02 §3이 수집하는 full-page screenshot의 자리가 없다(→ F5).

### 2.6 역방향 — 02가 수집하나 01에 자리 없음

- 02 §3 `full-page screenshot`, `computed CSS` → 저장 경로 컬럼 없음 (F5)
- 02 §2 `수집시각 기록` → 01 §4는 `audit_date`(일 단위)만 (F5)
- 02 §3 `primary-action candidate` → 후보 자체(selector·bbox)를 담는 표·컬럼 없음 (F4)
- 02 §13 `app-only` → 01 §3 `web_eligibility_status`와 01 §4 `measurement_status`가 동시에 주장 (F6)

### 2.7 Mart 산출가능성 — **PASS**

`mart_service_summary` 12항목: KWCAG V/M/C fail·undetermined ← `fact_criterion_result`
(`older_relevance` × `final_status`), MPFED·ExcessDepth ← `fact_task_entry` + archetype 중앙값,
modal·overlay·forced dismissal ← `fact_interrupt_element` + `fact_task_entry`,
primary action visibility·auth gate ← `fact_landing_observation`·`fact_task_entry`,
certification ← `dim_certification`. **전건 상류 산출 가능.**

`mart_archetype_summary` 5항목(n / MPFED median / IQR / endpoint reach / modal prevalence /
KWCAG summary)도 `dim_representative_task.interaction_archetype` group-by로 전건 산출 가능.
00 §11의 `median / IQR / mode / ECDF` 우선 규칙과도 정합.

단 `decision coverage`(01 §10, 03 Phase 5)는 분자·분모 정의가 없다(→ F7).

---

## 3. 검사 3 — CRISP-DM ↔ SSOT 정합 : **PASS(내용) / FAIL(gate 이름)**

### 3.1 Phase 정합 — 모순 0

| 대조 | 결과 |
|---|---|
| 03 Phase 1~6 ↔ 00 전반 | Phase1 완료조건(문제정의·L0/L1 범위·축 A/B/C·금지주장)이 00 §1~§4·§14와 1:1 |
| 03 EDA-00 factual claim ↔ 실제 데이터 | `source 261` `panel 17` — 실측 `source_ranking_rows` 261행 / `panel_registry` 17행 **일치**. 00 §12의 `261 Source Rows → 81 Entities`도 `service_master` 81행 **일치** |
| 03 Phase 5 ↔ 00 §11 | Depth median/IQR/mode/ECDF · Kruskal–Wallis · Spearman · Fisher · leave-one-* · UNDETERMINED stress — 검정 목록 동일 |
| 03 Phase 3 ↔ 01 서두 | `기존 state/*.parquet는 그대로 둔다` ↔ `삭제·rename·migration하지 않는다` 동일 |
| 03 P-E ↔ 00 §15 | 00 §15의 14개 종료조건은 03 P-B(target/task frozen)·P-C(collector/detector/cascade validated)·P-D(smoke)·P-E(2감사·blocking 0·STOP)의 합집합으로 **전건 커버**. P-E가 체크리스트를 재기술하지 않을 뿐 누락·모순 없음 |
| 03 P0 ↔ 05 §7 | 05 §7 7단계 중 03 P0는 5개만 열거(`v2 execution directive 생성`·`orchestrator reconcile` 미기재). **부분집합이며 모순 아님** — 본 커밋은 `EXECUTION_AUTHORITY.md`로 directive를 실제 생성했다 |
| 03이 요구하는 산출물 | Phase3의 `L0/L1 feature schema`·`evidence identity`·`mapping layer`, Phase4 M0~M4, Phase6 `analysis marts` 모두 01(표)·02(수집절차)에 정의처가 있다 |

### 3.2 Gate 이름 — **FAIL** (→ F8)

지시대로 실측 grep한 결과:

```
V2_SSOT_FROZEN                    → bootstrap/07:174  (only)
ANALYSIS_AND_TASK_CODEBOOK_FROZEN → bootstrap/07:226  (only)
TARGET_TASK_FRAME_FROZEN          → bootstrap/07:260  (only)
L0_L1_ENGINE_READY                → bootstrap/07:327  (only)
READY_FOR_E001_V2                 → 00:456 · 03:198 · CLAUDE.md:106,120 · EXECUTION_AUTHORITY:96 ✅
V2_REFREEZE                       → 05:120 · EXECUTION_AUTHORITY:4 ✅
```

**4개 gate 이름이 SSOT pack 어디에도 없고, 비권위로 격리된 부트스트랩 프롬프트에만 존재한다.**

---

## 4. 검사 4 — 프로젝트 CLAUDE.md : **PASS(경로·역할) / P2(권위 프레이밍)**

| 검사 | 결과 |
|---|---|
| 참조 경로 실재성 | **PASS.** `research/landing_accessibility/docs/v2/00_SSOT_v2.0.md` 실재. 하위 01~05는 동일 디렉터리 bare name, 전건 실재 |
| v2만 current로 읽히는가 | **PASS.** `현재 연구의 최상위 실행권위: …/docs/v2/00_SSOT_v2.0.md` + `기존 v1 문서는 역사·회귀검증용이며 v2와 충돌하면 v2가 우선한다` |
| Hard Scope 상속 | **PASS.** 금지목록 6종이 00 §3 `절대 제외` 9종의 부분집합이며 상충 없음 |
| root CLAUDE.md 역할분리 | **PASS.** 05 §5가 `Root CLAUDE.md는 건드리지 않는다`를 명시하고, nested 파일이 `Root CLAUDE.md의 환경규칙은 그대로 상속한다. 이 파일은 연구 하위프로젝트의 실행규칙만 정의한다`로 선언. **root 무변경 실측 확인** (`git diff 5a9015d1 eb36d17 -- CLAUDE.md` 공집합) |

### 설치 지시문 잔존 판정 — **오해 없음, finding 아님**

```
> 이 파일의 실제 설치 위치: `research/landing_accessibility/CLAUDE.md`
```

**판정: 오해를 낳지 않는다.** 이 문장은 설치 후 문맥에서 **사실 진술로 참**이며(파일이 실제로
그 경로에 있다), 미이행 지시로 읽히지 않는다. 오히려 파일이 옮겨졌을 때 원위치를 알려주는
앵커로 기능한다. 시정 불요.

### 실제 문제 — F9

CLAUDE.md는 Claude Code가 **자동 로드하는 진입점**인데, `docs/v2/EXECUTION_AUTHORITY.md`와
`docs/INDEX.md`를 한 번도 가리키지 않으며, EXECUTION_AUTHORITY §2가 자신에게 부여한
`원본과 충돌 시 원본 우선`이라는 **종속 지위를 자기 본문에 담고 있지 않다.**

---

## 5. 검사 5 — v1/v2 권위 모호성 : **PASS (모순 0)**

### 5.1 §2 · §3 · INDEX 삼자 대조 — 모순 없음

| 자산 | EXECUTION_AUTHORITY §2/§3 | INDEX.md | 일치 |
|---|---|---|---|
| `v2/00`~`v2/05` | 권위 1~6위 | Current(v2) 6행 동일 순서 | ✅ |
| `CLAUDE.md` | `— / 프로젝트 컨텍스트 요약, 원본 우선` | `../CLAUDE.md — 프로젝트 컨텍스트 (v2 파생)` | ✅ |
| `v2/README.md` | 비권위 `읽기 순서 안내` | `읽기 순서는 v2/README.md` | ✅ |
| `bootstrap/07` | `NON_AUTHORITATIVE_BOOTSTRAP_RECORD` | 미기재 | 모순은 아님(누락) |
| `07_EVIDENCE_MANIFEST_CONTRACT.md` | §3 `현행 유효` | `Current (v1 상속, 유효)` | ✅ |
| v1 SSOT / DIRECTIVE v5.0 / 00~06 / HANDOFF | §3 `control/landing-orchestrator` 브랜치 | `control/landing-orchestrator 브랜치에 보존, 이 브랜치에는 없다` | ✅ **실측 확인** |

`origin/control/landing-orchestrator`에 11개 v1 문서가 §3 기재 경로 그대로 실재함을
`git ls-tree`로 확인했다. `eb36d17` 트리에 v1 00~06이 없는 것도 확인 — INDEX 기술과 정확히 일치.

`SUPERSEDED_FOR_SCOPE / OPERATIONAL_GUARDS_INHERITED` 정의(§3)가 상속한다고 선언한 7개 가드는
05 §6이 열거한 8개 규칙과 충돌 없이 포함관계다.

### 5.2 `07_EVIDENCE_MANIFEST_CONTRACT.md` ↔ 02 §11~§12 — **본문 대조 결과 모순 없음. P1 아님**

| 02 조항 | 07 계약 본문 | 판정 |
|---|---|---|
| §11 `한 observation은 정확히 DOM/AX/screenshot/probe/manifest와 대응` | §3 manifest 필드에 `observation_id`(어느 관측의 산출물인가) + `relpath` 보유 | 정합 |
| §11 `display name을 file id로 사용하지 않는다. hash-based observation id 사용` | §3 `sha256` 필수 + §4 `(observation_id, relpath)` 중복 → `MalformedRunManifestError` | 정합·강화 |
| §12 `같은 evidence를 덮어쓰지 않는다 / 재수집 → 새 evidence run` | §3 `사후에 관측을 추가하거나 삭제하면 manifest가 어긋난다` + `write_run_manifest()` 결정적 바이트 고정 | 정합 |
| §13 수집실패를 접근성 FAIL로 세지 않음 | §4 `MANIFEST_WELL_FORMED_FILES_NOT_CHECKED`를 `VERIFIED`와 분리, `검사하지 않은 것을 통과로 세지 않는다` | **동일 규범** |

07 §5가 `이 모듈은 네트워크에 접근하지 않는다. E001 본수집과 무관하며, 수집을 승인하지도 않는다`로
스스로 범위를 좁혀 v2 gate(00 §15 `evidence manifest validated`)를 침범하지 않는다.

**결론: 모순 0. P1 아님.** 잔존 사안은 권위서열 미기재(F9)와 manifest grain 명시(F9 부속) 두 가지의 명확화뿐이다.

### 5.3 README 기준선 문구 ↔ §4 — **PASS**

README `authoritative verified baseline: research/landing-accessibility-main @ 5a9015d1e95b…`
= EXECUTION_AUTHORITY §4 1행 = 00 헤더 `현재 기준선` = **실제 remote HEAD**. 3문서 + 실측 4중 일치.
`preserved unverified WIP: agent/landing-exec @ 87a0464e8159d5526069d5e654e648b0dae506ca`도 동일.

### 5.4 bootstrap/07 격리 — **구조적 격리 O / 파일 내부 표식 X** (→ F11)

격리 장치 3중(`bootstrap/` 하위 디렉터리 배치 · §2 비권위 표 · INSTALL_MANIFEST
`authority: NON_AUTHORITATIVE_BOOTSTRAP_RECORD`)은 실재한다. 그러나 파일 본문 1~7행은
`너는 이 프로젝트의 Main Orchestrator … 다` 로 시작하는 **명령형 실행지침**이며 내부 표식이 없다.

---

## 6. 추가 검사 6·7·8

### 6.1 설치 무결성 — **PASS**

```
$ python3 …/landing_v2_exec/research/landing_accessibility/scripts/verify_v2_docs.py
OK       docs/v2/00_SSOT_v2.0.md   … (10건 전부 OK)
V2_DOCS_VERIFY: PASS            exit=0
```

스크립트 신뢰 없이 **독립 `sha256sum` 대조**를 별도 수행했다. `SSOT/` 원본 ↔ `eb36d17` blob:

| 파일 | sha256 | 일치 |
|---|---|---|
| 00_SSOT_v2.0.md | `2176c295…8f502` | ✅ |
| 01_DATA_SPEC | `c325c107…18bd43` | ✅ |
| 02_COLLECTION | `2bd3a77e…74ce8b` | ✅ |
| 03_CRISP_DM | `3e82eca6…13d19b94` | ✅ |
| 04_GLOSSARY | `8ad4d78c…04f451` | ✅ |
| 05_REPO_ORCH | `3ade1db7…e1dfddd5` | ✅ |
| README.md | `e892029b…272837` | ✅ |
| 07 → `bootstrap/07…` | `0b528722…ee3e5e` | ✅ (rename만) |
| 06 → `CLAUDE.md` | `e0a1d1f8…b958a4` | ✅ (rename만) |
| MANIFEST.json | `88ce7033…c681c4` | ✅ |

**10/10 원본 바이트 동일.** 06→`CLAUDE.md`, 07→`bootstrap/07…` 재배치는 INSTALL_MANIFEST의
`source_pack_name` 필드로 추적 가능하며, verify 스크립트가 역방향
`UNINSTALLED 원본 pack {name} 이 설치 매니페스트에 없다` 검사까지 수행해 pack 파일 누락도 잡는다.
**설계 품질 양호.**

coverage 공백 1건 → F13: `docs/INDEX.md`, `docs/v2/MANIFEST.json`, `scripts/verify_v2_docs.py`가
INSTALL_MANIFEST 대상 밖이다(MANIFEST.json은 본 감사가 수동 대조로 무결 확인).

### 6.2 기준선 SHA 정합 — **PASS (7/7 + main 주석 사실 확인)**

| §4 기재 | 실제 `git rev-parse origin/*` | 일치 |
|---|---|---|
| main `5a9015d1e95b15304aaf53a73efb475934610b82` | 동일 | ✅ |
| v2 executor `본 커밋` = `eb36d173…` | 동일 | ✅ |
| old WIP `87a0464e8159d5526069d5e654e648b0dae506ca` | 동일 | ✅ |
| Pilot `32460b87334a67f6a74823ac55f85ca80a9f8980` | 동일 | ✅ |
| adversarial `510d5f21a4de3d6420a3e41eeb44972e5973c5ac` | 동일 | ✅ |
| ssot `1bc2c71b2c48f060609fb458e2dd169086f59111` | 동일 | ✅ |
| orchestrator `bfa16624e55e15c4626e74547ed885156a8f2a9e` | 동일 | ✅ |

> §4 주석 `이 저장소의 origin/main(a835d5d8)은 저장소 부트스트랩 브랜치이며 이 연구의
> authoritative main이 아니다.`

**사실이다.** `origin/main` = `a835d5d8b06a15d5a75dee9ba34a3654555fc930`
(`chore: 워크트리 환경 연결 스크립트 추가`)로 SHA 접두 일치하며, 해당 브랜치에는
`research/landing_accessibility/` 연구 산물이 올라와 있지 않다. 주석은 정확하고 필요하다.

### 6.3 01 §2 표 이름 ↔ 실제 `state/*.parquet` — **해소 선언 불충분** (→ F12)

실측:

| 01 §2 논리표 | 실제 파일 |
|---|---|
| `dim_panel` | `panel_registry` (17행) |
| `fact_source_ranking` | `source_ranking_rows` (261행) |
| `dim_measurement_entity` | `service_master` (81행) |
| `bridge_source_membership` | `source_membership` (142행) |

**판정: 부분적으로만 해소된다.**

*충분한 부분* — 01 서두 `기존 state/*.parquet 파일은 삭제·rename·migration하지 않는다.
분석용 표는 매핑/머티리얼라이제이션 레이어로 만든다.`가 **메커니즘**을 확정하고,
03 Phase3-7 `mapping/materialization layer`와 P-A가 **작업 귀속처**를 확정한다.
따라서 권위 모순(P0/P1)이 아니다.

*불충분한 부분* — ① 01 §2의 절 제목이 `기존 정형 데이터`여서 `dim_panel` 등을 **이미 존재하는
표로 단언**하는데 물리적으로는 그런 파일이 없다. ② 논리명↔물리명 **대응표가 어느 문서에도 없다.**
③ 불일치가 이름뿐이 아니다 — `bridge_source_membership`은 `source_row_id`를 포함한 행 단위
bridge로 기술됐으나 실제 `source_membership`은 142행짜리 패널 단위 집계다(261 source row와
grain 자체가 다르다). 지시에 따라 컬럼 단위 조사는 여기서 중단한다.

즉 "매핑 레이어로 만든다"는 선언은 **누가 언제 하는지는 해소하지만, 대응이 자명하다는 인상을
남긴 채 grain 차이를 은폐**한다. P-A 최초 산출물로 대응표를 강제해야 한다.

---

## 7. 발견 목록

### F1 · `ned-ied-split-not-operationalized` — **P1 / blocking**

**파일** `docs/v2/02_COLLECTION_MEASUREMENT_SPEC_v2.0.md` §9 ↔ `01_DATA_SPEC_v2.0.md` §6 / `00_SSOT_v2.0.md` §7

**인용**
- 00 §7: `NED — Navigation Entry Depth … 랜딩에서 대표기능이 있는 영역에 도달하기까지의 최소 state-changing activation 수.` / `IED … 대표기능 영역에서 predefined endpoint까지 필요한 최소 activation 수.`
- 01 §6 `fact_task_entry`: `- NED` / `- IED` / `- MPFED`
- 02 §9 전문(`Depth 계산`)은 activation 포함/제외만 규정: `activation 예: button tap / link tap / menu open / item select` … `제외: 텍스트 한 글자씩 입력 / passive loading / redirect 자체 / server wait / scroll distance / popup dismiss`

**모순** grep 실측 결과 `NED`·`IED`·`대표기능 영역`이라는 표현이 **02에 단 한 번도 등장하지 않는다.**
EXECUTION_AUTHORITY §2가 02를 `수집·측정·정지조건`의 권위(3위)로 지정했는데, 그 문서가
NED와 IED를 가르는 경계 — 즉 "대표기능 영역에 도달했다"를 판정할 **관측 가능한 신호** — 를
정의하지 않는다. `dim_representative_task`(01 §3)에도 `endpoint_definition`·`endpoint_signal_type`만
있고 영역 도달 신호에 대응하는 `region_definition`/`region_signal_type`이 없다.
결과적으로 `MPFED`(총 activation 수)는 산출 가능하지만 **그 두 구성요소는 산출 불가능**하다.
NED/IED 분리는 00 §13이 Augello et al.(2026) `올바른 section까지의 click 수와 section 내부 task
click 수를 분리한 접근을 차용한다. 본 연구의 NED와 IED 설계 근거.`로 명시한 방법론적 핵심이므로
누락 시 Axis B의 선행연구 정박점이 무너진다. 문서가 `FROZEN FOR IMPLEMENTATION`인 상태에서
이 경계를 P-C 구현자가 정하게 두면 핵심 지표 정의가 SSOT 밖에서 결정된다.

**시정** 02 §9에 NED|IED 경계 규칙을 추가한다(예: `dim_representative_task`에
`region_definition`·`region_signal_type`을 신설하고, 02 §6이 ranking한 대표기능 후보 control이
현재 상태에서 조작 가능하게 노출된 최초 시점을 영역 도달로 정의). 01 §3에 대응 컬럼을 추가한다.

---

### F2 · `interrupt-dismiss-fields-have-no-collection-procedure` — **P2 / blocking (E001_V2-blocking)**

**파일** `docs/v2/01_DATA_SPEC_v2.0.md` §5 ↔ `02_COLLECTION_MEASUREMENT_SPEC_v2.0.md` §3·§5

**인용**
- 00 §8: `또한 다음을 기록한다. - 닫기 control 유무 / - 닫기 control 가시성 / - accessible name / - 조작 대상 크기 / - 실제 dismissal 성공 / - 대표기능 진입을 위해 반드시 닫아야 하는지`
- 01 §5: `dismiss_control_exists` `dismiss_control_visible` `dismiss_control_accessible_name` `dismiss_control_width` `dismiss_control_height` `dismiss_succeeded`
- 02 §5 4단계 전문: `1차 후보` / `2차 공간검사` / `3차 blocking 여부` / `4차 의미분류` — 닫기 control 언급 0
- 02 §3 L0 수집목록 마지막: `- modal candidate / - body scroll lock / - primary-action candidate`

**모순** 00(1위 권위)이 기록을 지시하고 01(2위)이 컬럼을 정의했는데, 수집절차의 유일한 권위인
02가 닫기 control 탐색·기하·accessible name 추출을 **어느 단계에서도 지시하지 않는다.**
grep 실측: `닫기`는 00에만 3회, 02에는 0회. 02의 `dismiss` 언급은 §9
`Popup dismiss는 forced_dismissal_count에 따로 기록.` 뿐이며 이는 L1 depth 규칙이지 L0 수집이 아니다.
특히 `dismiss_succeeded`는 **L0에서 실제 닫기 조작을 수행해야 채워지는 값**인데,
02 §3은 `L0에서는 대표기능을 아직 클릭하지 않는다`만 규정해 dismissal 조작의 허용 여부·수행 시점·
그 전후 evidence 순서(§12 append-only와의 관계)가 전부 미정이다. 구현자가 임의 결정하게 된다.

**시정** 02 §5에 `5차 dismiss control 검사`를 추가해 탐색 규칙·기하/accessible name 추출·
dismissal 시도 시점(초기 viewport evidence 확정 **이후**)·실패 시 상태값을 규정한다.

---

### F3 · `episode-counters-undefined-and-uncollected` — **P2 / blocking (E001_V2-blocking)**

**파일** `docs/v2/01_DATA_SPEC_v2.0.md` §6 ↔ `02` §7·§9 ↔ `04_GLOSSARY_v2.0.md`

**인용**
- 00 §7 `별도 기록`: `- text input / - scroll / - forced popup dismissal / - auth gate / - redirect` … `이들은 Depth와 합치지 않는다.`
- 01 §6: `- text_input_episode_count` / `- scroll_episode_count`
- 02 §7 activation별 기록목록 전문: `- URL / - DOM state key / - screenshot / - clicked control / - endpoint signal / - popup / - auth gate`
- 02 §9: `제외: 텍스트 한 글자씩 입력 … scroll distance` / `Popup dismiss는 forced_dismissal_count에 따로 기록.`

**모순** 두 겹이다. ① **정의 부재** — `episode`가 04 용어집에 없고 다른 어느 문서에도 정의가 없다.
"텍스트 입력 1 episode"가 focus~blur인지 submit 단위인지, "scroll 1 episode"가 몇 px·몇 초
간격으로 끊기는지 규정이 없어 **정수 카운트가 재현 불가능**하다. ② **수집 부재** — 02 §9는
세 형제 항목 중 popup dismiss만 `forced_dismissal_count`로 귀속처를 지정하고, text input·scroll은
"depth에서 제외"만 하고 **어디에 적재할지 말하지 않는다.** 02 §7의 기록목록에도 없다.
00 §7이 `별도 기록`을 지시했으므로 이는 상위 지시의 하위 미이행이다.

**시정** 04에 `episode` 항목을 추가해 경계조건을 수치로 고정하고, 02 §7 기록목록에
`text input episode` / `scroll episode`를 추가한다.

---

### F4 · `primary-action-identity-not-stored` — **P2 / blocking (E001_V2-blocking)**

**파일** `docs/v2/01_DATA_SPEC_v2.0.md` §4·§5 ↔ `02` §3·§6 ↔ `00` §8

**인용**
- 00 §8: `PrimaryActionOcclusion = 대표기능 control이 overlay에 가려진 면적 / 대표기능 control 면적`
- 02 §3 수집목록: `- primary-action candidate`
- 02 §6: `Interaction archetype의 endpoint 설명과 embedding similarity를 계산해 후보를 ranking한다.`
- 01: `primary_action_visible_initial` `max_primary_action_occlusion` `blocks_primary_action` `primary_action_occlusion` — 전부 **파생 스칼라**

**모순** 01의 어느 표에도 대표기능 control **자체의 정체(selector·bbox·accessible name·
후보 ranking 점수)를 담는 컬럼이 없다.** 02 §3이 수집하고 §6이 ranking한 산출물이
저장처 없이 소실된다. 그 결과 00 §8이 정의한 occlusion 비율의 **분모(대표기능 control 면적)가
저장 데이터로부터 재구성되지 않아** 재검증·재판정이 불가능하다. 이는 02 §12
`같은 evidence 재판정 → 새 judgment version`(재판정 가능성 전제)과 실질적으로 충돌한다.
또한 embedding ranking에는 오분류 여지가 있는데(§6 `모호하면 AI review`), 어느 후보가 선택됐는지
기록되지 않으면 AI review 결과의 감사 추적도 끊긴다.

**시정** 01 §4에 `primary_action_selector` / `primary_action_bbox` / `primary_action_name` /
`primary_action_selection_basis`(또는 `fact_primary_action_candidate` 표)를 신설한다.

---

### F5 · `l0-evidence-artifacts-without-storage-slot` — **P2 / blocking (E001_V2-blocking)**

**파일** `docs/v2/02` §2·§3·§11 ↔ `01_DATA_SPEC_v2.0.md` §4

**인용**
- 02 §3: `- 최초 viewport screenshot` / `- full-page screenshot` / `- DOM snapshot` / `- AX tree` / `- computed CSS`
- 02 §2: `- 수집시각 기록`
- 02 §11: `한 observation은 정확히 다음과 대응해야 한다. - DOM / - AX / - screenshot / - probe / - manifest`
- 01 §4: `screenshot_path` `dom_path` `ax_path` `probe_path` `manifest_path`, 그리고 시간 컬럼은 `audit_date` 하나

**모순** ① 02 §3이 **두 종류의 screenshot**을 수집하는데 01은 `screenshot_path` 단수이고
02 §11의 evidence identity 집합도 `screenshot` 단수다 → full-page screenshot은 **선언된 정체
슬롯이 없는 증거**가 되어 §11·§12의 append-only 무결성 대상 밖으로 새어 나간다.
② `computed CSS`는 별도 산출물로 열거됐으나 `probe_path` 안에 포함되는지 명시가 없다.
③ 02 §2는 `수집시각`(시각)을, 01 §4는 `audit_date`(일자)를 규정해 grain이 다르다 —
동일 web target을 같은 날 재수집하면(§12 `재수집 → 새 evidence run`) 두 run이
`(web_target_id, audit_date)`로 구분되지 않는다.

**시정** 01 §4에 `screenshot_fullpage_path`·`collected_at`(timestamp) 추가, 02 §11의 identity
집합에 full-page screenshot 명시, computed CSS의 귀속(probe 내부 여부)을 1행으로 확정.

---

### F6 · `measurement-status-vocabulary-unreconciled` — **P2 / blocking (E001_V2-blocking)**

**파일** `docs/v2/01_DATA_SPEC_v2.0.md` §3·§4·§11 ↔ `02` §13

**인용**
- 01 §11: `- 수집 실패 = MEASUREMENT_FAILED` (전 문서에서 이 토큰의 유일 등장)
- 02 §13: `다음은 접근성 FAIL이 아니다. - ACCESS_BLOCKED / - ROBOTS/transport issue / - browser crash / - page timeout / - app-only` … `별도 measurement status로 기록.`
- 01 §4: `- measurement_status` (허용값 열거 없음)
- 01 §3 `dim_web_target`: `- web_eligibility_status`

**모순** ① `measurement_status`의 **허용값 집합이 어디에도 열거돼 있지 않다.** 01 §11은
`MEASUREMENT_FAILED` 한 값만, 02 §13은 다섯 조건(그중 토큰 형태는 `ACCESS_BLOCKED` 하나뿐,
나머지 넷은 자연어)만 말하며 둘 사이의 포함관계·매핑이 선언되지 않았다. 같은 실패를 두 문서가
다른 이름으로 부를 수 있다. ② **`app-only`는 두 표가 동시에 주장한다** — 앱 전용이라 웹이 없다는
사실은 01 §3 `dim_web_target.web_eligibility_status`(관측 이전의 적격성)에 속하지,
01 §4 `fact_landing_observation.measurement_status`(관측 시도의 결과)에 속하지 않는다.
02 §13은 이를 measurement status로 기록하라고 지시해 표 배치가 상충한다. 상태 중복 기록은
01 §11 `상태와 수치는 분리한다`의 취지와도 어긋난다.

**시정** 01 §11에 `measurement_status` 허용값을 열거하고(02 §13 다섯 조건을 토큰화해 포함),
`app-only`는 `web_eligibility_status` 소관임을 02 §13에 명시한다.

---

### F7 · `undefined-column-vocabularies` — **P2 / blocking (E001_V2-blocking)**

**파일** `docs/v2/01_DATA_SPEC_v2.0.md` §7·§9·§10 ↔ `04_GLOSSARY_v2.0.md` ↔ `00` §9 / `03` Phase 5

**인용**
- 01 §7: `- automation_grade` — grep 실측 결과 **전 문서 통틀어 이 1회만 등장**, 정의·값집합 없음
- 01 §10 / 03 Phase 5: `- decision coverage` — 분자·분모 정의 없음, 04 용어집에도 없음
- 00 §9: `나머지는 UNDETERMINED / ABSTAIN.` · 04: `| Abstain | 확신할 수 없어서 억지로 분류하지 않는 것 |`
- 01 §9 `fact_ai_adjudication`: `final_status` `human_required` — `ABSTAIN`을 담는다는 선언 없음

**모순** ① `automation_grade`는 `fact_criterion_result`의 저장 컬럼인데 **정의도 값집합도 없다** —
측정 시점에 채워야 하는 값이 무엇인지 알 수 없다. ② `decision coverage`는 03 Phase 5의 측정품질
합격 판단에 쓰이고 `mart_service_summary`에 적재되는데 계산식이 없다. ③ 00 §9가 `UNDETERMINED`와
병렬로 규정한 `ABSTAIN`을 **담을 컬럼이 지정되지 않았다.** `verdict_state`/`final_status`(01 §7),
`classification_status`/`final_label`(01 §5), `final_status`(01 §9)의 값 도메인이 각각 무엇인지,
그리고 두 status 컬럼이 어떻게 다른지도 선언돼 있지 않다. 결과적으로 `ABSTAIN`이 어느
컬럼에도 안착하지 못하면 03 Phase 5의 `abstention rate` 산출이 불가능하다.

**시정** 01에 컬럼별 허용값 표를 1개 추가한다(`verdict_state` / `final_status` /
`classification_status` / `final_label` / `automation_grade` / `measurement_status`).
`decision coverage`는 분자·분모를 04 또는 01 §10에 명시한다.

---

### F8 · `phase-gate-names-only-in-nonauthoritative-bootstrap` — **P1 / blocking**

**파일** `docs/v2/03_CRISP_DM_EXECUTION_PLAN_v2.0.md` ↔ `docs/v2/bootstrap/07_CLAUDE_FIRST_SESSION_PROMPT_v2.0.md` ↔ `EXECUTION_AUTHORITY.md` §2

**인용**
- EXECUTION_AUTHORITY §2 비권위 표: `docs/v2/bootstrap/07_… — NON_AUTHORITATIVE_BOOTSTRAP_RECORD. 이 세션의 부트스트랩 이력 보존용이며 실행규칙 근거로 인용하지 않는다`
- bootstrap/07:174 `V2_SSOT_FROZEN` / :226 `ANALYSIS_AND_TASK_CODEBOOK_FROZEN` / :260 `TARGET_TASK_FRAME_FROZEN` / :327 `L0_L1_ENGINE_READY`
- 03의 gate 표기는 절 제목뿐: `## P0 — V2 Refreeze` / `## P-A — Analysis Foundation + Task Codebook` / `## P-B — Target / Task Frame` / `## P-C — L0/L1 Engine` / `## P-D — E000_V2` / `## P-E — READY_FOR_E001_V2`

**모순** grep 실측 결과 위 **4개 gate 이름은 저장소 전체에서 오직 bootstrap/07에만 존재한다.**
EXECUTION_AUTHORITY §2는 그 파일을 실행규칙 근거로 **인용 금지**로 못박았다. 따라서
P-A~P-D의 phase 종료 gate에는 인용 가능한 이름이 없고, 05 §6이 규정한 오케스트레이션
(`orchestrator directive → executor → adversarial ∥ ssot → reconciliation → promotion`)에서
"어떤 gate가 닫혔는가"를 05 §10 추적 state(`current_phase`/`next_phase`)에 기록할 근거가 사라진다.
특히 **지금 진행 중인 P0가 닫아야 할 gate `V2_SSOT_FROZEN`이 바로 이 4개 중 하나**다 —
본 감사가 PASS를 냈을 때 orchestrator가 선언할 gate 이름이 금지된 출처에만 있다.
(대조군: `READY_FOR_E001_V2`는 00 §15·03 P-E·CLAUDE.md·EXECUTION_AUTHORITY §5 4곳에,
`V2_REFREEZE`는 05 §7·EXECUTION_AUTHORITY §4에 정상 정의돼 있다. 즉 규칙은 존재하고
4개만 누락된 것이다.)

**시정** 03의 각 Phase Gate 절에 gate 이름을 `V2_REFREEZE`/`READY_FOR_E001_V2`와 동일한
형식으로 명시하거나, EXECUTION_AUTHORITY §1 기계적 상태값에 gate 이름 목록을 추가한다.
어느 쪽이든 bootstrap/07 인용 없이 gate를 지칭할 수 있어야 한다.

---

### F9 · `claude-md-and-evidence-contract-authority-framing` — **P2 / blocking (E001_V2-blocking)**

**파일** `research/landing_accessibility/CLAUDE.md` ↔ `docs/v2/EXECUTION_AUTHORITY.md` §2·§3

**인용**
- EXECUTION_AUTHORITY §2: `| — | research/landing_accessibility/CLAUDE.md | 위 6종의 프로젝트 컨텍스트 요약. 원본과 충돌 시 원본 우선 |`
- CLAUDE.md 본문 전체에 `EXECUTION_AUTHORITY` 0회, `INDEX.md` 0회, 자신의 종속 지위 언급 0회
- EXECUTION_AUTHORITY §3: `| docs/07_EVIDENCE_MANIFEST_CONTRACT.md | 본 브랜치 … | **현행 유효.** evidence identity 계약은 v2에 그대로 상속 |`
- 같은 파일 §2 권위 서열 표(1~6위 + CLAUDE.md)에 `07_EVIDENCE_MANIFEST_CONTRACT.md` **부재**

**모순** ① CLAUDE.md는 Claude Code가 하위 디렉터리 작업 시 **자동 로드하는 유일한 진입점**인데,
권위 선언의 정본(`EXECUTION_AUTHORITY.md`)과 문서 인덱스(`INDEX.md`)를 가리키지 않는다.
INDEX.md는 `권위 선언의 정본은 docs/v2/EXECUTION_AUTHORITY.md`라고 올바르게 안내하지만,
CLAUDE.md만 읽은 에이전트는 INDEX.md의 존재조차 알지 못한다. 그 결과 §3의 v1 supersede 표,
§4 기준선 표, §1의 `E001_V2_STARTED = false` 기계 상태값이 자동 로드 경로에서 도달 불가능하다.
② CLAUDE.md는 자신이 `원본과 충돌 시 원본 우선`이라는 종속 문서임을 **본문에 담고 있지 않아**,
요약이 원본과 어긋났을 때 독자가 적용할 해소 규칙을 손에 쥐지 못한다(v1↔v2 우선순위는 명시돼 있으나
CLAUDE.md↔원본 우선순위는 없다). ③ 대칭적으로, `현행 유효`로 선언된
`07_EVIDENCE_MANIFEST_CONTRACT.md`가 §2 권위 서열에 없어 02와 충돌 시 우선순위 규칙이 없다.
**본 감사가 본문을 통독해 대조한 결과 실제 모순은 0이므로 P1이 아니다**(§5.2 표 참조).
구체적으로 명확화가 필요한 유일한 지점은 grain이다 — 07은 manifest를 run 단위
(`evidence/<run_id>/manifest.jsonl`, 파일당 1행, `observation_id` 키)로 정의하는데
01 §4는 관측당 `manifest_path` 1개를 둔다. run manifest 경로를 전 관측이 공유한다는 해석으로
정합하지만 그 해석이 명시돼 있지 않다.

**시정** CLAUDE.md 서두에 `권위 선언 정본: docs/v2/EXECUTION_AUTHORITY.md · 문서 인덱스:
docs/INDEX.md · 이 파일은 요약이며 원본과 충돌 시 원본 우선` 3행을 추가한다.
EXECUTION_AUTHORITY §2 표에 `07_EVIDENCE_MANIFEST_CONTRACT.md`를 `evidence identity 한정 권위`로
1행 추가하고, `manifest_path` 의미를 01 §4에 1행 확정한다.

---

### F10 · `readme-stale-pack-paths` — **P2 / non-blocking (POST_E001_DEBT)**

**파일** `docs/v2/README.md`

**인용**
```
7. `06_PROJECT_CLAUDE_MD_v2.0.md` — `research/landing_accessibility/CLAUDE.md`에 넣을 프로젝트 전용 지침.
8. `07_CLAUDE_FIRST_SESSION_PROMPT_v2.0.md` — 다음 Claude Code 첫 세션에 그대로 넣을 프롬프트.
```

**모순** 설치 트리의 `docs/v2/`에는 `06_…md`가 **존재하지 않고**(내용은 `../CLAUDE.md`로 설치됨),
`07_…md`는 bare name이 아니라 `docs/v2/bootstrap/07_…md`에 있다. README를 읽는 사람이
같은 디렉터리에서 두 파일을 찾으면 실패한다. 원인은 README가 원본 pack에서 **바이트 그대로**
설치됐기 때문이다(sha256 `e892029b…272837`, 원본과 동일 — §6.1). 부수적으로 항목 8이
07을 `그대로 넣을 프롬프트`라는 **실행 지시로 소개**해, EXECUTION_AUTHORITY §2의
`실행규칙 근거로 인용하지 않는다`와 어조가 어긋난다.

**비차단 근거** README는 §2에서 `읽기 순서 안내. 실행권위 아님`으로 명시적 비권위이며,
정확한 위치는 `INSTALL_MANIFEST.json`(`source_pack_name` 필드)과 `INDEX.md`·§2 표가 모두 갖고 있다.
원본 바이트 보존이라는 무결성 이득이 이 혼동보다 크다.

**시정** README를 수정하면 원본 대조가 깨지므로, `INSTALL_MANIFEST.json`에
`readme_path_note` 또는 `docs/INDEX.md`에 `README 항목 7·8의 설치 후 실제 위치` 각주 1행을 둔다.

---

### F11 · `bootstrap-07-lacks-in-file-nonauthority-banner` — **P2 / blocking (E001_V2-blocking)**

**파일** `docs/v2/bootstrap/07_CLAUDE_FIRST_SESSION_PROMPT_v2.0.md` 1~9행

**인용**
```
# CLAUDE CODE — FIRST SESSION BOOTSTRAP PROMPT
# ProjectFinal Landing Accessibility v2

너는 이 프로젝트의 **Main Orchestrator / Research Integrity Controller / Data Analysis Lead**다.
…
**v2 SSOT를 저장소 실행권위로 설치하고, 독립감사 후 동결한 다음, 기존 오케스트레이션 방식으로 즉시 P-A부터 진행**한다.
사용자에게 micro-task 승인을 요청하지 않는다.
```

**모순** 격리 장치는 **전부 파일 외부에만** 있다 — `bootstrap/` 디렉터리 배치,
EXECUTION_AUTHORITY §2 비권위 표, INSTALL_MANIFEST의 `"authority":
"NON_AUTHORITATIVE_BOOTSTRAP_RECORD"`. 파일을 직접 연 에이전트·사람은 **비권위임을 알 수단이 없고**,
첫 문장부터 역할 부여와 승인 생략 지시를 명령형으로 받는다. F8과 결합되면 위험이 실재화한다 —
gate 이름 4개가 이 파일에만 있으므로 후속 세션은 **반드시 이 파일을 열게 되고**, 그때
같은 파일의 phase 계획·salvage 지침·정지규칙까지 권위로 흡수할 개연성이 높다.
`README.md` 항목 8이 이 파일을 `그대로 넣을 프롬프트`로 안내하는 것도 같은 방향으로 작용한다.

**시정** 파일 최상단에 4행 배너를 추가한다 —
`> NON_AUTHORITATIVE_BOOTSTRAP_RECORD. 실행규칙 근거로 인용하지 않는다.
> 현행 실행권위는 docs/v2/00_SSOT_v2.0.md 및 docs/v2/EXECUTION_AUTHORITY.md.
> 이 파일은 2026-08-26 P0 V2_REFREEZE 세션의 부트스트랩 이력이다.`
배너 추가 시 sha256이 원본과 달라지므로 `INSTALL_MANIFEST.json`에
`"transform": "nonauthority_banner_prepended"`를 기록하고 `verify_v2_docs.py`의 DRIFT 검사를
해당 파일에 한해 예외 처리(또는 배너 제거 후 해시 대조)한다.

---

### F12 · `state-table-mapping-declared-without-correspondence` — **P2 / blocking (E001_V2-blocking)**

**파일** `docs/v2/01_DATA_SPEC_v2.0.md` §2 ↔ 실제 `research/landing_accessibility/state/*`

**인용**
- 01 서두: `기존 state/*.parquet 파일은 삭제·rename·migration하지 않는다. 분석용 표는 **매핑/머티리얼라이제이션 레이어**로 만든다.`
- 01 §2 절 제목: `## 2. 기존 정형 데이터` — 이어서 `### dim_panel` `### fact_source_ranking` `### dim_measurement_entity` `### bridge_source_membership`
- 실제 파일: `panel_registry`(17행) `source_ranking_rows`(261행) `service_master`(81행) `source_membership`(142행) `entity_alias_map` `web_target_group`(68행)

**모순** 매핑 레이어 선언은 **메커니즘과 작업 귀속처(03 Phase 3-7, P-A)를 확정하므로 권위 모순이 아니다.**
그러나 해소는 불완전하다. ① §2의 제목 `기존 정형 데이터`가 이들을 **현존 표로 단언**하는데
그 이름의 파일은 없다 — 독자는 `state/dim_panel.parquet`을 찾다 실패한다.
② 논리명↔물리명 **대응표가 전 문서에 없다.** ③ 불일치가 이름에 그치지 않는다 —
`bridge_source_membership`은 `- source_row_id`를 포함한 source row 단위 bridge로 기술됐으나
실제 `source_membership`은 142행의 (service_id, panel_id) 집계로 **grain이 다르다**(source row는 261).
`dim_measurement_entity.measurement_entity_id` ↔ `service_master.service_id`도 키 이름이 다르다.
지시에 따라 컬럼 단위 조사는 여기서 중단한다. 또한 01 §3이 `dim_web_target`을 `신규`로 분류하지만
기준선에 이미 `web_target_group`(68행, C012 산물)이 존재하며 둘의 관계가 선언돼 있지 않다.

**시정** 01 §2 서두에 `아래 이름은 매핑 레이어가 제공할 논리 표이며 물리 파일명이 아니다`
1행을 추가하고, 논리명↔물리명 대응표(+grain 차이 표기)를 **P-A A1의 최초 산출물**로 강제한다.
`dim_web_target` ↔ `web_target_group` 관계도 같은 표에 넣는다.

---

### F13 · `install-integrity-coverage-gap` — **P2 / non-blocking (POST_E001_DEBT)**

**파일** `docs/v2/INSTALL_MANIFEST.json` · `scripts/verify_v2_docs.py`

**인용**
- EXECUTION_AUTHORITY §6: `설치본의 sha256이 docs/v2/INSTALL_MANIFEST.json과 일치하는지, 그리고 원본 docs pack MANIFEST.json과 동일 바이트인지 검증한다.`
- `INSTALL_MANIFEST.json`의 `files` 키 10개: `docs/v2/{00..05}` `docs/v2/README.md` `docs/v2/bootstrap/07…` `CLAUDE.md` `docs/v2/EXECUTION_AUTHORITY.md`

**모순** 커밋이 설치한 14파일 중 **3개가 무결성 대상 밖**이다 — `docs/INDEX.md`,
`docs/v2/MANIFEST.json`, `scripts/verify_v2_docs.py`. `INDEX.md`는
`현행 실행권위는 docs/v2/ 다`를 선언하는 권위 내비게이션 문서인데 해시 기록이 없어
변조가 탐지되지 않는다. `MANIFEST.json`은 verify 로직의 **기준선 자체**인데
스크립트가 `if name == "MANIFEST.json": continue`로 건너뛰어 자기 참조 검증이 없다
(본 감사가 수동 `sha256sum`으로 원본 `88ce7033…c681c4`와 동일함을 확인했다).
검증기 자신(`verify_v2_docs.py`)도 대상이 아니다.

**비차단 근거** 세 파일 모두 git으로 추적되어 커밋 히스토리로 변경 감사가 가능하고,
실행권위 판단은 `EXECUTION_AUTHORITY.md`(대상 포함)에 있으므로 INDEX 변조만으로 권위가 바뀌지 않는다.

**시정** `INSTALL_MANIFEST.json`에 세 파일을 `authority: NAVIGATION` / `INTEGRITY_BASELINE` /
`VERIFIER`로 추가한다(MANIFEST.json은 자기 참조 회피를 위해 INSTALL_MANIFEST 쪽에만 기록).

---

## 8. 검사 결과 요약

| 검사 | 결과 | 관련 finding |
|---|---|---|
| `intra_v2_consistency` | FAIL | F1 F6 F7 |
| `dataspec_collectionspec_linkage` | FAIL | F1 F2 F3 F4 F5 F6 F7 |
| `crispdm_alignment` | PASS | — |
| `gate_names_defined` | FAIL | F8 |
| `project_claude_current_only` | PASS_WITH_FINDINGS | F9 |
| `v1_v2_authority_unambiguous` | PASS_WITH_FINDINGS | F9 F10 F11 |
| `evidence_contract_compat` | PASS | (모순 0 — §5.2) |
| `install_integrity` | PASS | F13(coverage만) |
| `baseline_sha_accuracy` | PASS | — |
| `state_table_mapping_declared` | PARTIAL | F12 |

## 9. 승격 판단

**HOLD.** P0는 0이므로 권위 구조 자체는 건전하며, 설치·기준선·v1 supersede 선언은
재작업 없이 그대로 유지 가능하다. 그러나 F1·F8 두 P1을 시정한 새 executor 커밋을 받아
동일 exact SHA로 재감사한 뒤 승격할 것을 권고한다. F2~F7·F9·F11·F12(blocking P2 9건)는
같은 커밋에서 함께 닫는 것이 효율적이다 — 대부분 01/02에 표·행을 추가하는 문서 작업이며
`READY_FOR_E001_V2`의 `open blocking P0/P1/P2 = 0` 조건에 어차피 걸린다.
F10·F13은 non-blocking으로 분류한다.

---

## 부기 — 산출물 경로

본 보고서는 지시받은 경로 `research/landing_accessibility/audit/ssot/V2_C001.md`에 둔다.
기존 관행은 `research/landing_accessibility/audits/<CYCLE>_ssot.json`(C001·C002_C003·C009·C011·C012)이므로
두 경로가 공존하게 된다. orchestrator가 이후 사이클에서 한쪽으로 통일할지 결정할 사항이며,
본 감사는 지시 경로를 따랐다.
