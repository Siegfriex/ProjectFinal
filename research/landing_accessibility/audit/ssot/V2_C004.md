# V2-C004 / SSOT · Authority Audit

| 항목 | 값 |
|---|---|
| cycle | `V2-C004` |
| auditor | `ssot` (SSOT / Authority Auditor) |
| target | `agent/landing-v2-exec` @ `d5f1da5652953542d5c8be377026cc3293f2075a` |
| prev target | `4a0585e05a2119f11d86e530162d9674e0268a53` (V2-C003) |
| base | `research/landing-accessibility-main` @ `5a9015d1e95b15304aaf53a73efb475934610b82` |
| control | `control/landing-orchestrator` @ `580e67219d79ca318c9a7c50e03dbe4f762b82e7` |
| 감사일 | 2026-08-26 |
| **verdict** | **FAIL** |
| 집계 (신규) | P0 **0** / P1 **0** / P2 **1** (blocking 1 — `V2_SSOT_FROZEN-blocking`) |
| 승계 blocking | **0** |
| ssot 귀속 open blocking 합계 | **1** |

> **판정 요약.** 내 V2-C003 finding 5건이 **전건 CLOSED**다. 두 사이클 연속 OPEN이던
> `a1-endpoint-status-placement-contradicts-a2`도 닫혔다 — A1 §2.2가 `endpoint_status_detail`로
> 고쳐졌고, 옛 문장이 제거됐으며, §2.4 `mart_service_summary` 행까지 따라왔다.
> 커뮤니티 gate 무주지는 규칙 E-6a로 **실제로 없어졌다**(§2에서 경로를 끝까지 추적했다).
> EXC-2·E-9는 형식 등재가 아니라 실질을 닫았다 — E-8의 2항 합집합이 이전 3항 합집합과
> **집합으로 같음을 내가 직접 증명**했다(§2.4).
>
> **설치 무결성은 세 사이클 연속 완전 통과**이며, 이번에는 **공격 재현까지 실측**했다.
> 배너 정책이 코드로 강제되는지를 스크립트의 주장이 아니라 그 함수에 직접 위조 입력을 넣어
> 확인했고(§6.3), 배너 3종의 `banner_sha256`을 재계산하고 **배너·본문 어느 쪽에도 앵커되지
> 않는 잔여 바이트가 무엇인지**까지 특정했다 — `b"\n\n"` 두 바이트뿐이며 `strip_banner()`의
> `lstrip(b"\n")` 때문에 그 자리에는 개행 외 어떤 바이트도 존재할 수 없다. 은닉 표면이 없다.
>
> 그럼에도 FAIL인 이유는 **단 하나**다. `a1-endpoint-status-placement-contradicts-a2`를 닫으려고
> 신설한 **A1 §0.3 관할 선언표 자체가 세 겹으로 어긋나 있다.** executor 스스로
> "이 표는 A2 §0의 대칭 조항과 같은 목록을 담아야 한다. 한쪽만 갱신되면 그 자체가 drift다"라고
> 썼는데, **그 문장을 쓴 커밋에서 이미 drift가 성립한다.** 상세는 §1.1·§7.

변경량 `git diff 4a0585e0 d5f1da56 --stat` = **5파일 / +561 / −57**.
`00~05` + `MANIFEST.json` 7종은 BASE부터 지금까지 **수정 이력 0**(diff 0).
`PHASE_GATES.md` diff 0, Pilot(`research/refcohort/**`) diff 0, `state/*.parquet` diff 0,
root `CLAUDE.md` diff 0 — 전건 실측.

---

## 0. 감사 범위와 방법

```
d5f1da56 변경분:
  docs/v2/A2_VOCABULARY_AND_SCHEMA_BINDING.md   +487/−?  (126,907B → 175,606B)
  docs/v2/A1_MEASUREMENT_OPERATIONALIZATION.md  +38/−?   (34,751B → 36,510B)
  docs/v2/EXECUTION_AUTHORITY.md                +30/−?   (10,206B → 11,785B)
  docs/v2/INSTALL_MANIFEST.json                 +26/−7
  scripts/verify_v2_docs.py                     +37

대조 원본:
  /home/sieg/projects-wsl/ProjectFinal/SSOT/00~07 · README.md (사용자 제공 원본 pack, 9파일)
  control/landing-orchestrator @ 580e6721 : control/state.json · scripts/promote_landing_main.sh
  audit/landing-adversarial @ ea1d2448 : V2_C003.json
  audit/landing-ssot @ 43fab0ac : V2_C003.{md,json} (내 직전 보고서)
```

실행한 것:

- `python3 …/verify_v2_docs.py` → **exit 0 / `V2_DOCS_VERIFY: PASS`** (16행 OK)
- `python3 -m pytest tests/test_v2_docs_integrity.py -q` → **2 passed**
- `INSTALL_MANIFEST.json` 14항목 전건 `bytes`/`sha256` **독립 재계산**
- 배너 3종의 `body_sha256`·`banner_sha256` **독립 재계산** + **미앵커 잔여 바이트 특정**
- `MANIFEST.json` 9항목을 `SSOT/` 원본과 재계산 대조
- **배너 위조 공격 4변종 재현** — `verify_v2_docs.py`의 함수를 직접 임포트해 in-memory 주입
- A1 §0.3 표 ↔ A2 §0 4항 ↔ A1 §0.3 후속 문단의 **3자 대조**
- 커뮤니티 본인인증 gate의 기록 경로 **끝까지 추적**(§1.5.1 → E-6a → E-9 → E-8 → §6.3)
- E-8 2항 합집합 ≡ 이전 3항 합집합 **집합 동치 증명**
- A2 규칙 id 전건 추출 → **중복 정의 0건** 확인
- `open_blocking_total = 19` **재계산 대조**, 기준선 SHA 7건 `git ls-remote` 실재·귀속 확인

---

## 1. V2-C003 내 finding 5건 + 승계 건의 판정

### 1.1 `a1-endpoint-status-placement-contradicts-a2` — **CLOSED** (두 사이클 만에)

**세 겹 전부 닫혔다.** 실측:

| 요구 | V2-C003 | V2-C004 |
|---|---|---|
| A1 §2.2가 `endpoint_status_detail`을 쓰는가 | 전문 출현 **0회** | **4회**. §2.2(258·264행)가 인용블록 + 본문으로 배치를 확정 |
| 옛 문장이 제거됐는가 | `endpoint_status`에 이 값을 기록하고 | **제거**. `endpoint_status = UNRESOLVED`, `endpoint_status_detail = UNRESOLVED_DEPTH_BUDGET_EXCEEDED`로 대체 |
| §0.2 관할 열거에 §2.2가 있는가 | 없음 | **§0.3 표에 굵게 등재** |
| §2.4 세분값 손실 경로 | `endpoint_status` 원값만 전달 | `endpoint_status` 원값**과 `endpoint_status_detail`을 함께** 전달 |

`02 §7`의 7값 불확장이라는 자기 문장이 이제 **스키마에서도 참**이다. `A2` §1.5.2의 진단 문장과
모순되지 않는다. 닫는 finding id를 인용블록에 자진 기록한 것도 추적에 좋다. **CLOSED.**

### 1.2 `a2-1-5-1a-widens-00-3-community-gate-clause-to-any-auth-gate` — **CLOSED**

`00 §3` 원문을 `SSOT/00_SSOT_v2.0.md` 64·65행에서 직접 읽어 대조했다.

```
64: | 금융 | 금융기능 진입 또는 로그인/인증 gate가 나타난 순간 |
65: | 커뮤니티 | 게시물/스레드/작성영역 진입 또는 로그인 gate |
```

`A2` §1.5.1a 규범표가 이제 **archetype × gate 종류** 2축으로 갈리고, 세 행이 정확히 원문을 따른다.
규칙 E-6a가 표로 다시 못박고, E-6이 "`COMMUNICATION_ENTRY`의 본인인증 gate를 endpoint로 승격시키는
것도 똑같이 `00 §3` 범위 확대이며 금지"라고 명시적으로 닫는다. §1.5.1의 `AUTH_GATE_REACHED` 행도
같은 단서를 달아 **양방향으로** 정합했다. `A1` §1.5의 교차참조도 같은 문장으로 고쳐졌다 —
**A1과 A2가 이제 같은 말을 한다**(§2.1).

**커뮤니티 본인인증 gate 경로 추적 — 무주지가 실제로 없다**(§2.2). **CLOSED.**

### 1.3 `a2-1-5-1a-deviates-from-01-11-without-exc-registration` — **CLOSED**

`A2` §0 7항 등재부에 **EXC-2**가 신설됐고, 내가 처방한 6열을 전부 채웠다 —
원본 조항 `01 §11`, 원본 문면 `로그인 전까지만 가능 = AUTH_GATE_REACHED`, 배치, 근거(`00 > 01`),
**보존되는 곳**(`endpoint_status_detail` + E-9 + E-8 합집합 + E-10 층화), 절.

**375행 인용도 정합화됐다.** 이전 판은 비-예외 5 archetype 행의 근거로 `01 §11`을 인용해
성실한 독자가 "`01 §11`이 온전히 지켜졌다"고 읽게 만들었다. 지금은 그 두 행이
`01 §11 문면 그대로 (EXC-2의 대상이 **아니다**)`로 적혀 **예외의 존재와 그 범위를 동시에** 알린다.
커뮤니티 본인인증 gate 행도 같은 단서를 단다. **CLOSED.**

### 1.4 `rule-e-8-omits-01-6-auth-gate-before-endpoint-column` — **CLOSED**

**규칙 E-9**가 신설돼 `01 §6`의 컬럼을 `A2`에 바인딩했다(전문 출현 0회 → **10회**).

- **`01 §6` 정의와 일치하는가 — 일치한다.** 정본 원천을 `fact_task_step.auth_gate_detected`로 두어
  `01 §6`이 실제로 가진 step 컬럼에서 유도하고, `02 §7`이 각 activation 후 `auth gate` 기록을
  요구한다는 근거를 댄다. 허용값 `0`/`1`, `NULL` 금지(규칙 N-3 — 행이 있으면 step 로그가 있으므로
  미관측은 **관측된 0**)까지 `01 §11`(결측을 0으로 바꾸지 않는다)과 충돌 없이 정합한다.
  "endpoint를 실현한 gate step은 정확히 하나"라는 특정이 6상황표로 전건 열거됐다.
- **E-8과의 관계가 모순 없는가 — 없다. 오히려 개선됐다.** 합집합이 3항 → **2항**으로 줄었다.

```
auth_gate_observed = (auth_gate_before_endpoint = 1)
                  OR (endpoint_status_detail = 'ENDPOINT_VIA_AUTH_GATE')
```

**집합 동치를 내가 직접 확인했다**(§2.4). 문서의 주장("`01 §6` 전용 컬럼을 그대로 쓰는 구현자와
합집합을 쓰는 구현자가 같은 값에 도달한다")이 참이며, 규칙 S-1(한 사실 = 한 컬럼) 위반 위험도
이 축소로 해소됐다. §6.3에 컬럼이 등재됐고 실패주입 I-7·I-8이 붙었다. **CLOSED.**

### 1.5 `execution-authority-4-v2-ledger-scope-stale-at-v2-c001` — **CLOSED**

> | v2 | `control/state.json` → `v2_transition.v2_audit_findings` (V2-C001 이후 **모든 사이클**의 두 감사 finding. 사이클이 진행될 때마다 등재·갱신된다) |

`state.json`의 `v2_audit_findings.totals`가 실제로 `V2_C001` / `V2_C002` / `V2_C003` /
`orchestrator_registered` 4키를 담으므로 새 문면이 참이다. **CLOSED.**

### 1.6 `control-state-and-promote-header-mislabel-verify-check-number` — **CLOSED**

번호를 고치는 대신 **번호를 폐기하고 이름으로 참조**하는 쪽을 택했다 — 재발 원인(삽입 시 번호 밀림)을
없애는 처리이며 내 처방보다 낫다. **삼자가 같은 이름을 쓰는지 실측했다.**

| 소스 | 확인 |
|---|---|
| `EXECUTION_AUTHORITY` §8 | `[INSTALL_INTEGRITY]` 지목 + 9검사 이름 전건 열거. **`검사 N` 표현 0건** |
| `promote_landing_main.sh` (control @ 580e6721) | 헤더 35–44행이 **같은 9개 이름을 같은 순서로** 열거. 본문 섹션 주석도 `[HOOK_INSTALL]`…`[AUDIT_VERDICT]`. **`검사 N` 표현 0건** |
| `state.json` (control @ 580e6721) | 라이브 서술은 `[INSTALL_INTEGRITY]`·`[AUDIT_VERDICT]`·`[ORCH_TREE]`·`[HOOK_INSTALL]`·`[BLOCKING_DEBT]` 이름. 잔존 `검사 N` 12건은 **전부 이력 기록**이며 그 자리에서 "등재 당시 '검사 6'이라 적었으나 오기였다"로 자진 주석되거나, 옛 `cf5d107b`의 행번호를 가리키는 finding evidence다 |

9검사 이름·순서가 문서와 스크립트에서 **완전 일치**한다. **CLOSED.**

### 1.7 `install-integrity-coverage-gap` — **OPEN (partial) 유지 · 비차단**

이번의 `banner_sha256` 앵커는 **다른 결함**(adversarial 배너 정책)의 시정이며 이 finding의
3대상과 무관하다. 3대상 현황:

| 대상 | 상태 |
|---|---|
| `docs/v2/MANIFEST.json` | **닫힘**(V2-C003) — `[manifest, git-anchored]` 실행 확인 |
| `scripts/verify_v2_docs.py` | **잔존** — `INSTALL_MANIFEST.json` `files` 14키에 없다(git 추적은 확인) |
| `tests/test_v2_docs_integrity.py` | **잔존** — 같음. 저장소 루트 `tests/` 에 있으며 커버리지 스윕(`docs/v2/**`) 밖이다 |

등급·class **P2 / `POST_E001_DEBT` 유지, 차단으로 올리지 않는다** — 승격 `[EXEC_TREE]`가
exec 워크트리 clean + HEAD 고정을 요구하므로 승격 경로 안에서는 두 파일도 git 앵커를 얻는다.
두 파일이 실제로 동작함은 실행으로 확인했다(스크립트 exit 0 / pytest 2 passed).

### 1.8 `install-manifest-is-self-anchored` (adversarial) — **본 감사 소관 밖 · 사실만 기록**

`INSTALL_MANIFEST.json`은 git 앵커를 받고 있으며 실행 확인됐다. 다만 배너 정책 강제의 잔여
가정은 여전히 "매니페스트를 고치면 워킹트리가 dirty가 되거나 커밋에 남는다"이므로,
**커밋된 위조**는 여전히 승격 절차(`[ORCH_TREE]`·`[EXEC_TREE]`·감사)로만 막힌다. 판정은 adversarial 소관.

---

## 2. auth gate 시정의 SSOT 정합 (직접 추적)

### 2.1 A1 ↔ A2 문면 일치

`A1` §1.5 교차참조 인용블록이 `[V2-C004 시정]`으로 갱신돼

> `00` §3 원문은 금융에 "또는 로그인/**인증** gate", 커뮤니티에 "또는 **로그인** gate"를 준다.
> 커뮤니티에서 **본인인증** gate는 endpoint가 아니라 `02` §7 종료값
> (`AUTH_GATE_REACHED` / `PERSONAL_DATA_REQUIRED`)으로 기록된다.

라고 쓴다. `A2` §1.5.1 `AUTH_GATE_REACHED` 행·§1.5.1a 규범표 3행·규칙 E-6a 표의 문면과
**토큰 단위로 같다.** 정본 지목도 `A2 §1.5.1a (규칙 E-5~E-10)`로 범위가 갱신됐다.
`A1`이 신호를, `A2`가 저장값을 맡는 분업이 유지된다.

### 2.2 커뮤니티 본인인증 gate — S-3 실패 없이 기록되는가 (경로 추적)

| 단계 | 결과 |
|---|---|
| §1.5.1 `AUTH_GATE_REACHED` 행 | 두 archetype 예외를 **gate 종류로 한정**. `COMMUNICATION_ENTRY`의 본인인증 gate는 "이 값을 그대로 쓴다"고 **명시적으로 허가** |
| §1.5.1a 규범표 3행 | `endpoint_status = AUTH_GATE_REACHED`(개인정보 입력 요구 시 `PERSONAL_DATA_REQUIRED`) · `endpoint_status_detail = NULL` · `endpoint_reached = 0` · `NED/IED/MPFED = NULL` |
| 규칙 E-6a 표 | 같은 값. 결제·CAPTCHA·차단의 귀속까지 함께 열거 |
| 규칙 E-6 | 이 gate를 endpoint로 올리는 것을 **금지**로 등재 |
| 규칙 E-9 | `auth_gate_before_endpoint = 1` (6상황표 5행) — 유병률에서 사라지지 않는다 |
| 규칙 E-8 | `auth_gate_observed = 1` (1항으로 성립) |
| 규칙 S-3 | **발화하지 않는다.** §1.5.1a가 "`COMMUNICATION_ENTRY`에서 관측 가능한 모든 종료 사건이 `02 §7`의 7값 중 정확히 하나로 간다"고 닫는다 |
| 실패주입 I-3 | 반대 방향(본인인증 gate → `FUNCTION_ENDPOINT_REACHED`)이 **차단** 대상으로 등재 |

**무주지 0.** 내가 V2-C003에서 지적한 "둘 다 막혀 매핑이 없는 상태"가 실제로 해소됐다.

### 2.3 EXC-2 · E-10과 `01 §11` / `00 §7`

- EXC-2가 이탈 범위를 `두 archetype × 00 §3이 그 행에 준 gate 종류`로 한정하고, 그 밖은
  `01 §11` 문면 그대로임을 명시한다 — **원본을 넓히지도 좁히지도 않았다.**
- `00 §7 별도 기록`(auth gate를 Depth와 합치지 않는다) 위반 없음 — E-10 4항이 "gate는 depth에
  더해지는 항이 아니라 depth의 **정지점**이며 `m`을 늘리지 않는다"로 논증한다. `SSOT/00` §7 원문 대조 결과 옳다.

### 2.4 E-8 2항 합집합 ≡ 이전 3항 합집합 — 직접 증명

| 이전 항 | 새 정의에서의 귀결 |
|---|---|
| `endpoint_status = 'AUTH_GATE_REACHED'` | 그 종료 gate step은 `auth_gate_detected = 1`이고 `detail ≠ ENDPOINT_VIA_AUTH_GATE`이므로 "endpoint를 실현한 gate step"이 아니다 → **`auth_gate_before_endpoint = 1`에 포함** |
| step 로그에 gate가 있는 task | 그 gate가 endpoint 실현 step이면 `detail = ENDPOINT_VIA_AUTH_GATE` → **2항이 덮는다**. 아니면 → **1항이 덮는다** |
| 역방향 | 1항·2항 어느 쪽이 1이어도 이전 3항 중 하나가 1이다 |

**두 집합은 같다.** 축소는 규칙 S-1을 지키면서 정보를 잃지 않는다.

---

## 3. A2 176KB의 내부 정합 (신규 검사)

### 3.1 규칙 id 충돌·중복 정의 — **0건**

`**규칙 X-N**` 정의 지점을 전건 추출해 대조했다. 61개 id(`A-1`…`W-3`, `RC-1~RC-5`, `E-6a`, `E-9`,
`E-10`, `EP-*`, `I-1~I-4`)가 **각각 정확히 한 번** 정의된다. 금지 전이 `X-1`~`X-15` 15개도
등재부 표에 1행씩만 존재한다. 신규 규칙이 기존 규칙을 재정의한 자리는 없다.

### 3.2 RC 규칙군 ↔ `02 §12` · `01 §11` · `00 §9`

| 원본 | 정합 |
|---|---|
| `02 §12` (`재수집 → 새 evidence run` / append-only) | **위반 없음.** RC-1은 "상한은 기록의 금지가 아니다 — 상한을 넘어 수집된 run도 `02 §12` append-only로 남는다. 다만 정본 지표에 들어가지 않는다". RC-4도 "삭제하지도 않는다". 원본이 **허용한 것을 금지하지 않고**, 원본이 침묵한 자리(몇 번·무엇을·어떤 순서)만 채운다. §0 7항 **EXC-3**로 등재됐다 — 절차 부여형 이탈을 등재부에 올린 것은 등재부 규범의 올바른 적용이다 |
| `01 §11` (결측) | 충돌 없음. RC-3 말미가 "어떤 run도 `MEASURED`가 아니면 criterion 행 자체가 없다(규칙 M-1)"로 결측을 0으로 바꾸지 않는다 |
| `00 §9` (human ≤5) | 충돌 없음. RC는 사람 검토 예산을 건드리지 않고, §4.6 규칙 H-1의 정렬키를 통해서만 만난다 |
| `00 §14` (결론 유도 금지) | RC 전체의 **목적**이 그 조항의 이행이다. 중단조건 2가 "판정이 원하는 대로 나왔는가"가 아니라 "사전선언한 evidence가 산출됐는가"인 점, RC-3 교체가 **run 단위 일괄**이어서 criterion별 cherry-picking이 막히는 점이 핵심이며 둘 다 옳다 |

`MAX_RECOLLECTION_RUNS_PER_WEB_TARGET = 1`은 **해석 임계값이 아니라 수집 파라미터**로 선언됐고
(`A1 §7`과 같은 지위, P-C 구현 → P-D 검증 → 동결, §6.4 등재), `A1` §0.4의 구분과 정합한다.
`A2` §0 5항이 `recollection_preregistration` 블록을 위해 **명시적 단서 조항**을 신설한 것도
"이 문서는 새 컬럼을 요구하지 않는다"는 자기 규범과 충돌하지 않게 하는 올바른 처리다.

### 3.3 `impact_level` 재정의 — 결론 중립이 실제로 성립하는가

- **`01 §9` 정합.** `01 §9`는 `impact_level` 컬럼 이름만 두고 값·의미를 열거하지 않는다
  (`SSOT/01_DATA_SPEC_v2.0.md` 268행). `A2`가 의미를 확정하는 것이 `A2` §0 2항의 정확한 소관이다.
- **`00 §9` cascade 정합.** `00 §9` 6단계 `그래도 중요한 모호성만 HUMAN_FINAL`의 `중요`를
  §4.6 규칙 H-1이 `impact_level` 내림차순 → `evidence_gap {0=먼저}` 오름차순 → id 사전순으로
  결정적으로 조작화한다. 새 정의(결손의 성격·복구 가능성) 아래에서 이 정렬은
  "증거는 있는데 의미가 갈리는 항목이 사람 검토의 최적 대상"이라는 H-1의 논거와 일치한다.
  H-1이 명시한 **`접근성 outcome과 certified_current를 보기 전에 확정한다`**가 새 정의의
  입력 배제 목록과 정확히 같은 방향이다.
- **결론 참조 잔재 — 0건.** `impact_level` 전건 13곳을 읽었다. `HIGH`/`MEDIUM`/`LOW` 정의문은
  evidence 결손과 복구 개연성만 말하고, `verdict_state`·`certified_current`·기존 집계값을
  입력으로 쓰지 않는다고 **명시 배제**한다. §1.11.2 재수집 우선순위 문단, RC-2 `reason_impact_level`,
  X-9, X-14, I-23, §4.6 정렬키 — 어디에도 `결론을 바꿀 수 있는 정도` 계열 문면이 남아 있지 않다.
  **결론 중립 주장은 성립한다.**

### 3.4 `verdict_state = NA` 계열 신규 규칙 (T-4·T-6·X-13·X-15)

`A2` 자신이 자기공격으로 발견한 결손이다. 전건표 19·20행 + 규칙 충돌 해소 순서
(**T-6 > T-8 > T-7·T-4·T-9**)가 새로 명시됐고, `review_task_type = CRITERION_VERDICT`의 대상이
`{PASS, FAIL}` → `{PASS, FAIL, NA}`로 확장되면서 `NA ∧ ai_review_required = 1` 행의 S-3 무주지가
닫혔다. 그러면서 "이것이 `NA`에서 나가는 전이를 열지는 않는다"를 T-6·X-15로 이중 봉인한다.
전건 완전성 서술(4 × 2 × 4 = 32 → 1~4행 16 + 5~20행 16)도 산술이 맞다. **모순 없음.**

---

## 4. E-10 층화의 하류 정합

| 요구 지점 | 반영 |
|---|---|
| `01 §10 mart_archetype_summary` | ✓ E-10 1항 표 + §6.3(2022행) — `MPFED median`·`IQR`·`mode`·`ECDF`·`endpoint reach`를 층별 병기, **컬럼** `endpoint_via_auth_gate_rate` + 층별 `n` |
| `00 §7 ExcessDepth` | ✓ E-10 1항(기준선 = 같은 archetype 중앙값을 층별로) + **2항이 정본 산식 불변을 못박음** — `00 §7` 문면 그대로 유지되고 층별 값은 **병기**다. 새 분석 기준을 만들지 않는다는 §7 7항과 정합 |
| `00 §11` 분석 목록 | ✓ Depth 항목(median/IQR/mode/ECDF/`0/1/2/3/4+` 빈도)이 층화 대상으로 명시. `endpoint reach`·`auth gate`가 **서로 다른 지표**로 유지된다는 E-8 말미도 그대로 |
| `00 §12` 시각화 | ✓ **직접 조항은 없으나 §7 14항이 덮는다** — "두 endpoint 의미를 **한 분포에 섞어 제시하지 않는다**"는 제시(presentation) 금지이며 `00 §12` 2번(archetype별 MPFED 분포)·6번(ExcessDepth × …)이 그 범위 안이다. `A2`는 어휘·스키마 바인딩 문서이고 시각화는 P-F 소관이므로 별도 절을 요구하지 않는다 |
| 층 크기 노출 | ✓ 3항 — 한 층의 `n`이 작으면 "그 사실을 적고 산출하지 않는다" |
| 해석 금지 | ✓ 5항 — 어느 층이 좋다/나쁘다고 말하지 않는다(`00 §14`) |
| 실패주입 | ✓ I-9(층 구분 없는 합산값만 산출 → 차단) · §6.3 가드 |

**A1 §2.4 절단 취급과 충돌하지 않는가 — 충돌하지 않는다. 문서가 그 축 구분을 명시적으로 다룬다.**
`A2` §1.5.2 (602–604행):

> **절단과 층화는 서로 다른 축이다.** 위 표는 `UNRESOLVED_*`(잴 수 **없었던** 관측)의 취급이고,
> 규칙 E-10의 층화는 `FUNCTION_ENDPOINT_REACHED`(**잰** 관측)를 endpoint의 **의미**로 가르는 것이다.
> 두 archetype에서는 두 축이 함께 적용된다 — 층별로 산출하고, 각 층 안에서 절단 건수를 별도로 노출한다.

두 축의 정의역이 서로소(`MPFED = NULL` vs `MPFED = 정수`)이므로 논리적으로도 교차 적용이 성립한다.
`A1` §2.4 표는 절단만 말하고 층화를 모르지만, 그것은 `A1`이 **언제 절단인가**를 정하는 절이고
층화는 `A2`가 **어떻게 집계하는가**를 정하는 규칙이므로 분업 위반이 아니다. **모순 0.**

---

## 5. 신규 열거값·필드의 전파 완결성

전건에 대해 §1.0 수준표 · §1.11 전이표/금지전이 · §4 지표 · §5~§6 산출목록 · §7 금지사항을
grep 전수 대조했다. **"한 곳만 고쳐진" 흔적은 0건이다.**

| 값/컬럼 | §1.0 | §1.11 | §4 | §6 산출 | §7 |
|---|---|---|---|---|---|
| `auth_gate_before_endpoint` | (0/1 플래그 — 상태값 표 대상 아님) | — | E-8 합집합 | §6.3 **컬럼 등재**(2021행) | 11항 |
| `auth_gate_observed` (파생) | — | — | E-8 | §6.3 **가드**(2035행) | 11항 |
| `endpoint_status_detail` 신규값 | ✓ Task 행 | T-5 | E-4 절단표 · E-10 층화 | §6.3 **4값**(2020행) | 11 · 14항 |
| `review_task_type` 확장(`NA` 편입) | ✓ Adjudication 행 | 전건표 19·20행 · T-6 · X-15 | §4.5 규칙 B-2 | §6.3 **5값**(2031행) | 15항 |
| `recollection_preregistration` (5필드) | (run 단위 산출물) | §1.11.2 RC-2 | §4.1 주의 4 · §4.2 RC-3 · §4.6 | §6.3 **run manifest 행**(2023행) | 13항 |
| `MAX_RECOLLECTION_RUNS_PER_WEB_TARGET` | — | RC-1 | §4.2 | §6.3 **상수 행**(2024행) · **§6.4 동결 목록** | 13항 |
| `recollection_rate` | — | RC-5 | §4.2 병기 의무 | §6.3 **지표 행**(2025행) | 13항 |
| `endpoint_via_auth_gate_rate` + 층별 `n` | — | — | E-10 1항 | §6.3 **컬럼**(2022행) | 14항 |

숫자 정합도 맞다 — `endpoint_status_detail` **4값**, `measurement_status` **7값**,
`review_task_type` **5값**, `ended_by` **9값**, `dismiss_method` **5값**이 §1 본문과 §6.3에서 동일하다.
실패주입 대응표 **I-1~I-29**(+ I-28b)가 `V2-C003`·`V2-C004` 신규 규칙 전건을 태우고,
§1.11의 V-a~V-g와 범위를 서로 배타적으로 나눈다고 명시한다(1249행) — **겹침도 공백도 없다.**

`A2` §1.6·§1.12를 `A1` §3.2·§3.3·§4.2·§4.3과 값 단위로 대조했다 — `dismiss_method` 5값,
`dismiss_failure_mode` 5값 + `NULL`, `ended_by` 9값, `input_mode` 3값, `episode_kind` 2값이
**전부 일치**한다. 현재 **살아 있는 A1↔A2 값 모순은 0건**이다.

---

## 6. 설치 무결성 실측

### 6.1 스크립트 실행

```
$ python3 …/landing_v2_exec/research/landing_accessibility/scripts/verify_v2_docs.py
OK  CLAUDE.md [pack+banner] / docs/INDEX.md [repo-authored] / 00~05 [pack] ×6
OK  A1 · A2 · EXECUTION_AUTHORITY · PHASE_GATES [repo-authored] ×4
OK  README.md · bootstrap/07 [pack+banner] ×2
OK  docs/v2/MANIFEST.json · docs/v2/INSTALL_MANIFEST.json [manifest, git-anchored]
V2_DOCS_VERIFY: PASS          (exit 0)

$ python3 -m pytest tests/test_v2_docs_integrity.py -q     → 2 passed
```

exec 워크트리 `HEAD = d5f1da5652…`, `git status --porcelain` **공란**.

### 6.2 스크립트를 신뢰하지 않은 독립 검증

**(a) 갱신 3항목 — 실제 파일과 일치.**

| 파일 | 선언 bytes | 실측 bytes | sha256 |
|---|---|---|---|
| `docs/v2/A1_MEASUREMENT_OPERATIONALIZATION.md` | **36,510** | 36,510 | ✓ `99dabb6f…` |
| `docs/v2/A2_VOCABULARY_AND_SCHEMA_BINDING.md` | **175,606** | 175,606 | ✓ `927ffe56…` |
| `docs/v2/EXECUTION_AUTHORITY.md` | **11,785** | 11,785 | ✓ `81895fe0…` |

`INSTALL_MANIFEST.json` **14항목 전건**의 `bytes`/`sha256`을 재계산해 **전부 일치**(불일치 0).

**(b) 배너 3종 — `body_sha256` · `banner_sha256` 재계산 + 미앵커 잔여 바이트 특정.**

| 설치본 | 원본 | `raw.endswith(orig)` | 원본 출현 | banner 길이 | `banner_sha256` | **어느 쪽에도 앵커 안 되는 잔여** |
|---|---|---|---|---|---|---|
| `docs/v2/README.md` (2,698B) | `SSOT/README.md` (1,753B) | **True** | 1 | 943B | ✓ `c0418167…` | `b"\n\n"` |
| `docs/v2/bootstrap/07_…md` (9,337B) | `SSOT/07_…md` (8,217B) | **True** | 1 | 1,118B | ✓ `fea1fce5…` | `b"\n\n"` |
| `CLAUDE.md` (4,012B) | `SSOT/06_…md` (2,725B) | **True** | 1 | 1,285B | ✓ `3cfc77c4…` | `b"\n\n"` |

세 파일 모두 `body_sha256`이 `SSOT/` 원본 sha256과 일치한다.
**잔여 2바이트에 은닉 표면이 없다** — `strip_banner()`가 `tail.lstrip(b"\n")`으로 본문을 얻으므로
그 자리에 개행 외 바이트가 들어가면 곧바로 본문에 편입돼 `DRIFT`로 걸린다.

**(c) `MANIFEST.json` 9항목** 전건을 `SSOT/` 원본과 재계산 대조 → **전건 일치**.

### 6.3 배너 위조 공격 재현 — 차단 실증

executor의 주장을 믿지 않고 `verify_v2_docs.py`를 임포트해 그 함수에 직접 위조 입력을 넣었다.

| 변종 | 결과 |
|---|---|
| `00_SSOT_v2.0.md`(서열 1위) 최상단에 위조 조항을 배너로 삽입 | **차단 ×2** — ① 허용목록 밖 배너(`BANNER_ALLOWED` 3키에 없음) ② `banner_inserted=false` 선언 ↔ 실제 `true` 불일치. 매니페스트 해시만 갱신해도 두 검사가 남는다 |
| 허용 파일(`CLAUDE.md`)의 `BANNER-END` **뒤·본문 앞** 틈에 위조 조항 주입 | **차단** — `body ≠ pack 원본` → `DRIFT` |
| 첫 배너 뒤에 **두 번째 배너 블록** 추가 | **차단** — `find()`가 첫 END까지만 걷어내므로 두 번째 블록이 body에 편입 → `DRIFT` |
| `BANNER_START` **앞**에 위조 조항 | **차단** — `data[:start]`가 body에 편입 → `DRIFT` |

adversarial V2-C003이 뚫었던 경로가 **실제로 닫혔다.** 정책이 문장이 아니라 코드에 있다.

### 6.4 `00~05` · `MANIFEST.json` 바이트 무변경

`git diff 4a0585e0 d5f1da56 -- docs/v2/0*.md docs/v2/MANIFEST.json` → **공란**.
BASE(`5a9015d1`) 이후 이 7종의 **수정 이력은 0**이다.

**설치 무결성 P0/P1/P2 0건.**

---

## 7. 신규 finding

### F1 `a1-0-3-jurisdiction-declaration-drifts-from-a2-and-collides-with-existing-section` — P2 / blocking (`V2_SSOT_FROZEN`)

**파일** `research/landing_accessibility/docs/v2/A1_MEASUREMENT_OPERATIONALIZATION.md` §0.3 (42–60행, 표 47–55 · drift 조항 57) ·
§0.3 금지 재확인 (62행) · `A2_VOCABULARY_AND_SCHEMA_BINDING.md` (9 · 29 · 546 · 579 · 2130행)

이번 커밋이 신설한 **A1 §0.3 관할 선언표**는 자기 조항을 스스로 위반한다.
표 바로 밑 문장이 이렇게 못박는다.

> 이 표는 A2 §0의 대칭 조항과 **같은 목록을 담아야 한다.** 한쪽만 갱신되면 그 자체가 drift다.

**(1) 두 목록이 이미 어긋나 있다.**

| 소스 | 목록 |
|---|---|
| `A1` §0.3 **표** (47–55행) | §1.5 · §1.8 · **§2.2** · §3.4 · §4.4 · §5.1 · §6.1 |
| `A2` §0 4항 (33–36행) · §1 머리말(118행) | §1.8 · **§2.2** · §3.2~§3.4 · §4.2~§4.4 · §5.1 · §6.1 |
| `A1` §0.3 **후속 문단** (59–60행, 옛 §0.2 문장 존치) | §1.8 · §3.4 · §4.4 · §5.1 · §6.1 |

**세 목록이 서로 다르다.** A1 표는 `§3.2` · `§3.3` · `§4.2` · `§4.3`을 빠뜨리고, A2는 `§1.5`를
빠뜨리며, A1의 후속 문단은 표 바로 위에서 확정한 **`§1.5`와 `§2.2`를 둘 다 빠뜨린다**
— 즉 이번 사이클이 고친 바로 그 §2.2가 같은 절 안에서 두 문단 사이에 모순된다.

**이것이 권위 문제인 이유.** `EXECUTION_AUTHORITY` §2에서 `A1`은 **8위**, `A2`는 **9위**다.
A1이 명시적으로 위임하지 않은 절에서는 **서열상 A1이 A2를 이긴다.** 그런데 A2 §0 4항은
`A1 §3.2~§3.3 · §4.2~§4.3`의 값 도메인 관할을 주장한다 — 그 절들은 실제로
`dismiss_method` 5값 · `dismiss_failure_mode` 5값 · `ended_by` 9값 · `input_mode` 값을
**A1 본문에서 직접 열거**한다(A1 §3.3 표 · §4.2 · §4.3). 관할 주장이 한쪽에만 있는 상태다.
지금은 A2 §1.6·§1.12가 A1과 값 단위로 일치하므로 **살아 있는 모순은 없다**(§5에서 확인).
그러나 이것은 두 사이클을 소모한 `a1-endpoint-status-placement-contradicts-a2`와 **정확히 같은
구조**이며, 어느 한쪽이 먼저 갱신되는 순간 같은 분쟁이 4개 절에서 재발한다.

**(2) 표의 매핑 한 행이 틀렸다.** `A1 §1.8 → A2 §1.5.2`로 적혀 있다.
`A1` §1.8은 `area_signal_detected` · `depth_segment` · `counts_toward_depth` ·
`area_signal_status` · `region_definition` · `region_signal_type` 6필드를 도입하는 절이고,
그 값 도메인은 `A2` **§1.5.3**(앞 넷)과 **§1.9**(뒤 둘, 절 제목이 문자 그대로
`region_definition · region_signal_type — A1 §1.8 신규 필드`)에 있다.
`A2` §1.5.2는 `endpoint_status_detail` 절로 **A1 §1.8과 무관**하다.
`A2` §0 4항이 등재처를 `§1.5 · §1.6 · §1.9 · §1.12 · §1.13`으로 적은 것과도 어긋난다.

**(3) 절 번호가 충돌한다.** A1에 **§0.3이 두 개** 생겼다 — 42행 `0.3 A2가 우선하는 절 (관할 선언)`,
62행 `0.3 금지 재확인`(기존). `A2` §7 7-b(2130행)가 세 축 단일 종합점수 금지의 근거로
**`A1 §0.3`을 인용**하는데, 그 인용이 이제 두 절 중 어느 쪽인지 정해지지 않는다.

**(4) A2의 A1 인용 4곳이 상하게 됐다.** 위임 문장이 §0.2에서 §0.3으로 옮겨졌으나
`A2`는 여전히 `A1 §0.2`를 인용한다 — 9행(머리말 `어휘 관할`) · 29행(§0 3항 서열표 8위 행) ·
546행(§1.5.1a `A1과의 정합`) · 579행(§1.5.2 배치 확정 근거). 현재 `A1` §0.2는
`이 문서가 닫는 finding` 표다. **관할의 근거로 인용되는 절 번호가 관할과 무관한 절을 가리킨다.**

**시정(한 커밋).**
(a) 신설 절을 `§0.2a`(또는 뒤 절을 §0.4·§0.5로 밀기)로 **재번호**해 §0.3 충돌을 없앤다.
(b) 표 목록을 `A2` §0 4항과 **동일하게** 맞춘다 — `§3.2` · `§3.3` · `§4.2` · `§4.3`을 추가하고,
`A2` §0 4항·§1 머리말에 `§1.5`를 추가한다(양쪽을 같은 커밋에서 고쳐야 drift 조항이 참이 된다).
(c) `§1.8 → A2 §1.5.3 · §1.9`로 매핑을 정정한다.
(d) 표와 중복·모순되는 **후속 문단(59–60행)을 삭제**한다. 사본은 drift한다.
(e) `A2`의 `A1 §0.2` 인용 4곳과 `A1 §0.3` 인용 1곳을 새 절 번호로 갱신한다.

**차단 범위.** `V2_SSOT_FROZEN`. 동결은 "이 문서 집합을 그대로 이행해도 된다"는 선언인데,
관할을 선언하는 절이 스스로 세 갈래로 갈리고 다섯 개의 상호참조가 빈 자리를 가리키는 상태에서는
어느 문서가 어느 값을 확정하는지가 문서만으로 결정되지 않는다. 전부 문면 수정이며 P-C를 막지 않는다.

---

## 8. 문서 ↔ state 정합

| 검사 | 결과 |
|---|---|
| `EXECUTION_AUTHORITY §4` v1 원장 (`total 24 / closed_pending_audit 3 / open 21`, E001-blocking 6) | `state.json.debt_inheritance` = v1_total **24** / v1_open **21** / v1_open_e001_blocking **6** — **일치** |
| `EXECUTION_AUTHORITY §4` v2 원장 서술 (V2-C001 이후 **모든 사이클**) | `v2_audit_findings.totals` = `V2_C001` · `V2_C002` · `V2_C003` · `orchestrator_registered` 4키 — **일치** (F4 CLOSED) |
| `EXECUTION_AUTHORITY §5` adversarial `ea1d2448eedc80259b421a2b8bb3b840ba6dced8` | `git ls-remote` `refs/heads/audit/landing-adversarial` = **동일**, `merge-base --is-ancestor` OK — **일치** |
| `EXECUTION_AUTHORITY §5` ssot `43fab0acb3a9352c36b9c0b844cb44cef9f9d79d` | `refs/heads/audit/landing-ssot` = **동일**, ancestor OK — **일치** |
| `EXECUTION_AUTHORITY §6` V2-C003 행 (adversarial P0 0/P1 1/P2 7 · ssot P0 0/P1 0/P2 5) | 두 보고서 JSON `counts` 원본과 **일치** |
| `EXECUTION_AUTHORITY §8` 승격 검사 이름 9종·순서 | `promote_landing_main.sh` 헤더 35–44행과 **완전 일치** |
| `open_blocking_total = 19` 재계산 | 6(v1) + 0(V2-C001) + 1(V2-C002 adv 잔여) + 1(V2-C002 ssot 잔여) + 7(V2-C003 adv) + 3(V2-C003 ssot) + 1(오케스트레이터) = **19**. 단계 ③이 인용한 "ssot blocking 4건 중 3 CLOSED, 잔존 1 = `a1-endpoint-status-placement…`"와 단계 ⑤의 "신규 3 + 승계 1 = 4"가 **내 V2-C003 보고서 원본과 정확히 일치**. `previous_total 17 → delta +2`도 성립 |
| V2-C004 control 시정분 미차감 | `v2_c004_remediation_not_deducted` — self-approval 금지 원칙대로 **빼지 않았다.** 옳다 |
| `E001_V2_STARTED = false` · `FULL_COLLECTION_STARTED = NO` · `CURRENT_GATE = V2_SSOT_FROZEN(미달성)` | `state.json.v2_transition`과 **일치** |
| `PILOT = READ_ONLY` @ `32460b87…` | `refs/heads/research/refcohort-r1` = 동일, diff 0 — **일치** |
| `authoritative main = 5a9015d1…` / `origin/main a835d5d8`은 authoritative 아님 | 실재·귀속 확인 — **일치** |
| `old C013 WIP = 87a0464e…` `UNVERIFIED` | `refs/heads/agent/landing-exec` = 동일 — **일치** |

`state.json`의 `v2_exec_sha`는 `4a0585e0`(V2-C003 감사 대상)이며 이번 target `d5f1da56`은 아직
등재 전이다 — control이 이번 사이클 감사 이후 갱신하는 정상 순서이므로 결함이 아니다.

**F1을 제외하면 문서↔state 불일치 0건.**

---

## 9. PHASE_GATES 회귀 없음 · 기준선 SHA 정확성

- `git diff 4a0585e0 d5f1da56 -- docs/v2/PHASE_GATES.md` → **공란**. 회귀 0.
  `V2_SSOT_FROZEN` 통과조건(`해당 Gate의 blocking finding 0건` · 무결성 스크립트 exit 0 ·
  승격 가드 실효)은 V2-C002·V2-C003에서 검증한 그대로다.
- 기준선 SHA **7건 전건**을 `git ls-remote --heads origin`으로 실재·귀속 확인. **오류 0.**

---

## 10. 관찰 (finding 아님)

- **`EXECUTION_AUTHORITY` §5 orchestrator 행**이 `bfa16624… 이후 V2-C002`로 남아 있고 control은
  `580e6721`(V2-C004)이다. exec 커밋은 control의 후속 SHA를 알 수 없으므로 구조적 지연이다.
- **`A2` §1.11 실패주입 표의 V-d~V-g 4행**이 V-a~V-c와 빈 줄로 분리돼 별도 표로 렌더된다.
  내용·id·기대는 정확하므로 이행에 영향이 없다. 다음 편집 때 붙이면 된다.
- **`ned-ied-path-minimality-not-operationalized`**(adversarial V2-C002 승계)와
  `install-manifest-is-self-anchored`는 adversarial 소관이며 본 감사 집계 밖이다.
- `A2` §0 5항의 `recollection_preregistration` 단서 조항은 **잘 놓였다** — "새 컬럼을 요구하지
  않는다"는 자기 규범과 RC-2의 기록 요구가 충돌할 뻔한 자리를 미리 열어 두었다.

---

## 11. 집계와 차단 범위

| 등급 | 건수 | id |
|---|---|---|
| P0 | **0** | — |
| P1 | **0** | — |
| P2 (blocking) | **1** | F1 (`V2_SSOT_FROZEN`) |
| P2 (비차단) | **0** | — |
| 승계 blocking | **0** | — |

`PHASE_GATES.md §2`가 `해당 Gate의 blocking finding이 0건`을 요구한다.
`V2_SSOT_FROZEN`을 막는 ssot 귀속 blocking은 **1건**이며 전부 문면 수정으로 한 커밋에 닫힌다.

**FAIL.** 다만 이번 사이클은 시정 쪽으로 크게 기울었다 — 내 finding 5건 전건 CLOSED,
두 사이클 묵은 승계 건 해소, 커뮤니티 gate 무주지 실제 소멸, 배너 정책의 코드 강제가
공격 재현으로 실증됐다. 남은 하나는 그 시정이 만든 **관할 선언 자체의 마무리**다.
