{
 "schema": "C_GATE1_PREREGISTRATION",
 "version": "1.1",
 "plane": "Claude C (Independent Scientific Assurance)",
 "base_commit": "d597c84",
 "branch": "claude-c/assurance-v21",
 "generated_on": "2026-08-28",
 "inputs_read_only": {
  "lane1": "lane1_task_binding/{README.md,EXPECTATIONS.json,task_contracts.json}",
  "lane2": "lane2_label_reveal/{README.md,EXPECTATIONS.json meta.*}",
  "lane3": "lane3_sequence_dismiss_auth/{README.md §Definitional boundaries,EXPECTATIONS.json derivation_rules_preregistered}",
  "lane4": "lane4_safety_adapter/{GATE1_SAFETY_PLAN.md,README.md,forbidden_action_matrix.json}",
  "lane5": "lane5_evidence/{EVIDENCE_CONTRACT_C.md,README.md}",
  "lane6": "lane6_stats/{README.md §Pre-registered C choices,c_flow_derive.py docstrings/constants}",
  "lane7": "lane7_grain_determinism/DISMISS_DEFINITION_C.md (OUTSIDE the six-lane brief; present in the gate1 namespace at d597c84; cross-referenced only)",
  "A": ".agent_bus/landing_v2/tickets/{T-A-V3-STEP1-003,T-A-V3-STEP1-006,T-A-V3-STEP1-007}.json (A rulings; supersede C choices)"
 },
 "tie_break_authority": [
  "SSOTV3/04_FLOW_CODEBOOK_v3.0.md",
  "SSOTV3/03_COLLECTION_MEASUREMENT_SPEC_v3.0.md",
  "SSOTV3/02_DATA_SCHEMA_v3.0.md",
  "SSOTV3/05_ANALYSIS_PLAN_v3.0.md",
  "A tickets T-A-V3-STEP1-003 (R1..R7), T-A-V3-STEP1-006 (activation_depth 18-token classification), T-A-V3-STEP1-007 (R11..R14) where ruled"
 ],
 "not_read": [
  "any .agent_worktrees/claude_b_*",
  "any .agent_worktrees/claude_d_*",
  "network"
 ],
 "status_vocabulary": [
  "CONSISTENT",
  "RECONCILIATION_REQUIRED",
  "PROVISIONAL_PENDING_A",
  "C_DECIDED",
  "A_RULED"
 ],
 "md_sha256": "5f86adddcb7e8129861bd76df9b5b72abc7b0c0dbd46489b8e8be6db02ff7f95",
 "summary": {
  "n_choices": 73,
  "provisional_pending_a": [
   "P-62"
  ],
  "a_ruled": [
   {
    "id": "P-02",
    "authority_override": "CONFIRMED_BY_A_STEP1-006"
   },
   {
    "id": "P-03",
    "authority_override": "CONFIRMED_BY_A_STEP1-006"
   },
   {
    "id": "P-04",
    "authority_override": "SUPERSEDED_BY_A_STEP1-006"
   },
   {
    "id": "P-05",
    "authority_override": "CONFIRMED_BY_A_STEP1-006"
   },
   {
    "id": "P-07",
    "authority_override": "CONFIRMED_BY_A_STEP1-006"
   },
   {
    "id": "P-35",
    "authority_override": "CONFIRMED_BY_A_STEP1-006"
   },
   {
    "id": "P-36",
    "authority_override": "SUPERSEDED_BY_A_R7"
   },
   {
    "id": "P-37",
    "authority_override": "SUPERSEDED_BY_A_R7"
   },
   {
    "id": "P-40",
    "authority_override": "A_RULED_R2"
   },
   {
    "id": "P-41",
    "authority_override": "A_RULED_R3"
   },
   {
    "id": "P-42",
    "authority_override": "A_RULED_R4"
   },
   {
    "id": "P-43",
    "authority_override": "A_RULED_R5"
   },
   {
    "id": "P-44",
    "authority_override": "A_RULED_R6_Q8"
   },
   {
    "id": "P-45",
    "authority_override": "A_RULED_R6_Q1"
   },
   {
    "id": "P-46",
    "authority_override": "CONFIRMED_BY_A_R12"
   },
   {
    "id": "P-54",
    "authority_override": "EXTENDED_BY_A_R11"
   },
   {
    "id": "P-67",
    "authority_override": "CONFIRMED_BY_A_R14"
   },
   {
    "id": "P-69",
    "authority_override": "A_RULED_STEP1-006"
   },
   {
    "id": "P-70",
    "authority_override": "A_RULED_STEP1-006"
   },
   {
    "id": "P-71",
    "authority_override": "A_RULED_R11"
   },
   {
    "id": "P-72",
    "authority_override": "SUPERSEDED_BY_R13"
   }
  ],
  "reconciliation_required_r1": [
   {
    "id": "P-06",
    "variable": "SWITCH_TAB as a reveal token (feeds menu_dependency and nav_container_depth)",
    "divergent_case": "lane1 informative reference for C-F4-DECOY-01: SWITCH_TAB > SELECT_CATEGORY > SUBMIT_QUERY > ENDPOINT_REACHED → lane6 menu_dependency = 0 (menu_dependency_incl_tab = 1), nav_container_depth = 0; lane3 rule yields NO value ('undecided'); lane2 implicit rule → 0."
   },
   {
    "id": "P-09",
    "variable": "menu_dependency: basis of the derivation (S0-visibility-and-required-reveal vs any reveal token before terminal)",
    "divergent_case": "SELECT_FUNCTION > OPEN_LOCAL_MENU > SELECT_DATE > SUBMIT_QUERY > ENDPOINT_REACHED (entry control visible at S0; a local menu is opened later inside the task form): lane2 rule → 0 (entry control did not need a reveal); lane3/lane6 → 1 (a reveal token precedes the terminal). Same split for a non-minimal recorded path that opens a menu although the function control was visible at S0 (04 §3 example with the function visible at S0: lane2 0, lane3/lane6 1)."
   },
   {
    "id": "P-11",
    "variable": "nav_container_depth: anchor ('task control 노출 전') and behaviour when SELECT_FUNCTION is absent",
    "divergent_case": "(a) OPEN_GLOBAL_MENU > INPUT_QUERY > SUBMIT_QUERY > ENDPOINT_REACHED (search box lives inside the global menu; no SELECT_FUNCTION token): lane6 → 1 (anchor = INPUT_QUERY); lane3 rule has no anchor → undefined. (b) OPEN_GLOBAL_MENU > AUTH_GATE: lane6 → 1 with nav_anchor_found=False; lane3 → undefined. (c) A non-minimal path that opens a menu not required to expose the control: lane2 → 0 ('required'), lane3/lane6 → 1."
   },
   {
    "id": "P-13",
    "variable": "auth_gate_stage: what counts as task discovery / task select (BEFORE_TASK_DISCOVERY vs AFTER_TASK_SELECT boundary)",
    "divergent_case": "(a) OPEN_GLOBAL_MENU > SELECT_CATEGORY > AUTH_GATE: lane6 → AFTER_TASK_SELECT (SELECT_CATEGORY is a task-select token); lane3 → no category applies (not BEFORE, because a reveal container WAS available without auth; not AFTER, because no SELECT_FUNCTION occurred). (b) OPEN_GLOBAL_MENU > AUTH_GATE (menu opens, its contents demand login): lane6 → BEFORE_TASK_DISCOVERY; lane3 → undefined for the same reason."
   },
   {
    "id": "P-14",
    "variable": "auth_gate_stage: AT_ENDPOINT boundary",
    "divergent_case": "SELECT_FUNCTION > INPUT_QUERY > SUBMIT_QUERY > AUTH_GATE where the submit redirects straight to a login page (no results surface rendered): lane3 → AFTER_TASK_SELECT (no result-level token, no results surface); lane6 → AT_ENDPOINT (terminal AUTH_GATE after task-body tokens INPUT_QUERY/SUBMIT_QUERY). lane3 uses evidence beyond the sequence ('results surface rendered'); lane6 is sequence-only, so the same token string maps to two values."
   },
   {
    "id": "P-17",
    "variable": "endpoint_status: resolution from the sequence vs allowed partitions per fixture",
    "divergent_case": "Runner output for C-F5-NOSURF-01 with task_flow_sequence = [ABSTAIN] and endpoint_status = PUBLIC_WEB_UNOBSERVABLE: lane1 → PASS (both statuses allowed); lane6 compare_with_mart_row → diff {B: PUBLIC_WEB_UNOBSERVABLE, C: ABSTAIN} because action_token=ABSTAIN resolves endpoint_status=ABSTAIN and only UNRESOLVED_FROM_SEQUENCE suppresses the comparison."
   },
   {
    "id": "P-23",
    "variable": "task_control_occlusion: measurement method (hit-test vs geometry) and measured control",
    "divergent_case": "lane3 fixture seq_with_forced_dismissal at S0: lane3 measures the hamburger (path control of the current state) → 1.00 (hit) / 1.00 (geo); lane7 measures T = the SELECT_FUNCTION control, which is inside the closed drawer, so area(T ∩ V) = 0 → undefined. Also any overlay with pointer-events:none: lane3 hit-test → 0, lane7 geometry → > 0."
   },
   {
    "id": "P-24",
    "variable": "dismiss_required_for_task: blocking proof",
    "divergent_case": "(a) A role=dialog aria-modal=true container that neither overlaps nor intercepts pointer events on the path control: lane7 → true; lane3 → false (blocking-proof click succeeds). (b) A backdrop with occlusion > 0 but pointer-events:none: lane7 → true (geometry > 0); lane3 → false. (c) lane3's proof requires an activation on the task control while the interrupt is present; lane7 requires no activation."
   },
   {
    "id": "P-30",
    "variable": "frozen synonym map: shape and identity entries",
    "divergent_case": "visible='배송조회', accessible='택배조회': lane2 → SEMANTIC_EQUIV (pair present). lane6 → SEMANTIC_EQUIV only if the frozen map, once converted to form→key, contains the identity entry '배송조회'→'배송조회'; a naive conversion of lane2's map (forms only) leaves '배송조회' unmapped → DIFFERENT."
   }
  ],
  "reconciliation_required": [],
  "c_decided": [
   {
    "id": "P-11",
    "ticket": "C-DECISION_REQUEST-031138",
    "rule": "C decides (measurement-method choice, C's authority; C-DECISION_REQUEST-031138 c_decides_itself, A confirms in T-A-V3-STEP1-011 §C_self_decided_4_confirmed): nav_container_depth anchor = the first SELECT_FUNCTION or, when absent, the first task-body token (INPUT_QUERY / SELECT_CATEGORY / SELECT_ORIGIN / SELECT_DESTINATION / SELECT_DATE / SUBMIT_QUERY / SELECT_RESULT / OPEN_ITEM_DETAIL / OPEN_PLACE_DETAIL); reveals before the anchor are counted; no anchor → reveals before the terminal with nav_anchor_found=false (lane6 C-5 adopted). Divergent cases (a)/(b) → 1; (c) → 1 (sequence-based, consistent with P-09)."
   },
   {
    "id": "P-23",
    "ticket": "C-DECISION_REQUEST-031138",
    "rule": "C decides (C-DECISION_REQUEST-031138): PRIMARY task_control_occlusion = fraction of a 9×9 `elementFromPoint` grid on the path control of the CURRENT state whose topmost element is not the control or a descendant (lane3 method, sampling (i+0.5)/9, out-of-viewport points dropped, null when the control is outside the viewport); the geometric T∩V overlap on the frozen entry control is `occlusion_geom_crosscheck` only. Divergent cases resolve as lane3: seq_with_forced_dismissal S0 → 1.0 on the hamburger; pointer-events:none overlay → 0."
   },
   {
    "id": "P-24",
    "ticket": "C-DECISION_REQUEST-031138",
    "rule": "C decides (C-DECISION_REQUEST-031138; A credits it in T-A-V3-STEP1-011 as the exact application of presence ≠ operative): PRIMARY dismiss_required_for_task = the blocking proof — a click at the path control's centre while the interrupt is present leaves data-c-state unchanged (lane3); `aria-modal` and occlusion>0 / geometric overlap>0 are recorded as `dismiss_required_signal` and are never the verdict. Divergent cases: (a) aria-modal dialog that does not intercept → false (signal ARIA_MODAL); (b) pointer-events:none backdrop → false (signal GEOM_OVERLAP_GT0); (c) the proof requires the activation."
   },
   {
    "id": "P-30",
    "ticket": "C-DECISION_REQUEST-031138",
    "rule": "C decides (C-DECISION_REQUEST-031138): the frozen synonym map shape is canonical → [forms] with identity implicit (lane2's `synonym_map_fixed` as it stands); lane6's lookup becomes bidirectional (a string matches if it is the canonical key or any listed form; both sides in the same group ⇒ SEMANTIC_EQUIV). Divergent case → SEMANTIC_EQUIV."
   }
  ],
  "a_ruled_r2": [
   {
    "id": "P-06",
    "ticket": "T-A-V3-STEP1-011",
    "authority_override": "A_RULED_STEP1-011"
   },
   {
    "id": "P-09",
    "ticket": "T-A-V3-STEP1-011",
    "authority_override": "A_RULED_STEP1-011"
   },
   {
    "id": "P-13",
    "ticket": "T-A-V3-STEP1-011",
    "authority_override": "A_RULED_STEP1-011"
   },
   {
    "id": "P-14",
    "ticket": "T-A-V3-STEP1-011",
    "authority_override": "A_RULED_STEP1-011"
   },
   {
    "id": "P-17",
    "ticket": "T-A-V3-STEP1-011",
    "authority_override": "A_RULED_STEP1-011"
   },
   {
    "id": "P-08",
    "ticket": "T-A-V3-STEP1-012 GAP-03",
    "authority_override": "CONFIRMED_BY_A_STEP1-012_GAP-03"
   },
   {
    "id": "P-21",
    "ticket": "T-A-V3-STEP1-012 GAP-02",
    "authority_override": "SUPERSEDED_BY_A_STEP1-012_GAP-02"
   },
   {
    "id": "P-22",
    "ticket": "T-A-V3-STEP1-012 GAP-05",
    "authority_override": "CONFIRMED_BY_A_STEP1-012_GAP-05"
   },
   {
    "id": "P-25",
    "ticket": "T-A-V3-STEP1-012 GAP-04",
    "authority_override": "CONFIRMED_BY_A_STEP1-012_GAP-04"
   },
   {
    "id": "P-33",
    "ticket": "T-A-V3-STEP1-012 GAP-04",
    "authority_override": "SUPERSEDED_BY_A_STEP1-012_GAP-04"
   },
   {
    "id": "P-35",
    "ticket": "T-A-V3-STEP1-012 GAP-06",
    "authority_override": "EXTENDED_BY_A_STEP1-012_GAP-06"
   },
   {
    "id": "P-38",
    "ticket": "T-A-V3-STEP1-012 GAP-07",
    "authority_override": "EXTENDED_BY_A_STEP1-012_GAP-07"
   },
   {
    "id": "P-39",
    "ticket": "T-A-V3-STEP1-012 GAP-04",
    "authority_override": "CONFIRMED_BY_A_STEP1-012_GAP-04"
   },
   {
    "id": "P-73",
    "ticket": "T-A-V3-STEP1-012",
    "authority_override": "A_RULED_STEP1-012"
   }
  ],
  "status_counts": {
   "CONSISTENT": 54,
   "A_RULED": 14,
   "C_DECIDED": 4,
   "PROVISIONAL_PENDING_A": 1
  }
 },
 "what_these_are_not": [
  "C's pre-registered comparison rules, frozen from SSOTV3 text and C fixtures before any B runner output or real-service observation was seen",
  "not SSOT amendments; SSOTV3 is not edited or reinterpreted; gaps are cited as gaps",
  "any A ruling supersedes them; T-A-V3-STEP1-003 R2-R7, T-A-V3-STEP1-006, T-A-V3-STEP1-007 R11-R14 already do",
  "not derived from B or D definitions; no claude_b_*/claude_d_* worktree read",
  "not verdicts; no fixture run, no lane file modified; expectations are never fitted to runner output"
 ],
 "rows": [
  {
   "id": "P-01",
   "variable": "canonical token vocabulary (closed set; scroll / passive wait are not tokens)",
   "c_rules": [
    {
     "lane": "lane3",
     "wording": "token_sets.ACTIVATION_SET / REVEAL_SET / TERMINAL_SET / EXCLUDED_FROM_ACTIVATION_DEPTH enumerate only 04 §2 tokens; walk step 'SCROLL' is 'kind: scroll, runner_must_record: false … recorded in fact_surface_state (S1), never as a flow step'; activation_depth_excluded_tokens lists 'SCROLL(not a token)'."
    },
    {
     "lane": "lane6",
     "wording": "CANONICAL_TOKENS = the 18 tokens of 04 §2; classify_token 'Raises ValueError for a non-canonical token (scroll / passive wait are NOT tokens in 04 §2; they are measured elsewhere — 04 §4 first_visible_scroll_state)'; _clean raises unless drop_noncanonical=True."
    }
   ],
   "ssotv3_clause": "04 §2 (token table); 03 §3 'Scroll은 discovery/exposure 변수이며 activation depth에 포함하지 않는다'",
   "lane_sources": [
    "lane3",
    "lane6"
   ],
   "status": "CONSISTENT",
   "divergent_case": null,
   "notes": "Both lanes treat the vocabulary as closed and scroll as a surface (not flow) event.",
   "authority_override": null,
   "follow_up": null
  },
  {
   "id": "P-02",
   "variable": "activation_depth: SUBMIT_QUERY is a state-changing activation",
   "c_rules": [
    {
     "lane": "lane3",
     "wording": "'activation_depth counts SUBMIT_QUERY (button activation; 03 §6 excludes only scroll/passive/typing/dismiss)'; EXPECTATIONS: 'SUBMIT_QUERY IS counted (it is a button activation; 03 §6 exclusion list names only scroll, passive load, per-character typing, popup dismiss)'."
    },
    {
     "lane": "lane6",
     "wording": "C-1: 'SUBMIT_QUERY is a state-changing activation (executes a control, changes page state)'; SUBMIT_IS_ACTIVATION_DEFAULT = True; flip submit_is_activation=False, activation_depth_excl_submit always reported."
    },
    {
     "lane": "A",
     "wording": "STEP1-006: 'SUBMIT_QUERY 는 activation_depth 에 포함된다'; '두 제외 목록 어디에도 submit 이 없다. 03 의 포함 목록 첫 항목 link/button/tab/menu open 에 submit control 은 button 으로 들어간다'; IN_activation_depth lists SUBMIT_QUERY."
    }
   ],
   "ssotv3_clause": "04 §5 'activation_depth: state-changing activation token 수. scroll/typing/passive/dismiss 제외'; 03 §6 Depth 포함/제외 list; A T-A-V3-STEP1-006",
   "lane_sources": [
    "A",
    "lane3",
    "lane6"
   ],
   "status": "CONSISTENT",
   "divergent_case": null,
   "notes": "Identical primary value in both lanes, now confirmed by A. lane6's activation_depth_excl_submit alternative may remain as a declared sensitivity but is no longer a live choice. 04 §3 example → 2.",
   "authority_override": "CONFIRMED_BY_A_STEP1-006",
   "follow_up": null
  },
  {
   "id": "P-03",
   "variable": "activation_depth: INPUT_QUERY is typing → excluded from activation_depth, included in flow_step_count",
   "c_rules": [
    {
     "lane": "lane3",
     "wording": "EXCLUDED_FROM_ACTIVATION_DEPTH includes INPUT_QUERY; 'Typing/submit counted' in flow_step_count."
    },
    {
     "lane": "lane6",
     "wording": "C-2: 'INPUT_QUERY … are typing / form-intent → not activation depth (04 §5 \"typing 제외\") but are flow steps'; FORM_INTENT_TOKENS; activation_depth_incl_form reported as alt."
    },
    {
     "lane": "A",
     "wording": "STEP1-006 OUT_activation_depth: 'INPUT_QUERY: 타이핑이다. 03·04 둘 다 명시 제외. flow_step_count 에는 포함'."
    }
   ],
   "ssotv3_clause": "04 §5; 03 §6 '텍스트 한 글자 입력' excluded, 'flow_step_count에는 task-intent typing/submit을 별도 token으로 보존'; A T-A-V3-STEP1-006",
   "lane_sources": [
    "A",
    "lane3",
    "lane6"
   ],
   "status": "CONSISTENT",
   "divergent_case": null,
   "notes": null,
   "authority_override": "CONFIRMED_BY_A_STEP1-006",
   "follow_up": null
  },
  {
   "id": "P-04",
   "variable": "activation_depth: SELECT_ORIGIN / SELECT_DESTINATION / SELECT_DATE membership",
   "c_rules": [
    {
     "lane": "lane3",
     "wording": "token_sets.ACTIVATION_SET = [OPEN_GLOBAL_MENU, OPEN_LOCAL_MENU, SWITCH_TAB, EXPAND_ACCORDION, SELECT_CATEGORY, SELECT_FUNCTION, SELECT_ORIGIN, SELECT_DESTINATION, SELECT_DATE, SUBMIT_QUERY, SELECT_RESULT, OPEN_ITEM_DETAIL, OPEN_PLACE_DETAIL] — the three form-selection tokens ARE activations."
    },
    {
     "lane": "lane6",
     "wording": "C-2: 'INPUT_QUERY, SELECT_ORIGIN, SELECT_DESTINATION, SELECT_DATE are typing / form-intent → not activation depth (04 §5 \"typing 제외\") but are flow steps'; FORM_INTENT_TOKENS = {INPUT_QUERY, SELECT_ORIGIN, SELECT_DESTINATION, SELECT_DATE} → state_changing_activation=False unless form_is_activation=True."
    },
    {
     "lane": "A",
     "wording": "STEP1-006 CONDITIONAL: tokens SELECT_ORIGIN, SELECT_DESTINATION, SELECT_DATE — '입력수단에 따라 갈린다. picker/dropdown/calendar 처럼 control 을 활성화해야 값이 정해지면 activation_depth 에 포함한다. 자유입력란에 타이핑했다면 타이핑이므로 제외하고 flow_step_count 에만 넣는다'; how_to_decide 'fixture_input_mode 가 이미 이것을 기록한다 — DROPDOWN/MAP_PAN 계열이면 포함, FREE_TEXT 면 제외, MIXED 면 실제로 사용한 수단 기준'; record '각 관측에 depth_conditional_tokens 로 어느 토큰이 어떤 근거로 포함/제외됐는지 남긴다'."
    }
   ],
   "ssotv3_clause": "04 §5; 03 §6; A T-A-V3-STEP1-006 (CONDITIONAL) + T-A-V3-STEP1-003 R5 (fixture_input_mode)",
   "lane_sources": [
    "A",
    "lane3",
    "lane6"
   ],
   "status": "CONSISTENT",
   "divergent_case": "Pre-ruling: F5 sequence SELECT_FUNCTION > SELECT_ORIGIN > SELECT_DESTINATION > SELECT_DATE > SUBMIT_QUERY > ENDPOINT_REACHED → lane3 activation_depth = 5 (blanket IN); lane6 = 2 (blanket OUT; incl_form alt = 5). No lane3 fixture exercises these tokens, so the split never surfaced in fixture values. Under A's rule the same sequence is 5 when the three values were set via picker/dropdown/calendar/map (fixture_input_mode DROPDOWN/MAP_PAN) and 2 when typed into free-text fields (FREE_TEXT); MIXED decides per token by the means actually used.",
   "notes": "Status CONSISTENT because A decided; neither lane's blanket rule survives.",
   "authority_override": "SUPERSEDED_BY_A_STEP1-006",
   "follow_up": "lane6 C-2 blanket exclusion and lane3 ACTIVATION_SET blanket inclusion are both superseded: classify_token needs a per-token input-mode argument (or per-observation depth_conditional_tokens) and derive must emit depth_conditional_tokens with the reason; lane3 EXPECTATIONS token_sets must move the three tokens to a CONDITIONAL set. C's GATE 1 check: CONDITIONAL tokens handled consistently with fixture_input_mode (A's expected_response for C)."
  },
  {
   "id": "P-05",
   "variable": "activation_depth: SWITCH_TAB is a state-changing activation",
   "c_rules": [
    {
     "lane": "lane3",
     "wording": "SWITCH_TAB ∈ ACTIVATION_SET."
    },
    {
     "lane": "lane6",
     "wording": "SWITCH_TAB ∈ ACTIVATION_CORE (docstring: 'Unambiguous state-changing activations (open / switch / expand / select / detail)')."
    },
    {
     "lane": "A",
     "wording": "STEP1-006 IN_activation_depth includes SWITCH_TAB."
    }
   ],
   "ssotv3_clause": "04 §5; 03 §6 'link/button/tab/menu open' 포함; A T-A-V3-STEP1-006",
   "lane_sources": [
    "A",
    "lane3",
    "lane6"
   ],
   "status": "CONSISTENT",
   "divergent_case": null,
   "notes": "Two different variables: SWITCH_TAB counts for activation_depth (A: IN) while the lanes treat it as NOT a reveal token for menu_dependency / nav_container_depth (P-06, not ruled by A). Conflation check: lane3 keeps SWITCH_TAB in ACTIVATION_SET and out of REVEAL_SET; lane6 keeps it in ACTIVATION_CORE and out of REVEAL_TOKENS; lane2 defines no activation rule. No lane conflated the two variables.",
   "authority_override": "CONFIRMED_BY_A_STEP1-006",
   "follow_up": null
  },
  {
   "id": "P-06",
   "variable": "SWITCH_TAB as a reveal token (feeds menu_dependency and nav_container_depth)",
   "c_rules": [
    {
     "lane": "lane2",
     "wording": "menu_dependency_rule names the reveal activations as '(OPEN_GLOBAL_MENU / OPEN_LOCAL_MENU / EXPAND_ACCORDION)' — SWITCH_TAB not listed (implicit exclusion)."
    },
    {
     "lane": "lane3",
     "wording": "REVEAL_SET = [OPEN_GLOBAL_MENU, OPEN_LOCAL_MENU, EXPAND_ACCORDION]; 'SWITCH_TAB is left undecided (not exercised by these fixtures) and must be pre-registered before use.'"
    },
    {
     "lane": "lane6",
     "wording": "C-3: 'reveal tokens = OPEN_GLOBAL_MENU, OPEN_LOCAL_MENU, EXPAND_ACCORDION (the three named in 04 §5); SWITCH_TAB is a view switch, not a reveal'; menu_dependency_incl_tab always reported."
    }
   ],
   "ssotv3_clause": "04 §5 'OPEN_GLOBAL_MENU/OPEN_LOCAL_MENU/EXPAND_ACCORDION 등 reveal token' (the '등' leaves the set open)",
   "lane_sources": [
    "lane2",
    "lane3",
    "lane6"
   ],
   "status": "A_RULED",
   "divergent_case": "lane1 informative reference for C-F4-DECOY-01: SWITCH_TAB > SELECT_CATEGORY > SUBMIT_QUERY > ENDPOINT_REACHED → lane6 menu_dependency = 0 (menu_dependency_incl_tab = 1), nav_container_depth = 0; lane3 rule yields NO value ('undecided'); lane2 implicit rule → 0.",
   "notes": "lane3 explicitly demands pre-registration before use; lane6 has pre-registered 'not a reveal' with the alternative reported. Adopting C-3 across lanes is the obvious closure but is NOT made here. A's STEP1-006 classifies SWITCH_TAB for activation_depth only (IN) and says nothing about reveal membership, so this row stays open.",
   "authority_override": "A_RULED_STEP1-011",
   "follow_up": null,
   "resolution_r2": "A accepts C's reading (T-A-V3-STEP1-011 P-06): SWITCH_TAB is NOT a reveal token — 04 §5's '등' is bounded by 'OPEN/REVEAL 계열'; a tab switch changes which panel is active and reveals no hidden control; nav_container_type has no tab value. Divergent case → menu_dependency 0, nav_container_depth 0; `menu_dependency_incl_tab` kept as the declared sensitivity. Axis separation: SWITCH_TAB stays IN for activation_depth (STEP1-006).",
   "lane_files_r2": "lane3 EXPECTATIONS.menu_dependency + README §3 (r2); lane2 meta.menu_dependency_rule annotated; lane6 C-3 already matches (not edited here)",
   "authority_ticket": "T-A-V3-STEP1-011"
  },
  {
   "id": "P-07",
   "variable": "activation_depth exclusion set",
   "c_rules": [
    {
     "lane": "lane3",
     "wording": "EXCLUDED_FROM_ACTIVATION_DEPTH = [INPUT_QUERY, DISMISS_OBSTRUCTION, AUTH_GATE, ENDPOINT_REACHED, ABSTAIN, scroll, passive wait/redirect]."
    },
    {
     "lane": "lane6",
     "wording": "classify_token: state_changing_activation False for DISMISS_OBSTRUCTION, AUTH_GATE, ENDPOINT_REACHED, ABSTAIN and (C-2) form-intent tokens."
    },
    {
     "lane": "A",
     "wording": "STEP1-006 OUT_activation_depth = {INPUT_QUERY, DISMISS_OBSTRUCTION ('forced_dismissal_count 로 별도 집계'), AUTH_GATE ('사용자 활성화가 아니라 마주친 상태다. 기준 ①에 걸린다'), ENDPOINT_REACHED ('종결 표지이지 행위가 아니다'), ABSTAIN ('판정 유보')}."
    }
   ],
   "ssotv3_clause": "04 §5; 03 §6; A T-A-V3-STEP1-006",
   "lane_sources": [
    "A",
    "lane3",
    "lane6"
   ],
   "status": "CONSISTENT",
   "divergent_case": null,
   "notes": "Both lanes' OUT sets equal A's OUT set exactly; the three CONDITIONAL tokens are handled in P-04.",
   "authority_override": "CONFIRMED_BY_A_STEP1-006",
   "follow_up": null
  },
  {
   "id": "P-08",
   "variable": "flow_step_count",
   "c_rules": [
    {
     "lane": "lane3",
     "wording": "'tokens of task_flow_sequence minus pure terminal markers (ENDPOINT_REACHED/ABSTAIN); AUTH_GATE is counted (04 §4 \"auth encounter 포함\")'. 'Typing/submit counted. Scroll/dismiss never counted.'"
    },
    {
     "lane": "lane6",
     "wording": "C-4: 'task-intent (flow_step_count) = all canonical tokens except DISMISS_OBSTRUCTION, ENDPOINT_REACHED (state marker), ABSTAIN; AUTH_GATE counts (\"auth encounter 포함\")' — counted over task_flow_sequence."
    }
   ],
   "ssotv3_clause": "04 §4/§5 'flow_step_count: task-intent token 수. typing/submit/auth encounter 포함, scroll/passive 제외'",
   "lane_sources": [
    "lane3",
    "lane6"
   ],
   "status": "A_RULED",
   "divergent_case": null,
   "notes": "Equivalent (task_flow_sequence already excludes DISMISS_OBSTRUCTION). 04 §3 example → 3 under both; lane3 auth_gate_after_task_select → 2.",
   "authority_override": "CONFIRMED_BY_A_STEP1-012_GAP-03",
   "follow_up": null,
   "resolution_r2": "T-A-V3-STEP1-012 GAP-03 confirms lane3/lane6: flow_step_count excludes ENDPOINT_REACHED and ABSTAIN, includes AUTH_GATE (04 §5 names 'auth encounter' and nothing else; ENDPOINT_REACHED is a measurement marker, not task intent). Asymmetry with activation_depth (Δ9 excludes both) is by SSOT wording.",
   "lane_files_r2": "lane3 EXPECTATIONS.flow_step_count annotated; all 8 fixture values comply (walker re-derives)",
   "authority_ticket": "T-A-V3-STEP1-012"
  },
  {
   "id": "P-09",
   "variable": "menu_dependency: basis of the derivation (S0-visibility-and-required-reveal vs any reveal token before terminal)",
   "c_rules": [
    {
     "lane": "lane2",
     "wording": "menu_dependency_rule: '1 iff the entry control is not visible at S0 and becomes visible only after >=1 reveal activation (OPEN_GLOBAL_MENU / OPEN_LOCAL_MENU / EXPAND_ACCORDION). nav_container_depth = number of reveal activations required (04 §5).'"
    },
    {
     "lane": "lane3",
     "wording": "'true iff any REVEAL_SET token precedes the terminal token.'"
    },
    {
     "lane": "lane6",
     "wording": "derive: 'menu_dependency 04 §5 — 1 iff a reveal token (C-3) occurs before ENDPOINT_REACHED (or anywhere if endpoint never reached)'."
    }
   ],
   "ssotv3_clause": "04 §4 'menu_dependency: action_sequence에 OPEN/REVEAL 계열 token이 endpoint 이전에 존재하는지' (sequence-based wording); 04 §5",
   "lane_sources": [
    "lane2",
    "lane3",
    "lane6"
   ],
   "status": "A_RULED",
   "divergent_case": "SELECT_FUNCTION > OPEN_LOCAL_MENU > SELECT_DATE > SUBMIT_QUERY > ENDPOINT_REACHED (entry control visible at S0; a local menu is opened later inside the task form): lane2 rule → 0 (entry control did not need a reveal); lane3/lane6 → 1 (a reveal token precedes the terminal). Same split for a non-minimal recorded path that opens a menu although the function control was visible at S0 (04 §3 example with the function visible at S0: lane2 0, lane3/lane6 1).",
   "notes": "04 §4 wording is sequence-based, which favours lane3/lane6; lane2's rule is the S0-surface reading. Both are cited; no pick is made here. On all 14 lane2 fixtures and all 8 lane3 fixtures the three rules agree.",
   "authority_override": "A_RULED_STEP1-011",
   "follow_up": null,
   "resolution_r2": "A accepts C's reading (T-A-V3-STEP1-011 P-09): menu_dependency is sequence-based — 1 iff a reveal token precedes the endpoint (04 §4 and §5 agree); the divergent case is 1. A records the real tension with 10_GLOSSARY ('task control이 바로 보이지 않고') and resolves it by hierarchy (04 = T3 frozen definition > 10 = T5 prose); the glossary's concept is carried by nav_container_depth and s0_task_control_visible, not by menu_dependency.",
   "lane_files_r2": "lane3 EXPECTATIONS.menu_dependency (r2); lane2 meta.menu_dependency_rule annotated (all 14 lane2 fixture values identical under both readings)",
   "authority_ticket": "T-A-V3-STEP1-011"
  },
  {
   "id": "P-10",
   "variable": "menu_dependency: meaning of 'endpoint 전' when the endpoint is never reached",
   "c_rules": [
    {
     "lane": "lane3",
     "wording": "'…precedes the terminal token' (terminal = ENDPOINT_REACHED | AUTH_GATE | ABSTAIN)."
    },
    {
     "lane": "lane6",
     "wording": "C-10: '\"endpoint 전\" = before ENDPOINT_REACHED; if endpoint never reached, the whole observed path counts' (_before_terminal cuts only at ENDPOINT_REACHED)."
    }
   ],
   "ssotv3_clause": "04 §4/§5",
   "lane_sources": [
    "lane3",
    "lane6"
   ],
   "status": "CONSISTENT",
   "divergent_case": null,
   "notes": "Equivalent under the terminal-token-last invariant (P-19): a reveal token can never follow a terminal AUTH_GATE/ABSTAIN in a contract-valid sequence.",
   "authority_override": null,
   "follow_up": null
  },
  {
   "id": "P-11",
   "variable": "nav_container_depth: anchor ('task control 노출 전') and behaviour when SELECT_FUNCTION is absent",
   "c_rules": [
    {
     "lane": "lane2",
     "wording": "'nav_container_depth = number of reveal activations required' (to make the entry control visible)."
    },
    {
     "lane": "lane3",
     "wording": "'count of REVEAL_SET tokens that precede SELECT_FUNCTION. SELECT_CATEGORY that navigates to a category page is a page transition, not a container reveal, so it does not add depth (fixture 1: depth 1, not 2).' README: 'SWITCH_TAB left undecided.'"
    },
    {
     "lane": "lane6",
     "wording": "C-5: 'nav_container_depth anchor = first SELECT_FUNCTION or task-body token (form/submit/result/detail); reveals before it are counted; SELECT_CATEGORY is not an anchor; no anchor → count reveals before terminal, nav_anchor_found=False'. TASK_CONTROL_ANCHOR_TOKENS = {SELECT_FUNCTION, INPUT_QUERY, SELECT_ORIGIN, SELECT_DESTINATION, SELECT_DATE, SUBMIT_QUERY, SELECT_RESULT, OPEN_ITEM_DETAIL, OPEN_PLACE_DETAIL}."
    }
   ],
   "ssotv3_clause": "04 §4 'nav_container_depth: task control 노출 전 menu/drawer expansion 수'; 04 §5 'task control 노출 전 nested reveal 수'",
   "lane_sources": [
    "lane2",
    "lane3",
    "lane6"
   ],
   "status": "C_DECIDED",
   "divergent_case": "(a) OPEN_GLOBAL_MENU > INPUT_QUERY > SUBMIT_QUERY > ENDPOINT_REACHED (search box lives inside the global menu; no SELECT_FUNCTION token): lane6 → 1 (anchor = INPUT_QUERY); lane3 rule has no anchor → undefined. (b) OPEN_GLOBAL_MENU > AUTH_GATE: lane6 → 1 with nav_anchor_found=False; lane3 → undefined. (c) A non-minimal path that opens a menu not required to expose the control: lane2 → 0 ('required'), lane3/lane6 → 1.",
   "notes": "Agreement on the 04 §3 example (1), lane3 fixture 1 (1, not 2), lane1 F3 reference OPEN_GLOBAL_MENU > EXPAND_ACCORDION > SELECT_FUNCTION > ENDPOINT_REACHED (2).",
   "authority_override": "C_DECIDED_031138",
   "follow_up": null,
   "resolution_r2": "C decides (measurement-method choice, C's authority; C-DECISION_REQUEST-031138 c_decides_itself, A confirms in T-A-V3-STEP1-011 §C_self_decided_4_confirmed): nav_container_depth anchor = the first SELECT_FUNCTION or, when absent, the first task-body token (INPUT_QUERY / SELECT_CATEGORY / SELECT_ORIGIN / SELECT_DESTINATION / SELECT_DATE / SUBMIT_QUERY / SELECT_RESULT / OPEN_ITEM_DETAIL / OPEN_PLACE_DETAIL); reveals before the anchor are counted; no anchor → reveals before the terminal with nav_anchor_found=false (lane6 C-5 adopted). Divergent cases (a)/(b) → 1; (c) → 1 (sequence-based, consistent with P-09).",
   "lane_files_r2": "lane3 EXPECTATIONS.nav_container_depth + README §3 + walker nav_depth() (fixture 1 stays 1); lane2 wording unchanged (values coincide); lane6 C-5 already the rule",
   "authority_ticket": "C-DECISION_REQUEST-031138"
  },
  {
   "id": "P-12",
   "variable": "SELECT_CATEGORY is neither a reveal token nor a depth anchor",
   "c_rules": [
    {
     "lane": "lane3",
     "wording": "'a SELECT_CATEGORY that navigates to a category page is not a container reveal (fixture 1 → 1, not 2)'."
    },
    {
     "lane": "lane6",
     "wording": "C-3 reveal set excludes SELECT_CATEGORY; C-5 'SELECT_CATEGORY is not an anchor'."
    }
   ],
   "ssotv3_clause": "04 §2 (SELECT_CATEGORY '과업 관련 카테고리/서비스군을 선택한다'); 04 §5",
   "lane_sources": [
    "lane3",
    "lane6"
   ],
   "status": "CONSISTENT",
   "divergent_case": null,
   "notes": "But lane6 DOES treat SELECT_CATEGORY as task discovery for auth_gate_stage while lane3 does not — see P-13.",
   "authority_override": null,
   "follow_up": null
  },
  {
   "id": "P-13",
   "variable": "auth_gate_stage: what counts as task discovery / task select (BEFORE_TASK_DISCOVERY vs AFTER_TASK_SELECT boundary)",
   "c_rules": [
    {
     "lane": "lane3",
     "wording": "BEFORE_TASK_DISCOVERY: 'AUTH_GATE with no SELECT_FUNCTION before it: no task-specific control observable at any scroll state S0..Sn and no reveal container available without auth.' AFTER_TASK_SELECT: 'AUTH_GATE occurs at/after SELECT_FUNCTION and before any result-level token … and before any task results surface is rendered.'"
    },
    {
     "lane": "lane6",
     "wording": "C-6: 'BEFORE_TASK_DISCOVERY if no SELECT_FUNCTION/SELECT_CATEGORY precedes AUTH_GATE (a bare OPEN_GLOBAL_MENU is not discovery)'; TASK_SELECT_TOKENS = {SELECT_FUNCTION, SELECT_CATEGORY}; 'else AFTER_TASK_SELECT'."
    }
   ],
   "ssotv3_clause": "04 §4 auth_gate_stage vocabulary; 03 §7 'auth stage를 BEFORE_TASK_DISCOVERY / AFTER_TASK_SELECT / AT_ENDPOINT로 기록'",
   "lane_sources": [
    "lane3",
    "lane6"
   ],
   "status": "A_RULED",
   "divergent_case": "(a) OPEN_GLOBAL_MENU > SELECT_CATEGORY > AUTH_GATE: lane6 → AFTER_TASK_SELECT (SELECT_CATEGORY is a task-select token); lane3 → no category applies (not BEFORE, because a reveal container WAS available without auth; not AFTER, because no SELECT_FUNCTION occurred). (b) OPEN_GLOBAL_MENU > AUTH_GATE (menu opens, its contents demand login): lane6 → BEFORE_TASK_DISCOVERY; lane3 → undefined for the same reason.",
   "notes": "Agreement: bare AUTH_GATE (lane3 auth_gate_before_discovery) → BEFORE_TASK_DISCOVERY under both; 04 §3 example → AFTER_TASK_SELECT under both.",
   "authority_override": "A_RULED_STEP1-011",
   "follow_up": null,
   "resolution_r2": "A fixes one positional rule (T-A-V3-STEP1-011 P-13/P-14; 00 §6 / 03 §7 give vocabulary only, a definition gap A must fill — R13 UNDETERMINED is for evidence absence, not definition gaps). task_specific_token_set = {SELECT_CATEGORY, SELECT_FUNCTION, INPUT_QUERY, SELECT_ORIGIN, SELECT_DESTINATION, SELECT_DATE, SUBMIT_QUERY, SELECT_RESULT, OPEN_ITEM_DETAIL, OPEN_PLACE_DETAIL}; general navigation (OPEN_GLOBAL_MENU / OPEN_LOCAL_MENU / SWITCH_TAB / EXPAND_ACCORDION / DISMISS_OBSTRUCTION) is not task-specific. BEFORE_TASK_DISCOVERY = no task-specific token before AUTH_GATE (so OPEN_GLOBAL_MENU > AUTH_GATE → BEFORE); AFTER_TASK_SELECT = ≥1 task-specific token before the gate, endpoint contract unmet (OPEN_GLOBAL_MENU > SELECT_CATEGORY > AUTH_GATE → AFTER). 04 §3 example → AFTER_TASK_SELECT.",
   "lane_files_r2": "lane3 EXPECTATIONS.auth_gate_stage rewritten to the ticket text + walker derives the stage from the sequence and requires equality (8/8: login wall BEFORE, F1 select AFTER, F5 result AT_ENDPOINT, others NONE — no fixture value changed); lane6 C-6 TASK_SELECT_TOKENS must widen to the 10-token set (lane6 worker; not edited here)",
   "authority_ticket": "T-A-V3-STEP1-011"
  },
  {
   "id": "P-14",
   "variable": "auth_gate_stage: AT_ENDPOINT boundary",
   "c_rules": [
    {
     "lane": "lane3",
     "wording": "AT_ENDPOINT: 'AUTH_GATE occurs on a result-level activation after a task results surface was rendered without auth, OR the endpoint surface is rendered but the contract-required content (e.g. F5 time+price) is withheld behind auth.' AFTER_TASK_SELECT: '…before any result-level token (SELECT_RESULT/OPEN_ITEM_DETAIL/OPEN_PLACE_DETAIL) and before any task results surface is rendered. Position rule wins even if the gated action would have yielded the endpoint (e.g. F3 where the form itself is the endpoint).'"
    },
    {
     "lane": "lane6",
     "wording": "C-6: 'AT_ENDPOINT if AUTH_GATE immediately precedes ENDPOINT_REACHED or is terminal after a task-body token (query submitted / result chosen)'; TASK_BODY_TOKENS = FORM_INTENT_TOKENS ∪ {SUBMIT_QUERY, SELECT_RESULT, OPEN_ITEM_DETAIL, OPEN_PLACE_DETAIL}. Alternative terminal_auth_is_endpoint=True (any terminal AUTH_GATE = AT_ENDPOINT) reported as auth_gate_stage_alt_terminal_is_endpoint."
    }
   ],
   "ssotv3_clause": "04 §4; 03 §7; 04 §3 example (OPEN_GLOBAL_MENU > SELECT_FUNCTION > AUTH_GATE)",
   "lane_sources": [
    "lane3",
    "lane6"
   ],
   "status": "A_RULED",
   "divergent_case": "SELECT_FUNCTION > INPUT_QUERY > SUBMIT_QUERY > AUTH_GATE where the submit redirects straight to a login page (no results surface rendered): lane3 → AFTER_TASK_SELECT (no result-level token, no results surface); lane6 → AT_ENDPOINT (terminal AUTH_GATE after task-body tokens INPUT_QUERY/SUBMIT_QUERY). lane3 uses evidence beyond the sequence ('results surface rendered'); lane6 is sequence-only, so the same token string maps to two values.",
   "notes": "Agreement: 04 §3 example → AFTER_TASK_SELECT (lane6 alt: AT_ENDPOINT, reported only as alternative); … > SELECT_RESULT > AUTH_GATE (lane3 auth_gate_at_endpoint) → AT_ENDPOINT under both; F3 SELECT_FUNCTION > AUTH_GATE → AFTER_TASK_SELECT under both. Per A R6 Q8 every value here is field-qualified: action_token=AUTH_GATE vs endpoint_status=AUTH_GATE.",
   "authority_override": "A_RULED_STEP1-011",
   "follow_up": null,
   "resolution_r2": "Same rule (T-A-V3-STEP1-011 P-14): AT_ENDPOINT only when the endpoint surface was rendered WITHOUT auth and the gate hits on accessing its content; SELECT_FUNCTION > INPUT_QUERY > SUBMIT_QUERY > AUTH_GATE with no results surface rendered → AFTER_TASK_SELECT (C/lane3 reading). One rule yields both P-13 and P-14 answers — not case-by-case. lane6's sequence-only 'terminal after task-body token = AT_ENDPOINT' is superseded; the extra evidence (surface rendered) is required.",
   "lane_files_r2": "lane3 EXPECTATIONS/README (r2) incl. fixture flag endpoint_surface_rendered_without_auth on auth_gate_at_endpoint; lane6 C-6 AT_ENDPOINT rule to be replaced by the surface-rendered condition (lane6 worker; not edited here)",
   "authority_ticket": "T-A-V3-STEP1-011"
  },
  {
   "id": "P-15",
   "variable": "generic login presence never produces AUTH_GATE; auth_gate_stage=NONE without a gate on the frozen path",
   "c_rules": [
    {
     "lane": "lane1",
     "wording": "C-F4-DECOY-01: 'generic login 존재만으로 중단 금지, so endpoint_status must NOT be AUTH_GATE and auth_gate_stage must be NONE'; all four fixtures auth_gate_stage_allowed = [NONE]."
    },
    {
     "lane": "lane3",
     "wording": "generic_login_rule: 'presence of a login control/link anywhere on the page never produces AUTH_GATE; only a gate that the frozen task path enters counts (00 §6, 03 §7). Fixture 4 is the negative control.'"
    },
    {
     "lane": "lane4",
     "wording": "AUTH_ENTRY_ALLOWED_CONDITIONALLY: 'the login ENTRY link/button may be followed only on the task-specific path; the resulting form's password field and submit remain FORBIDDEN_CREDENTIAL_INPUT'; cross-fixture invariant 'run is NOT killed by presence of transaction/login controls'."
    }
   ],
   "ssotv3_clause": "03 §7 'generic login 버튼 존재만으로 중단 금지. task-specific path를 따라가다 auth가 불가피한 순간 AUTH_GATE terminal'; A T-A-V3-STEP1-007 R13 (NONE is affirmative)",
   "lane_sources": [
    "lane1",
    "lane3",
    "lane4"
   ],
   "status": "CONSISTENT",
   "divergent_case": null,
   "notes": "Unchanged by R13 for fixtures whose path was observed to the endpoint (lane1 F2/F3/F4, lane3 fixture 4): there NONE is evidence-backed. For lane1 C-F5-NOSURF-01 (no path observed) the expected auth_gate_stage NONE is superseded — see P-72.",
   "authority_override": null,
   "follow_up": null
  },
  {
   "id": "P-16",
   "variable": "action_token=AUTH_GATE is terminal (nothing follows it); credential input / login submit never happens",
   "c_rules": [
    {
     "lane": "lane3",
     "wording": "task_flow_sequence rule: 'terminal token (ENDPOINT_REACHED|AUTH_GATE) kept as last element'; credential_check 'all [data-c-forbidden] inputs empty'."
    },
    {
     "lane": "lane6",
     "wording": "violations: 'terminal token {t} at index {i} is not last (00 §6 AUTH_GATE terminal / 04 §2)'."
    },
    {
     "lane": "lane4",
     "wording": "finance/nonfinance login gates expected_terminal 'AUTH_GATE … observation only'; never_activate id/pw/submit."
    }
   ],
   "ssotv3_clause": "03 §7; 04 §2 AUTH_GATE definition",
   "lane_sources": [
    "lane3",
    "lane4",
    "lane6"
   ],
   "status": "CONSISTENT",
   "divergent_case": null,
   "notes": null,
   "authority_override": null,
   "follow_up": null
  },
  {
   "id": "P-17",
   "variable": "endpoint_status: resolution from the sequence vs allowed partitions per fixture",
   "c_rules": [
    {
     "lane": "lane1",
     "wording": "Per fixture endpoint_status_allowed / _forbidden partition the 04 §4 vocabulary; C-F5-NOSURF-01 allows [PUBLIC_WEB_UNOBSERVABLE, ABSTAIN] with informative sequence [ABSTAIN]."
    },
    {
     "lane": "lane3",
     "wording": "'REACHED iff the family endpoint contract is satisfied in the final state; AUTH_GATE iff final token is AUTH_GATE; nothing else exercised here.'"
    },
    {
     "lane": "lane6",
     "wording": "C-12 / endpoint_status_from_sequence: 'REACHED if ENDPOINT_REACHED present; else AUTH_GATE if AUTH_GATE present; else ABSTAIN if ABSTAIN present; else UNRESOLVED_FROM_SEQUENCE … C compares B's value against this and flags REACHED/AUTH_GATE/ABSTAIN mismatches only.'"
    }
   ],
   "ssotv3_clause": "04 §4 endpoint_status vocabulary; A R1 'endpoint_status 는 ABSTAIN 또는 PUBLIC_WEB_UNOBSERVABLE'; A R6 Q8 field qualification",
   "lane_sources": [
    "lane1",
    "lane3",
    "lane6"
   ],
   "status": "A_RULED",
   "divergent_case": "Runner output for C-F5-NOSURF-01 with task_flow_sequence = [ABSTAIN] and endpoint_status = PUBLIC_WEB_UNOBSERVABLE: lane1 → PASS (both statuses allowed); lane6 compare_with_mart_row → diff {B: PUBLIC_WEB_UNOBSERVABLE, C: ABSTAIN} because action_token=ABSTAIN resolves endpoint_status=ABSTAIN and only UNRESOLVED_FROM_SEQUENCE suppresses the comparison.",
   "notes": "Requires deciding whether action_token=ABSTAIN may co-occur with endpoint_status=PUBLIC_WEB_UNOBSERVABLE (A R1 names both as valid outcomes for a target that cannot perform the task). Field-qualified per A R6 Q8. A R11 (STEP1-007) adds terminal_reason (TASK_SURFACE_ABSENT vs NO_PUBLIC_MOBILE_WEB) and an endpoint_status × terminal_reason allowed-combination table that C verifies at GATE 1 — that table, once B publishes it, is where this row is closed; the endpoint_status enum itself is unchanged, so the lane1/lane6 split is still open today.",
   "authority_override": "A_RULED_STEP1-011",
   "follow_up": null,
   "resolution_r2": "A accepts C's layer separation (T-A-V3-STEP1-011 P-17): action_token=ABSTAIN and endpoint_status are different layers (R6 Q8 field qualification keeps them apart). A adds: endpoint_status=ABSTAIN ('we do not know' — multiple candidates / path undetermined) ≠ endpoint_status=PUBLIC_WEB_UNOBSERVABLE × terminal_reason=TASK_SURFACE_ABSENT ('we know the surface is absent'); mixing them over-counts absence (same logic as R13 NONE vs UNDETERMINED). The C-F5-NOSURF-01 case is therefore PUBLIC_WEB_UNOBSERVABLE × TASK_SURFACE_ABSENT with sequence [ABSTAIN]; lane6's mechanical ABSTAIN-token→ABSTAIN-status resolution must not be diffed against it. Closure against B's combination table remains pending.",
   "lane_files_r2": "lane3 EXPECTATIONS.endpoint_status annotated (r2); lane1 NOSURF allowed pair already admits it; lane6 compare_with_mart_row suppression rule (lane6 worker; not edited here)",
   "authority_ticket": "T-A-V3-STEP1-011"
  },
  {
   "id": "P-18",
   "variable": "task_flow_sequence = experienced_flow_sequence minus every DISMISS_OBSTRUCTION (order preserved)",
   "c_rules": [
    {
     "lane": "lane3",
     "wording": "'experienced_flow_sequence with every DISMISS_OBSTRUCTION removed; order preserved; terminal token … kept as last element'; comparison 'experienced − DISMISS == task'."
    },
    {
     "lane": "lane6",
     "wording": "sequence_consistent: 'experienced minus DISMISS_OBSTRUCTION must equal task_flow'; violation if task_flow contains DISMISS_OBSTRUCTION."
    }
   ],
   "ssotv3_clause": "04 §3",
   "lane_sources": [
    "lane3",
    "lane6"
   ],
   "status": "CONSISTENT",
   "divergent_case": null,
   "notes": null,
   "authority_override": null,
   "follow_up": null
  },
  {
   "id": "P-19",
   "variable": "terminal token must be last; DISMISS_OBSTRUCTION never in task_flow_sequence",
   "c_rules": [
    {
     "lane": "lane3",
     "wording": "Fixture 2's first triple '(LANDING_MODAL, DISMISS_OBSTRUCTION, LANDING) must be recorded but must be absent from task_flow_sequence'."
    },
    {
     "lane": "lane6",
     "wording": "violations list both conditions; derive 'Never raises on contract violations — it flags them'."
    }
   ],
   "ssotv3_clause": "04 §3; 04 §2",
   "lane_sources": [
    "lane3",
    "lane6"
   ],
   "status": "CONSISTENT",
   "divergent_case": null,
   "notes": null,
   "authority_override": null,
   "follow_up": null
  },
  {
   "id": "P-20",
   "variable": "forced_dismissal_count",
   "c_rules": [
    {
     "lane": "lane3",
     "wording": "'count of DISMISS_OBSTRUCTION in experienced_flow_sequence; each must be paired with a fact_task_obstruction row with dismiss_required_for_task=true.'"
    },
    {
     "lane": "lane6",
     "wording": "'forced_dismissal_count 04 §4 — DISMISS_OBSTRUCTION count in experienced_flow (04 §3)'; still counted for ABSTAIN sequences (C-9)."
    },
    {
     "lane": "lane7",
     "wording": "unit=target: 'dismiss_required_for_task … equals forced_dismissal_count > 0 for P=all'."
    }
   ],
   "ssotv3_clause": "04 §4 'task 진행에 실제 필요했던 dismissal 수'; 03 §9",
   "lane_sources": [
    "lane3",
    "lane6",
    "lane7"
   ],
   "status": "CONSISTENT",
   "divergent_case": null,
   "notes": null,
   "authority_override": null,
   "follow_up": null
  },
  {
   "id": "P-21",
   "variable": "first_visible_scroll_state",
   "c_rules": [
    {
     "lane": "lane3",
     "wording": "'first scroll-only state Sk (scroll step chosen by runner within [120,844] px) whose viewport intersects the SELECT_FUNCTION control; \"NONE\" when no scroll state exposes it (menu-hidden or login wall).' README: 'refers to the SELECT_FUNCTION control only'."
    }
   ],
   "ssotv3_clause": "04 §4 'task 진입 control이 최초 관측된 scroll state. scroll은 activation depth에 포함하지 않음'; 03 §3",
   "lane_sources": [
    "lane3"
   ],
   "status": "A_RULED",
   "divergent_case": null,
   "notes": "Single definer. lane2 reports s0 only (all its hidden-control fixtures are menu-hidden → consistent with NONE).",
   "authority_override": "SUPERSEDED_BY_A_STEP1-012_GAP-02",
   "follow_up": null,
   "resolution_r2": "T-A-V3-STEP1-012 GAP-02 supersedes lane3's 'NONE when scroll never exposes it': first_visible_scroll_state = the scroll state at which the control was FIRST observed; a control exposed by a reveal takes the scroll state where the reveal happened (usually S0); null ONLY when the control was never observed, with endpoint_status × terminal_reason carrying the reason. A also elevates the coverage gap: no lane3 fixture yields a scroll-only S1 except seq_typing_and_scroll_not_depth, so 03 §3 scroll capture is verified by one fixture only — GATE 1 must verify 03 §3 or list it under 'not verified'.",
   "lane_files_r2": "lane3 EXPECTATIONS: fixtures 1–3 NONE → S0, login wall NONE → null; walker records the scroll state of the first step after which the control is in the viewport; README §7 (8/8 PASS)",
   "authority_ticket": "T-A-V3-STEP1-012"
  },
  {
   "id": "P-22",
   "variable": "s0_task_control_visible",
   "c_rules": [
    {
     "lane": "lane3",
     "wording": "'true iff the SELECT_FUNCTION control's bbox intersects the S0 viewport and it is hittable. A hamburger that leads to it does not count.'"
    },
    {
     "lane": "lane2",
     "wording": "No explicit rule; expected values follow rendering at S0 (visible_label_text_rule: 'Not rendered (display:none / visibility:hidden / off-viewport) => \"\"'); hidden-until-reveal fixtures → false."
    }
   ],
   "ssotv3_clause": "04 §4 '최초 viewport에서 사전지정 task 진입 control이 직접 보이는가'",
   "lane_sources": [
    "lane2",
    "lane3"
   ],
   "status": "A_RULED",
   "divergent_case": null,
   "notes": "Values agree on every fixture of both lanes. Untested edge: a control rendered inside the S0 viewport but fully covered by an overlay (lane3 'hittable' → false; lane2 has no such fixture and no wording). Flagged as a gap, not a contradiction.",
   "authority_override": "CONFIRMED_BY_A_STEP1-012_GAP-05",
   "follow_up": null,
   "resolution_r2": "T-A-V3-STEP1-012 GAP-05: s0_task_control_visible = bbox intersects the S0 viewport AND hit-testable at S0; NO occlusion threshold — the two variables are independent (90 % occluded but a hit-testable corner → visible; 0 % occluded but off-viewport → not visible). lane3's wording already said 'hittable'; its walker used occlusion < 0.5, which is a threshold and is replaced by a centre hit-test.",
   "lane_files_r2": "lane3 EXPECTATIONS wording + walker hitCentre() (no value changed); lane2 measure_geometry.py hit-tests the centre for s0_task_control_visible (no value changed)",
   "authority_ticket": "T-A-V3-STEP1-012"
  },
  {
   "id": "P-23",
   "variable": "task_control_occlusion: measurement method (hit-test vs geometry) and measured control",
   "c_rules": [
    {
     "lane": "lane3",
     "wording": "'fraction of a uniform 9x9 sample grid inside the next-path-control bbox whose elementFromPoint is neither the control nor its descendant (hit-test); cross-checked with bbox-intersection against [data-c-overlay] elements. Measured against the path-entry control of the CURRENT state (hamburger at S0 when the task function is behind a menu).' README: 'Undefined (null) when the control is outside the viewport.'"
    },
    {
     "lane": "lane7",
     "wording": "'task_control_occlusion = area(T ∩ container rect ∩ V) / area(T ∩ V), rounded to 3 dp (04 §4)' where T = 'the frozen task-entry control (03 §4)'; unit=target aux = MAX over population, 0.0 if empty."
    }
   ],
   "ssotv3_clause": "04 §4 'task-entry control bbox와 blocking obstruction의 실제 겹침 비율'; 03 §9 'geometry overlap만으로 modal 의미를 확정하지 않는다'; 02 §5 task_control_occlusion — primary",
   "lane_sources": [
    "lane3",
    "lane7"
   ],
   "status": "C_DECIDED",
   "divergent_case": "lane3 fixture seq_with_forced_dismissal at S0: lane3 measures the hamburger (path control of the current state) → 1.00 (hit) / 1.00 (geo); lane7 measures T = the SELECT_FUNCTION control, which is inside the closed drawer, so area(T ∩ V) = 0 → undefined. Also any overlay with pointer-events:none: lane3 hit-test → 0, lane7 geometry → > 0.",
   "notes": "Within the six lanes only lane3 defines this (CONSISTENT among them). lane7 is outside the brief but is a C pre-registration in the same namespace; the split (hit-test primary vs geometric primary; current-state path control vs frozen task-entry control) must be reconciled before either is used as PRIMARY. Not decided here.",
   "authority_override": "C_DECIDED_031138",
   "follow_up": null,
   "resolution_r2": "C decides (C-DECISION_REQUEST-031138): PRIMARY task_control_occlusion = fraction of a 9×9 `elementFromPoint` grid on the path control of the CURRENT state whose topmost element is not the control or a descendant (lane3 method, sampling (i+0.5)/9, out-of-viewport points dropped, null when the control is outside the viewport); the geometric T∩V overlap on the frozen entry control is `occlusion_geom_crosscheck` only. Divergent cases resolve as lane3: seq_with_forced_dismissal S0 → 1.0 on the hamburger; pointer-events:none overlay → 0.",
   "lane_files_r2": "lane7 DISMISS_DEFINITION_C.md v1.2 §2/§4/§5/§8 A8; impl_a.py (probe `path_control.hit_grid`) / impl_b.py (`[data-c-path-control]`, static stacking hit-test); probe_like/*.json gained `path_control`; fixture f06 added; converge_check.py PASS 31/31 (f04: hit 0.556 vs geom 0.583 on the same fixture); validate_probe_like_playwright.py checks the grids against real elementFromPoint (39/39)",
   "authority_ticket": "C-DECISION_REQUEST-031138"
  },
  {
   "id": "P-24",
   "variable": "dismiss_required_for_task: blocking proof",
   "c_rules": [
    {
     "lane": "lane3",
     "wording": "'true iff the next frozen-path control is not hittable (hit-test at its center lands on the interrupt) until an allowed close control is activated. Geometric overlap alone is insufficient; a blocking_proof click must fail.' blocking_proof = click the path control's centre and expect the state unchanged while the interrupt is present."
    },
    {
     "lane": "lane7",
     "wording": "'dismiss_required_for_task = task_control_occlusion > 0 ∨ container has aria-modal=\"true\" (03 §9: geometry alone does not decide modal meaning; modal semantics block AT even without overlap)'."
    }
   ],
   "ssotv3_clause": "03 §9 'primary obstruction은 task-specific으로 판정 … geometry overlap만으로 modal 의미를 확정하지 않는다'; 02 §5",
   "lane_sources": [
    "lane3",
    "lane7"
   ],
   "status": "C_DECIDED",
   "divergent_case": "(a) A role=dialog aria-modal=true container that neither overlaps nor intercepts pointer events on the path control: lane7 → true; lane3 → false (blocking-proof click succeeds). (b) A backdrop with occlusion > 0 but pointer-events:none: lane7 → true (geometry > 0); lane3 → false. (c) lane3's proof requires an activation on the task control while the interrupt is present; lane7 requires no activation.",
   "notes": "Within the six lanes only lane3 defines it. lane7 cross-reference outside the brief. Both cite 03 §9 for opposite operationalisations; not decided here.",
   "authority_override": "C_DECIDED_031138",
   "follow_up": null,
   "resolution_r2": "C decides (C-DECISION_REQUEST-031138; A credits it in T-A-V3-STEP1-011 as the exact application of presence ≠ operative): PRIMARY dismiss_required_for_task = the blocking proof — a click at the path control's centre while the interrupt is present leaves data-c-state unchanged (lane3); `aria-modal` and occlusion>0 / geometric overlap>0 are recorded as `dismiss_required_signal` and are never the verdict. Divergent cases: (a) aria-modal dialog that does not intercept → false (signal ARIA_MODAL); (b) pointer-events:none backdrop → false (signal GEOM_OVERLAP_GT0); (c) the proof requires the activation.",
   "lane_files_r2": "lane7 DISMISS_DEFINITION_C.md v1.2 §4 A9; impl_a (probe `blocking_proof`) / impl_b (static centre prediction); f06 exercises (a) and (b); negative control catches a probe that reports aria-modal as the proof; Playwright validator executes the 8 blocking proofs by real clicks",
   "authority_ticket": "C-DECISION_REQUEST-031138"
  },
  {
   "id": "P-25",
   "variable": "overlay_coverage is secondary; non-zero coverage with occlusion 0 is not an obstruction",
   "c_rules": [
    {
     "lane": "lane3",
     "wording": "'secondary only: overlay bbox area / viewport area. Non-zero coverage with occlusion 0 is NOT an obstruction (fixture 3).'"
    }
   ],
   "ssotv3_clause": "02 §5 'overlay_coverage — 보조 설명값 … max_overlay_coverage만으로 modal obstruction을 대표하지 않는다'",
   "lane_sources": [
    "lane3"
   ],
   "status": "A_RULED",
   "divergent_case": null,
   "notes": null,
   "authority_override": "CONFIRMED_BY_A_STEP1-012_GAP-04",
   "follow_up": null,
   "resolution_r2": "T-A-V3-STEP1-012 GAP-04: overlay_coverage / task_control_occlusion are null when not observed and 0.0 only when observed-and-unoccluded; a row never mixes conventions (D-V3-FINDING-002 precedent). lane3 fixtures 6 and 8 already carry null for the unobserved S0 path control.",
   "lane_files_r2": "lane3 EXPECTATIONS.task_control_occlusion annotated; lane7 §4/§5 null convention line",
   "authority_ticket": "T-A-V3-STEP1-012"
  },
  {
   "id": "P-26",
   "variable": "visible_label_text",
   "c_rules": [
    {
     "lane": "lane2",
     "wording": "'Rendered text a sighted user sees on the control at that state: innerText of a rendered (checkVisibility) control, PLUS CSS generated ::before/::after content (provenance PSEUDO_ELEMENT), PLUS input[type=submit|button|reset] value (provenance INPUT_VALUE). alt/title/aria-* are NOT visible text. Not rendered (display:none / visibility:hidden / off-viewport) => \"\".'"
    }
   ],
   "ssotv3_clause": "04 §4 'visible_label_text: 사람 화면에 실제 렌더된 task control 문구'; 04 §7",
   "lane_sources": [
    "lane2"
   ],
   "status": "CONSISTENT",
   "divergent_case": null,
   "notes": null,
   "authority_override": null,
   "follow_up": null
  },
  {
   "id": "P-27",
   "variable": "accessible_name is the browser AX computation (two engines must agree; never synthesised or copied from CSS/innerText)",
   "c_rules": [
    {
     "lane": "lane2",
     "wording": "measure via '(1) CDP Accessibility.getPartialAXTree (name + name sources, primary), (2) Playwright locator.aria_snapshot() (second accname engine, must agree), (3) DOM fallbacks … recorded only'; pseudo fixture: 'the runner must report what the AX tree actually says, never synthesize'; unnamed fixture: 'Any runner that fills accessible_name from class names, href, or nearby text fails'."
    }
   ],
   "ssotv3_clause": "04 §4 'accessible_name: 브라우저 accessibility tree가 계산한 이름'; 04 §7",
   "lane_sources": [
    "lane2"
   ],
   "status": "CONSISTENT",
   "divergent_case": null,
   "notes": null,
   "authority_override": null,
   "follow_up": null
  },
  {
   "id": "P-28",
   "variable": "accessible_name_source precedence",
   "c_rules": [
    {
     "lane": "lane2",
     "wording": "accessible_name_source_rule: 'ARIA_LABELLEDBY if aria-labelledby resolves to non-empty text (hidden referents included per accname 2B)'; 'ARIA_LABEL if aria-label non-empty'; 'LABEL if name comes from an associated <label>'; 'for name-from-content: VISIBLE_TEXT if DOM innerText or CSS ::before/::after generated text supplies the name (pseudo case flagged provenance=PSEUDO_ELEMENT); ALT if the content name is supplied by a descendant <img alt>/<svg><title>; MIXED if from several'; 'TITLE if title attribute is the only name source; VALUE if <input type=submit|button|reset value>'; 'NONE if computed name is empty'."
    }
   ],
   "ssotv3_clause": "04 §4 accessible_name_source vocabulary; 04 §7",
   "lane_sources": [
    "lane2"
   ],
   "status": "CONSISTENT",
   "divergent_case": null,
   "notes": "Single definer among the six lanes. Cross-reference: lane7's dismiss-candidate naming string is ordered 'aria-label → aria-labelledby → subtree text … → title → value', i.e. aria-label before aria-labelledby — the opposite of accname 2B/2C and of lane2. That ordering only feeds lane7's lexicon match, not accessible_name_source, so it is recorded as a note, not a contradiction of this variable.",
   "authority_override": null,
   "follow_up": null
  },
  {
   "id": "P-29",
   "variable": "label_relation: normalisation, MATCH exactness, substring rule, no embeddings",
   "c_rules": [
    {
     "lane": "lane2",
     "wording": "text_normalization 'NFC + collapse whitespace + trim (04 §5)'; label_relation_rule: NONE 'visible==\"\" and ax==\"\"', AX_ONLY 'visible==\"\" and ax!=\"\"', VISIBLE_ONLY 'visible!=\"\" and ax==\"\"', MATCH 'normalized visible == normalized ax', SEMANTIC_EQUIV 'pair present in synonym_map_fixed (no embedding auto-merge, 04 §5)', DIFFERENT 'otherwise (substring/containment is NOT equivalence)'."
    },
    {
     "lane": "lane6",
     "wording": "normalize_label 'NFC + whitespace collapse/strip (04 §5). casefold is OFF by default (C choice: 04 §5 says \"exact\")'; label_relation order NONE → VISIBLE_ONLY → AX_ONLY → MATCH → SEMANTIC_EQUIV (both map to the same canonical key) → DIFFERENT; 'Embedding similarity is never used'. C-8: MATCH case-strict; synonym lookup casefold-insensitive."
    }
   ],
   "ssotv3_clause": "04 §5 'label_relation: Unicode normalize + whitespace normalize 후 exact; 사전 고정 synonym map으로 semantic-equivalent를 별도 표시. embedding similarity만으로 자동 merge 금지'",
   "lane_sources": [
    "lane2",
    "lane6"
   ],
   "status": "CONSISTENT",
   "divergent_case": null,
   "notes": "Same values on every lane2 fixture (조회 ⊂ 운송장 배송조회 → DIFFERENT under both). Case handling: lane2 silent (Korean-only fixtures), lane6 case-strict — no Korean case exists, and for Latin labels both yield DIFFERENT for 'Login' vs 'login' unless the map says otherwise.",
   "authority_override": null,
   "follow_up": null
  },
  {
   "id": "P-30",
   "variable": "frozen synonym map: shape and identity entries",
   "c_rules": [
    {
     "lane": "lane2",
     "wording": "synonym_map_fixed = {\"배송조회\": [\"배송 조회\", \"택배조회\", \"택배 조회\", \"배송추적\"]} (canonical → forms)."
    },
    {
     "lane": "lane6",
     "wording": "'synonym map is an explicit {form: canonical_key} dict'; SEMANTIC_EQUIV only if smap.get(v) is not None and equals smap.get(a) — BOTH strings must be keys."
    }
   ],
   "ssotv3_clause": "04 §5 '사전 고정 synonym map'",
   "lane_sources": [
    "lane2",
    "lane6"
   ],
   "status": "C_DECIDED",
   "divergent_case": "visible='배송조회', accessible='택배조회': lane2 → SEMANTIC_EQUIV (pair present). lane6 → SEMANTIC_EQUIV only if the frozen map, once converted to form→key, contains the identity entry '배송조회'→'배송조회'; a naive conversion of lane2's map (forms only) leaves '배송조회' unmapped → DIFFERENT.",
   "notes": "Mechanical, but it changes a value. One frozen map file with explicit identity entries (or a documented conversion) must be fixed before any real row is compared. Note '배송 조회' vs '배송조회' differ only by an internal space, which whitespace COLLAPSE does not remove — it is a genuine synonym entry, not a normalisation artefact.",
   "authority_override": "C_DECIDED_031138",
   "follow_up": null,
   "resolution_r2": "C decides (C-DECISION_REQUEST-031138): the frozen synonym map shape is canonical → [forms] with identity implicit (lane2's `synonym_map_fixed` as it stands); lane6's lookup becomes bidirectional (a string matches if it is the canonical key or any listed form; both sides in the same group ⇒ SEMANTIC_EQUIV). Divergent case → SEMANTIC_EQUIV.",
   "lane_files_r2": "lane2 README ambiguity 7 + meta.synonym_map_shape sentence (expectations unchanged); lane6 lookup (lane6 worker; not edited here)",
   "authority_ticket": "C-DECISION_REQUEST-031138"
  },
  {
   "id": "P-31",
   "variable": "pseudo-element (::before/::after) label handling",
   "c_rules": [
    {
     "lane": "lane2",
     "wording": "Two accepted encodings: '(a) visible_label_text=\"배송조회\" + provenance=PSEUDO_ELEMENT, or (b) visible_label_text=\"\" (DOM) + separate rendered_pseudo_text=\"배송조회\". A bare visible_label_text=\"\" with no provenance field FAILS'; accessible_name expected from the AX tree ('배송조회' in Chromium, name source contents), 'never copied from the CSS'."
    }
   ],
   "ssotv3_clause": "04 §7 'pseudo-element text가 의미있는 label이면 rendered evidence로 별도 provenance를 남긴다'",
   "lane_sources": [
    "lane2"
   ],
   "status": "CONSISTENT",
   "divergent_case": null,
   "notes": null,
   "authority_override": null,
   "follow_up": null
  },
  {
   "id": "P-32",
   "variable": "input[type=submit|button|reset] value: visible text (provenance INPUT_VALUE) and accessible_name_source=VALUE",
   "c_rules": [
    {
     "lane": "lane2",
     "wording": "'rendered caption ⇒ visible text (provenance INPUT_VALUE) and accessible_name_source=VALUE; innerText of an <input> is empty, so a naive reader would mis-report'."
    }
   ],
   "ssotv3_clause": "04 §4 (VALUE in accessible_name_source vocabulary); 04 §7",
   "lane_sources": [
    "lane2"
   ],
   "status": "CONSISTENT",
   "divergent_case": null,
   "notes": null,
   "authority_override": null,
   "follow_up": null
  },
  {
   "id": "P-33",
   "variable": "HIDDEN_UNTIL_REVEAL: S0 row encoding of a not-yet-rendered control",
   "c_rules": [
    {
     "lane": "lane2",
     "wording": "'control not rendered ⇒ not in the AX tree ⇒ C stores \"\"/NONE/NONE (visible_label_text / accessible_name_source / label_relation) and modality HIDDEN_UNTIL_REVEAL; the control's own modality/relation is reported on the post-reveal state. A runner storing null is equivalent; storing the post-reveal name in the S0 row is not.'"
    }
   ],
   "ssotv3_clause": "04 §4 entry_label_modality HIDDEN_UNTIL_REVEAL; 02 §3 (one row per scroll state)",
   "lane_sources": [
    "lane2"
   ],
   "status": "A_RULED",
   "divergent_case": null,
   "notes": "Cross-reference: lane7 encodes an unexposed node's name as the sentinel NAME_ABSENT (REFERENCE_DEFINITIONS.md), lane2 as \"\"; both mean 'not exposed'. An adapter must treat \"\", null and NAME_ABSENT as one class for S0 comparison.",
   "authority_override": "SUPERSEDED_BY_A_STEP1-012_GAP-04",
   "follow_up": null,
   "resolution_r2": "T-A-V3-STEP1-012 GAP-04 supersedes lane2's S0 encoding of a not-yet-rendered control (''/NONE/NONE): unobserved categorical/text fields are the explicit NOT_OBSERVED (never ''), unobserved numeric geometry is null (never 0); one row never mixes conventions. An observed-but-empty AX name (label_icon_only_unnamed) stays '' with source NONE — an observed absence. lane7's NAME_ABSENT for an existing-but-unexposed control is an explicit sentinel and complies.",
   "lane_files_r2": "lane2 EXPECTATIONS (7 reveal fixtures: visible_label_text / accessible_name / accessible_name_source / label_relation → NOT_OBSERVED, s0_entry_x/y_norm → null) + meta.null_convention + measure_geometry.py (14/14 PASS)",
   "authority_ticket": "T-A-V3-STEP1-012"
  },
  {
   "id": "P-34",
   "variable": "entry_label_modality and entry_control_type classification",
   "c_rules": [
    {
     "lane": "lane2",
     "wording": "EXPLICIT_TEXT / ICON_TEXT / ICON_ONLY_AX_NAMED / ICON_ONLY_UNNAMED / HIDDEN_UNTIL_REVEAL derived from (rendered text present?, icon present?, AX name present?, rendered at S0?); ICON_ONLY_UNNAMED 'Must NOT be classified ICON_ONLY_AX_NAMED'; control type from element/role (TEXT_BUTTON, TEXT_LINK, ICON_TEXT, ICON_ONLY, HAMBURGER …)."
    }
   ],
   "ssotv3_clause": "04 §4 entry_label_modality / entry_control_type vocabularies",
   "lane_sources": [
    "lane2"
   ],
   "status": "CONSISTENT",
   "divergent_case": null,
   "notes": null,
   "authority_override": null,
   "follow_up": null
  },
  {
   "id": "P-35",
   "variable": "nav_container_type and reveal_direction derive from geometry / state change; naming never decides",
   "c_rules": [
    {
     "lane": "lane2",
     "wording": "reveal_direction_rule_C: motion 'delta of the entry control bbox center between closed and opened state … |dx|>=|dy| and |dx|>4 => LEFT if dx>0 … else RIGHT. |dy|>|dx| and |dy|>4 => TOP if dy>0 else BOTTOM'; no motion + rendered before → 'container fixed/absolute => CENTER; container in-flow => INLINE'; not rendered before → 'in-flow => INLINE; fixed/absolute covering >=90% viewport => CENTER; else edge-anchoring'; precedence 'Geometry wins. class/id/data-*/aria-label naming NEVER decides reveal_direction'."
    },
    {
     "lane": "lane3",
     "wording": "Fixture expectations nav_container_type=LEFT_DRAWER / reveal_direction=LEFT for the drawer fixtures (values only, no rule stated)."
    },
    {
     "lane": "A",
     "wording": "STEP1-006 T_B_FC_013_confirmed: 'OPEN_RIGHT_DRAWER 는 없다'; '오른쪽 drawer 를 여는 행위는 OPEN_GLOBAL_MENU 또는 OPEN_LOCAL_MENU 토큰 + nav_container_type=RIGHT_DRAWER + reveal_direction=RIGHT 로 표현한다. 방향은 토큰이 아니라 별도 변수다'; '상위 지시와 SSOT 가 다르면 SSOT 를 따른다'."
    }
   ],
   "ssotv3_clause": "04 §4 nav_container_type / reveal_direction (Flow/Geometry layer); 03 §11 'menu/drawer reveal + direction'; A T-A-V3-STEP1-006",
   "lane_sources": [
    "A",
    "lane2",
    "lane3"
   ],
   "status": "A_RULED",
   "divergent_case": null,
   "notes": "lane3 states values consistent with lane2's rule; no second rule to conflict. Both lanes already keep direction out of the token (lane2 drawer fixtures use OPEN_GLOBAL_MENU + nav_container_type + reveal_direction), matching A.",
   "authority_override": "EXTENDED_BY_A_STEP1-012_GAP-06",
   "follow_up": null,
   "resolution_r2": "T-A-V3-STEP1-012 GAP-06 extends: nav_container_type = the INNERMOST container directly holding the entry control (hamburger > accordion → INLINE_EXPAND, depth 2); new field nav_container_chain = outer→inner array (len == nav_container_depth; [] when NONE). Geometry-not-naming derivation (STEP1-006) unchanged.",
   "lane_files_r2": "lane2 EXPECTATIONS: nav_container_chain added to all 14 fixtures (single-level: [type] or []) + meta.nav_container_chain_rule; measure_geometry.py checks it",
   "authority_ticket": "T-A-V3-STEP1-012"
  },
  {
   "id": "P-36",
   "variable": "entry_zone band thresholds",
   "c_rules": [
    {
     "lane": "lane2",
     "wording": "zone_rule_C_provisional: 'TOP_LEFT|TOP_CENTER|TOP_RIGHT: y_norm < 0.12, split by x_norm < 1/3, <= 2/3, > 2/3'; 'BOTTOM: y_norm > 0.88'; 'MID: otherwise'; 'thresholds are not given in SSOT. C declares these provisional thresholds'."
    },
    {
     "lane": "lane6",
     "wording": "C-7: 'y < 0.15 → TOP band split by x thirds (x < 1/3 LEFT, 1/3 ≤ x ≤ 2/3 CENTER, x > 2/3 RIGHT); y ≥ 0.85 → BOTTOM; else MID' (ZONE_TOP_Y=0.15, ZONE_BOTTOM_Y=0.85)."
    },
    {
     "lane": "A",
     "wording": "R7 thresholds: 'y < 1/3 → TOP · 1/3 ≤ y < 2/3 → MID · y ≥ 2/3 → BOTTOM'; 'x_within_TOP: x < 1/3 → TOP_LEFT · 1/3 ≤ x < 2/3 → TOP_CENTER · x ≥ 2/3 → TOP_RIGHT'; 'MID·BOTTOM: x 삼등분을 적용하지 않는다'; boundary_rule '[a, b) … 정확히 1/3 인 점은 TOP 이자 TOP_CENTER 다'; coordinate_basis 'control bbox 중심을 viewport(390×844 CSS px)로 정규화 … 그 state 의 viewport 기준'."
    }
   ],
   "ssotv3_clause": "04 §6 (no cutoffs; 'Normalized center (x,y)를 보존하고 zone은 요약값으로만 쓴다'); A T-A-V3-STEP1-003 R7 (D-V3-FINDING-007)",
   "lane_sources": [
    "A",
    "lane2",
    "lane6"
   ],
   "status": "CONSISTENT",
   "divergent_case": "Pre-ruling the two lanes differed (y=0.13 → lane2 MID, lane6 TOP_*; y=0.86 → lane2 MID, lane6 BOTTOM; lane6 x=2/3 → CENTER, lane2 → CENTER, R7 → TOP_RIGHT). Moot: both provisional sets are superseded by R7.",
   "notes": "Status CONSISTENT because A has ruled; C's provisional numbers are withdrawn, not reconciled. Raw x/y_norm remain the primary comparison (±0.02, P-38).",
   "authority_override": "SUPERSEDED_BY_A_R7",
   "follow_up": "lane2 expected entry_zone values must be re-derived under R7 (from out/measure_result.json raw x/y): label_explicit_text (0.20,0.26) MID→TOP_LEFT; label_aria_labelledby_differs (0.17,0.26) MID→TOP_LEFT; label_pseudo_element_text (0.20,0.26) MID→TOP_LEFT; TOP_CENTER/TOP_RIGHT rows unchanged. lane6 ZONE_* constants → terciles with [a,b) boundaries (lane6 worker in progress; this document cites the ticket, not the lane file, as authority).",
   "fact_correction_r2": "**FACT_CORRECTION T-A-V3-FC-001** (A, on D-V3-FACT_CORRECTION-001): the R7 sentence '정확히 1/3 인 점은 TOP 이자 TOP_CENTER' was wrong on the y axis; the band table is canonical with [a,b) on both axes — y = 1/3 → MID, y = 2/3 → BOTTOM; inside TOP x = 1/3 → TOP_CENTER, x = 2/3 → TOP_RIGHT. No cut-off value changes. lane2 fixture centres are not on a boundary (y boundaries 281.33 / 562.67 px; nearest centre 522 px → MID).",
   "lane_files_r2": "r2 applied in lane2 (`EXPECTATIONS.json`, `measure_geometry.py`, 14/14 PASS): zones re-derived from r1 raw x/y under R7 — label_explicit_text / label_aria_labelledby_differs / label_pseudo_element_text MID→TOP_LEFT; label_icon_text / label_icon_only_ax_named / label_alt_title_value TOP_*→FLOATING (they sit inside a `position:fixed` header and R7 FLOATING has no size cap — this corrects the r1 follow-up sentence 'TOP_CENTER/TOP_RIGHT rows unchanged', which ignored the header); inline_expand MID→DRAWER; drawers/sheets/dropdown DRAWER unchanged; label_icon_only_unnamed FLOATING unchanged. C's one reading of R7 — FLOATING tested self-OR-ancestor — is declared in lane2 meta with the self-only alternative recorded per fixture as `entry_zone_band_R7`."
  },
  {
   "id": "P-37",
   "variable": "entry_zone structural overrides: FLOATING / DRAWER definitions and precedence",
   "c_rules": [
    {
     "lane": "lane2",
     "wording": "'DRAWER: control lies inside an opened reveal container whose computed position is fixed/absolute (any nav_container_type except INLINE_EXPAND)'; 'FLOATING: nearest fixed-positioned box (self or ancestor) has width < 0.9*vw AND height < 0.9*vh'; inline_expand fixture zone = MID."
    },
    {
     "lane": "lane6",
     "wording": "C-7 'Precedence DRAWER > FLOATING > band'; compare_with_mart_row: is_drawer iff nav_container_type ∈ {LEFT_DRAWER, RIGHT_DRAWER, HAMBURGER, MODAL_MENU, BOTTOM_SHEET} and row.entry_in_drawer; is_floating = row.entry_is_floating (external flag, undefined by lane6)."
    },
    {
     "lane": "A",
     "wording": "R7 structural_overrides: 'FLOATING 과 DRAWER 는 기하보다 우선한다'; 'FLOATING: computed position 이 fixed 또는 sticky 이고 일반 흐름에서 벗어나 viewport 에 고정된 경우'; 'DRAWER: control 이 reveal 을 요구하는 nav_container 안에 있는 경우 (menu_dependency=1 을 만든 그 container)'; 'both: DRAWER 가 우선한다'; 'record_anyway: entry_x_norm/entry_y_norm 은 그대로 저장'."
    }
   ],
   "ssotv3_clause": "04 §6; 04 §4 entry_zone vocabulary; A T-A-V3-STEP1-003 R7",
   "lane_sources": [
    "A",
    "lane2",
    "lane6"
   ],
   "status": "CONSISTENT",
   "divergent_case": "Pre-ruling: lane2 top_dropdown fixture → DRAWER; lane6 drawer set omits TOP_DROPDOWN → band → MID. lane2 FLOATING needs a size cap (<0.9vw ∧ <0.9vh), lane6 has no definition, R7 has no size cap (any fixed/sticky out-of-flow box). Moot under R7.",
   "notes": "Precedence DRAWER > FLOATING > geometry was already common to lane2 and lane6 and is confirmed by R7.",
   "authority_override": "SUPERSEDED_BY_A_R7",
   "follow_up": "Under R7's DRAWER wording ('the container that made menu_dependency=1') lane2's inline_expand (menu_dependency=1, INLINE_EXPAND) becomes DRAWER, not MID, and top_dropdown stays DRAWER; lane6's drawer set must become 'menu_dependency=1 container' (all reveal container types incl. TOP_DROPDOWN and INLINE_EXPAND) and is_floating must be computed from computed position fixed|sticky with no size cap. Lane owners re-derive; this document does not edit lane files.",
   "fact_correction_r2": "**FACT_CORRECTION T-A-V3-FC-001** (A, on D-V3-FACT_CORRECTION-001): the R7 sentence '정확히 1/3 인 점은 TOP 이자 TOP_CENTER' was wrong on the y axis; the band table is canonical with [a,b) on both axes — y = 1/3 → MID, y = 2/3 → BOTTOM; inside TOP x = 1/3 → TOP_CENTER, x = 2/3 → TOP_RIGHT. No cut-off value changes. lane2 fixture centres are not on a boundary (y boundaries 281.33 / 562.67 px; nearest centre 522 px → MID).",
   "lane_files_r2": "r2 applied in lane2 (`EXPECTATIONS.json`, `measure_geometry.py`, 14/14 PASS): zones re-derived from r1 raw x/y under R7 — label_explicit_text / label_aria_labelledby_differs / label_pseudo_element_text MID→TOP_LEFT; label_icon_text / label_icon_only_ax_named / label_alt_title_value TOP_*→FLOATING (they sit inside a `position:fixed` header and R7 FLOATING has no size cap — this corrects the r1 follow-up sentence 'TOP_CENTER/TOP_RIGHT rows unchanged', which ignored the header); inline_expand MID→DRAWER; drawers/sheets/dropdown DRAWER unchanged; label_icon_only_unnamed FLOATING unchanged. C's one reading of R7 — FLOATING tested self-OR-ancestor — is declared in lane2 meta with the self-only alternative recorded per fixture as `entry_zone_band_R7`."
  },
  {
   "id": "P-38",
   "variable": "raw entry_x_norm / entry_y_norm are primary; zone is a summary compared second; tolerance ±0.02",
   "c_rules": [
    {
     "lane": "lane2",
     "wording": "'Adapter must compare raw x/y_norm first, zone second'; 'entry_x_norm/entry_y_norm within ±0.02 of out/measure_result.json'."
    },
    {
     "lane": "lane6",
     "wording": "entry_zone docstring 'Raw (x,y) are never discarded (04 §6); zone is a summary'; raises if x/y missing or outside [0,1]."
    },
    {
     "lane": "A",
     "wording": "R7 'entry_x_norm · entry_y_norm 을 항상 저장한다'; 'override 가 적용돼도 … 그대로 저장한다'; later threshold changes only as declared sensitivity, co-reported with the original."
    }
   ],
   "ssotv3_clause": "04 §6; A R7",
   "lane_sources": [
    "A",
    "lane2",
    "lane6"
   ],
   "status": "A_RULED",
   "divergent_case": null,
   "notes": null,
   "authority_override": "EXTENDED_BY_A_STEP1-012_GAP-07",
   "follow_up": null,
   "resolution_r2": "T-A-V3-STEP1-012 GAP-07: entry_* geometry / label / AX facts are recorded at the state where the control FIRST became observable; every row declares it in the new field entry_observed_state ∈ {S0, S1, …, POST_REVEAL:<nav_container_type>}; s0_task_control_visible remains an S0 fact (false for reveal-gated). Raw x/y_norm remain primary (±0.02) and are compared at that state; at S0 they are null for a reveal-gated control (GAP-04).",
   "lane_files_r2": "lane2 EXPECTATIONS: entry_observed_state on all 14 fixtures (7 × POST_REVEAL:<type>, 7 × S0) + meta.entry_observed_state_rule; measure_geometry.py derives and checks it",
   "authority_ticket": "T-A-V3-STEP1-012"
  },
  {
   "id": "P-39",
   "variable": "ABSTAIN sequence → flow-derived fields are missing (None), never 0",
   "c_rules": [
    {
     "lane": "lane6",
     "wording": "C-9: 'ABSTAIN sequence → flow_evaluable=False; all flow-derived fields are None (missing), never 0, so family denominators shrink instead of being diluted (05 §6); forced_dismissal_count still counted'."
    },
    {
     "lane": "lane1",
     "wording": "C-F5-NOSURF-01 accepts endpoint_status ∈ {PUBLIC_WEB_UNOBSERVABLE, ABSTAIN}; ids/hash untouched."
    }
   ],
   "ssotv3_clause": "05 §6 denominator chain; 04 §2 ABSTAIN; A R1 ('결측을 결측으로 보고하는 것이 표본을 바꾸는 것보다 정직하다')",
   "lane_sources": [
    "lane1",
    "lane6"
   ],
   "status": "A_RULED",
   "divergent_case": null,
   "notes": null,
   "authority_override": "CONFIRMED_BY_A_STEP1-012_GAP-04",
   "follow_up": null,
   "resolution_r2": "T-A-V3-STEP1-012 GAP-04 applies R13's all-variable principle to entry_* and occlusion: None/null, never 0, for the unobserved; explicit UNDETERMINED / NOT_OBSERVED for categoricals. lane6 C-9 already does this for flow-derived fields.",
   "lane_files_r2": "no lane6 edit here (other worker); lane2/lane3/lane7 aligned as in P-33 / P-25",
   "authority_ticket": "T-A-V3-STEP1-012"
  },
  {
   "id": "P-40",
   "variable": "two denominators: entry-structure n (evidence-bearing) vs endpoint-dependent n; family-specific reading of endpoint_status=AUTH_GATE",
   "c_rules": [
    {
     "lane": "A",
     "wording": "R2: 'AUTH_GATE 는 성공/실패가 아니라 terminal 관측값이다'; F1 'endpoint_contract 가 AUTH_GATE 를 endpoint 로 명시한다 → endpoint 도달로 센다. flow-evaluable 포함'; F2~F5 'AUTH_GATE 는 endpoint 미도달 terminal 이다 → evidence-bearing n 에 포함, endpoint 도달률 분모에서는 미도달로 집계'; critical '진입 flow 지표는 AUTH_GATE 여부와 무관하게 산출된다 … 이것들의 분모에서 F2~F5 의 AUTH_GATE target 을 빼지 마라'; 'endpoint 도달률 · endpoint 이후 sequence 길이 같은 지표만 flow-evaluable n 을 쓴다'."
    },
    {
     "lane": "lane6",
     "wording": "As read at d597c84: a single flow_evaluable flag (False only for ABSTAIN); endpoint_status=AUTH_GATE rows are flow-evaluable for every family; no family-conditional endpoint-reached count."
    }
   ],
   "ssotv3_clause": "05 §6; 05 §2 G; 04 §4; A T-A-V3-STEP1-003 R2",
   "lane_sources": [
    "A",
    "lane6"
   ],
   "status": "CONSISTENT",
   "divergent_case": null,
   "notes": "Not a C choice; recorded because it constrains P-39 and P-42.",
   "authority_override": "A_RULED_R2",
   "follow_up": "lane6 must split its single flag into (i) entry-structure denominator = evidence-bearing n (entry position, visible_label, accessible_name, control type, menu/reveal, nav_container_depth, auth_gate_stage — computed for AUTH_GATE rows of every family) and (ii) endpoint-dependent denominator (endpoint-reached rate, post-endpoint sequence length) with F1 endpoint_status=AUTH_GATE counted as reached and F2–F5 as not reached. lane6 worker in progress."
  },
  {
   "id": "P-41",
   "variable": "task_role PRIMARY filter (literal filter string recorded with every aggregate)",
   "c_rules": [
    {
     "lane": "A",
     "wording": "R3: 'task_role 필드를 추가한다. 값은 PRIMARY / SECONDARY_REPEATED'; 'family-level 집계와 본표본 n 은 task_role=\\'PRIMARY\\' 로만 필터한다'; 'SECONDARY_REPEATED 는 별도 task_id 이자 별도 행이며 본표본 n 을 늘리지 않는다'; '적용했다는 주장이 아니라 필터 조건 문자열을 남긴다'."
    }
   ],
   "ssotv3_clause": "02 §4 fact_flow_observation (field addition, Δ8); 05 §1 n=10; A T-A-V3-STEP1-003 R3",
   "lane_sources": [
    "A"
   ],
   "status": "CONSISTENT",
   "divergent_case": null,
   "notes": null,
   "authority_override": "A_RULED_R3",
   "follow_up": "C's family_summary / pairwise_matrix inputs must be pre-filtered with the literal string task_role='PRIMARY' and that string echoed in the output; C checks at GATE 3 that B's aggregates carry the same literal."
  },
  {
   "id": "P-42",
   "variable": "denominator chain shows replaced k explicitly (0 written out), with per-replacement record",
   "c_rules": [
    {
     "lane": "A",
     "wording": "R4 format: 'candidate 10 → [replaced k, 사유별 내역] → eligible/frozen 10 → attempted n → evidence-bearing n → flow-evaluable n'; 'k=0 이어도 0 을 명시한다'; per replacement '(제외된 target_id, 사유, 투입된 reserve_rank, 판정 시각, 판정자)'; '교체는 precheck 단계에서만 가능하므로 이 내역은 attempted 이전에 동결된다'."
    },
    {
     "lane": "lane6",
     "wording": "As read at d597c84: denominator_chain(candidate, eligible, attempted, evidence_bearing, flow_evaluable) — monotonic check, no explicit replaced-k stage (only a note 'eligible < candidate: replacement allowed only before freeze')."
    }
   ],
   "ssotv3_clause": "05 §6; A T-A-V3-STEP1-003 R4 (+R1: no fifth replacement reason; TASK_COMPARABILITY_CONCERN is a finding, not a replacement)",
   "lane_sources": [
    "A",
    "lane6"
   ],
   "status": "CONSISTENT",
   "divergent_case": null,
   "notes": null,
   "authority_override": "A_RULED_R4",
   "follow_up": "lane6 denominator_chain gains a replaced-k stage that is always present (0 explicit) and a per-replacement record list; monotonicity check retained. lane6 worker in progress."
  },
  {
   "id": "P-43",
   "variable": "fixture_input_mode recorded per observation",
   "c_rules": [
    {
     "lane": "A",
     "wording": "R5: 'fixture 는 문자열이 아니라 의미 명세다'; new_recorded_field 'fixture_input_mode — FREE_TEXT / DROPDOWN / MIXED / MAP_PAN / OTHER'; '둘 다 있으면 task path 상 서비스가 먼저 제시하는 수단을 쓴다 — 수집자가 고르지 않는다'; applies to F2·F3·F4·F5."
    },
    {
     "lane": "lane1",
     "wording": "C-F4-DECOY-01 applies the fixed conditions via selects (sido=서울특별시, sigungu=중구, dept=내과) → under R5 this fixture's expected fixture_input_mode is DROPDOWN; C-F2-POS-01 (검색어=생수) → FREE_TEXT."
    }
   ],
   "ssotv3_clause": "02 §3/§4 (field addition, Δ8); 03 §4; A T-A-V3-STEP1-003 R5",
   "lane_sources": [
    "A",
    "lane1"
   ],
   "status": "CONSISTENT",
   "divergent_case": null,
   "notes": null,
   "authority_override": "A_RULED_R5",
   "follow_up": "lane1 EXPECTATIONS gain an expected fixture_input_mode per fixture (values above) once the runner schema carries the field; absence of the field on a fixture that uses a fixture is a MISSING_FIELD-class finding at GATE 1."
  },
  {
   "id": "P-44",
   "variable": "AUTH_GATE / ABSTAIN values are always field-qualified (action_token=… vs endpoint_status=…)",
   "c_rules": [
    {
     "lane": "A",
     "wording": "R6 Q8: '필드 한정을 의무화한다 — mart 컬럼·보고·집계에서 값만 단독으로 쓰지 않고 항상 endpoint_status=AUTH_GATE 또는 action_token=AUTH_GATE 로 층을 명시한다'; 'C 가 GATE 3 에서 이 한정이 지켜졌는지 검사한다'."
    }
   ],
   "ssotv3_clause": "04 §2 (token) vs 04 §4 (endpoint_status) — same strings in two layers; A T-A-V3-STEP1-003 R6 Q8",
   "lane_sources": [
    "A"
   ],
   "status": "CONSISTENT",
   "divergent_case": null,
   "notes": null,
   "authority_override": "A_RULED_R6_Q8",
   "follow_up": "This document applies the qualification in its own wording; lane READMEs (lane3 fixture table 'auth_gate_stage', 'endpoint' columns; lane6 README) use bare AUTH_GATE in places — a wording follow-up for lane owners, no value change. C adds a GATE 3 check: any bare AUTH_GATE/ABSTAIN in a mart column name, report or aggregate = finding."
  },
  {
   "id": "P-45",
   "variable": "F1 sequence truncation by auth: auth_gate_stage distribution co-reported; F1 flagged separately in cross-family depth comparison",
   "c_rules": [
    {
     "lane": "A",
     "wording": "R6 Q1: 'family 별 보고에 auth_gate_stage 분포를 병기하고, F1 의 짧은 sequence 를 \\'단순한 구조\\' 로 해석하지 않는다. cross-family depth 비교 시 F1 을 별도 표기한다'; '이것은 예상이지 관측이 아니다'."
    }
   ],
   "ssotv3_clause": "05 §5 sensitivity (pre-registered only); 05 §2 G; A T-A-V3-STEP1-003 R6 Q1",
   "lane_sources": [
    "A"
   ],
   "status": "CONSISTENT",
   "divergent_case": null,
   "notes": null,
   "authority_override": "A_RULED_R6_Q1",
   "follow_up": "lane6 family_summary output for F1 carries the auth_gate_stage distribution beside activation_depth / flow_step_count and an F1 marker in any cross-family depth table."
  },
  {
   "id": "P-46",
   "variable": "family statistics: median; Q1/Q3 Hyndman–Fan type 7; entropy in bits normalised by log2(k_observed); LCS/max-len (dice alt)",
   "c_rules": [
    {
     "lane": "lane6",
     "wording": "C-11: 'median standard; Q1/Q3 = Hyndman–Fan type 7 (numpy default); entropy in bits, entropy_norm divides by log2(k observed)'; seq_distance 'levenshtein_norm = lev / max(|a|,|b|)', 'lcs_sim = LCS / max(|a|,|b|) — C choice; lcs_sim_dice = 2·LCS/(|a|+|b|) reported as the alternative'."
    },
    {
     "lane": "A",
     "wording": "STEP1-007 R12: primary 'max(len(a), len(b)) 정규화'; also_stored 'sum(len) 정규화', 'Yujian-Bo'; 'primary 만 단일 보고에 쓴다'; '군집·MDS 를 수행할 때는 Yujian-Bo 를 병기한다'; '셋 다 저장하고 primary 를 지정하는 것이지, 하나만 계산하는 것이 아니다'."
    }
   ],
   "ssotv3_clause": "05 §4 'median/IQR/range', 'categorical distribution + entropy'; 05 §2 E 'normalized Levenshtein distance', 'LCS similarity' (normalisations unspecified); A T-A-V3-STEP1-007 R12",
   "lane_sources": [
    "A",
    "lane6"
   ],
   "status": "CONSISTENT",
   "divergent_case": null,
   "notes": null,
   "authority_override": "CONFIRMED_BY_A_R12",
   "follow_up": "lane6 seq_distance already uses lev/max(len) as primary (confirmed). It must additionally store levenshtein_norm_sum = lev/(|a|+|b|) and the Yujian-Bo normalised distance d_YB = 2·lev/(|a|+|b|+lev) on every pair, and any clustering/MDS output must carry the Yujian-Bo matrix beside the primary. Quantile type-7 / entropy choices of C-11 are untouched by R12 (no A ruling on them)."
  },
  {
   "id": "P-47",
   "variable": "pseudo-replication guard: n_service and n_pairs reported separately; 45 pairs are cells, never n=45; no composite score",
   "c_rules": [
    {
     "lane": "lane6",
     "wording": "PSEUDO_REPLICATION_GUARD = 'pairs are cells, not independent n'; family_summary/pairwise_matrix always report n_service, n_pairs, n_service_warning if n≠10; 'No composite score is produced (05 §3)'."
    }
   ],
   "ssotv3_clause": "05 §1; 05 §3 '가중합 단일 score 생성 금지'; 05 §4 'n=10 denominator 고정'",
   "lane_sources": [
    "lane6"
   ],
   "status": "CONSISTENT",
   "divergent_case": null,
   "notes": null,
   "authority_override": null,
   "follow_up": null
  },
  {
   "id": "P-48",
   "variable": "denominator chain is monotonic non-increasing (violation raises, hard blocker)",
   "c_rules": [
    {
     "lane": "lane6",
     "wording": "denominator_chain 'Raises ValueError if any later count exceeds an earlier one, or any count is negative'; README GATE 3 step 3 'any non-monotonic chain is a hard blocker'."
    }
   ],
   "ssotv3_clause": "05 §6",
   "lane_sources": [
    "lane6"
   ],
   "status": "CONSISTENT",
   "divergent_case": null,
   "notes": "Unchanged by R4 (which adds a stage; the monotonic rule still applies across all stages).",
   "authority_override": null,
   "follow_up": null
  },
  {
   "id": "P-49",
   "variable": "task binding immutability: runner echoes task_id / family_id / task_instruction / fixed_fixture / endpoint_contract / contract_sha256 verbatim; no re-binding output fields",
   "c_rules": [
    {
     "lane": "lane1",
     "wording": "immutable_fields = [task_id, family_id, task_family, task_instruction, fixed_fixture, endpoint_contract, contract_sha256]; output_fields_must_not_exist = [inferred_family, predicted_family, predicted_archetype, representative_function, rf_task_label, classifier_task_label, rebound_task_id]; 'Any immutable field drift or forbidden output field = FAIL (C0 candidate: task re-binding)'."
    }
   ],
   "ssotv3_clause": "03 §4 'v3 collector는 task family를 맞히지 않는다 … task label을 변경할 수 없다'; 02 §2 dim_task_contract (contract_sha256, freeze_status); 02 §1 '대표기능 classifier는 v3 main lineage에 없다'",
   "lane_sources": [
    "lane1"
   ],
   "status": "CONSISTENT",
   "divergent_case": null,
   "notes": null,
   "authority_override": null,
   "follow_up": null
  },
  {
   "id": "P-50",
   "variable": "contract_sha256 recipe (canonical field set and serialisation)",
   "c_rules": [
    {
     "lane": "lane1",
     "wording": "'contract_sha256 = sha256(json.dumps({family_id, task_id, task_instruction, fixed_fixture, endpoint_contract}, sort_keys=True, ensure_ascii=False, separators=(\",\",\":\")).encode(\"utf-8\"))'; C recomputes over the OUTPUT echo and requires equality with the input hash."
    },
    {
     "lane": "lane5",
     "wording": "Requires manifest fields task_contract_sha256 and endpoint_contract_sha256 per state and per step (presence + 64-hex form; not recomputed)."
    },
    {
     "lane": "lane4",
     "wording": "Failure injection 'task contract hash mismatch: TargetSpec carries task_contract_sha; mutate one byte → refused before launch; idempotency key changes'."
    }
   ],
   "ssotv3_clause": "02 §2 dim_task_contract.contract_sha256 (field list: task_id, family_id, service_id, task_instruction, fixture_json, endpoint_contract, auth_terminal_allowed, forbidden_action_set — recipe unspecified)",
   "lane_sources": [
    "lane1",
    "lane4",
    "lane5"
   ],
   "status": "CONSISTENT",
   "divergent_case": null,
   "notes": "Single recipe (lane1). C choice: the hash covers the five family-level contract strings only, not service_id / fixture_json / auth_terminal_allowed / forbidden_action_set of 02 §2. Field-name aliasing (contract_sha256 vs task_contract_sha256 vs task_contract_sha) is handled by lane5 FIELD_ALIASES; lane5 never recomputes the contract hash. A separate endpoint_contract_sha256 recipe is not defined by any lane — gap, to be fixed by the same json.dumps convention before GATE 2.",
   "authority_override": null,
   "follow_up": null
  },
  {
   "id": "P-51",
   "variable": "evidence identity spine and state identity (attempt_id added)",
   "c_rules": [
    {
     "lane": "lane5",
     "wording": "'Observation identity = service_id + task_id + run_id (the identity spine). State identity = spine + attempt_id + state_index.' §7.1 'attempt_id is added to state identity (02 §8 names only the spine)'. 'observation_id (state) and flow_observation_id (flow) are globally unique opaque ids.'"
    },
    {
     "lane": "lane4",
     "wording": "Idempotency key today 'ticket::run_id::target::collector_sha::protocol_sha'; adaptation 'the key must discriminate task_id (either target_id becomes a service-task pair or task_id joins the key)' — pending B's interface."
    }
   ],
   "ssotv3_clause": "02 §8 'service_id + task_id + run_id로 observation identity 구성'; 03 §10",
   "lane_sources": [
    "lane4",
    "lane5"
   ],
   "status": "CONSISTENT",
   "divergent_case": null,
   "notes": "lane4's key is a launch-suppression key, not the evidence identity; lane4 already plans the task_id join to align with the spine.",
   "authority_override": null,
   "follow_up": null
  },
  {
   "id": "P-52",
   "variable": "identifier syntax: ids are ASCII identifiers, never display names",
   "c_rules": [
    {
     "lane": "lane5",
     "wording": "'Ids are identifiers, not display names: ASCII [A-Za-z0-9][A-Za-z0-9_.-:]*, no whitespace, never equal to a display_name field.'"
    }
   ],
   "ssotv3_clause": "02 §8 'display name을 file id로 사용 금지'",
   "lane_sources": [
    "lane5"
   ],
   "status": "CONSISTENT",
   "divergent_case": null,
   "notes": null,
   "authority_override": null,
   "follow_up": null
  },
  {
   "id": "P-53",
   "variable": "append-only: re-collection = new run_id; existing evidence never rewritten",
   "c_rules": [
    {
     "lane": "lane5",
     "wording": "'Re-collection of the same service×task = new run_id (new directory). Existing evidence is never rewritten.'"
    },
    {
     "lane": "lane4",
     "wording": "'06 §6 same service-task 재수집 = new run_id' (used to shape the idempotency key)."
    }
   ],
   "ssotv3_clause": "02 §8 '재수집은 새 run, 재판정은 새 judgment version'",
   "lane_sources": [
    "lane4",
    "lane5"
   ],
   "status": "CONSISTENT",
   "divergent_case": null,
   "notes": null,
   "authority_override": null,
   "follow_up": null
  },
  {
   "id": "P-54",
   "variable": "required per-state artifacts and manifest fields",
   "c_rules": [
    {
     "lane": "lane5",
     "wording": "Artifacts dom, ax, screenshot, probe, control_facts each with path + sha256; manifest fields observation_id, service_id, task_id, run_id, attempt_id, state_index (S<n>), url, captured_at (ISO-8601), collector_sha, protocol_sha, task_contract_sha256, endpoint_contract_sha256, artifacts{kind:{path,sha256}}. §7.1: fields beyond 03 §10 'treated as required (lineage cannot be audited without them)'."
    },
    {
     "lane": "A",
     "wording": "STEP1-007 R11: '모든 terminal 관측은 endpoint_status 와 terminal_reason 을 둘 다 갖는다'; STEP1-003 R3/R5: task_role, fixture_input_mode fields; STEP1-006: depth_conditional_tokens."
    }
   ],
   "ssotv3_clause": "03 §10 (DOM, AX, screenshot, probe/CSS geometry, URL, selected control facts, manifest SHA); 02 §3 manifest pointers; A T-A-V3-STEP1-007 R11",
   "lane_sources": [
    "A",
    "lane5"
   ],
   "status": "CONSISTENT",
   "divergent_case": null,
   "notes": null,
   "authority_override": "EXTENDED_BY_A_R11",
   "follow_up": "lane5 required-field set for the terminal state record / flow observation gains terminal_reason (13-value enum; OTHER requires a note) — required pending B's schema; absence = MISSING_FIELD (isolated) once B's schema carries it, and a bare OTHER without note = MISSING_FIELD. task_role, fixture_input_mode, depth_conditional_tokens likewise join the flow-observation field list as Δ8/Δ9 additions."
  },
  {
   "id": "P-55",
   "variable": "per-step lineage: state ids resolve to the same spine; url continuity exact; six step hashes equal state hashes",
   "c_rules": [
    {
     "lane": "lane5",
     "wording": "'state_before_id/state_after_id MUST resolve to state records of the same spine; url_before/after MUST equal the referenced state's url; the six step hashes MUST equal the referenced states' manifest hashes.' §7.6 'URL continuity … is enforced exactly (no normalisation)'."
    },
    {
     "lane": "lane3",
     "wording": "lossless: 'url_before/url_after must differ exactly where the fixture changes location.hash (every recorded step)'."
    }
   ],
   "ssotv3_clause": "02 §4 fact_flow_step (state_before_id/state_after_id, url_before/url_after, before/after hashes)",
   "lane_sources": [
    "lane3",
    "lane5"
   ],
   "status": "CONSISTENT",
   "divergent_case": null,
   "notes": "Different aspects (lane5: step↔state equality; lane3: before≠after across a step). Compatible.",
   "authority_override": null,
   "follow_up": null
  },
  {
   "id": "P-56",
   "variable": "hash chain: level 1 artifact sha in evidence manifest; level 2 evidence-manifest sha bound by path manifest (or sidecar); manifest never self-hashed",
   "c_rules": [
    {
     "lane": "lane5",
     "wording": "'Evidence manifest lists sha256 of every artifact it references (level 1). Path manifest (path_manifest.json, runs[]) references each evidence manifest by evidence_manifest path + evidence_manifest_sha256 (level 2). Without a path manifest, a sidecar <manifest>.sha256 is required. Step hashes re-state the state hashes (level 1′)'. §7.2 'No sha inside the manifest of itself (circular)'."
    }
   ],
   "ssotv3_clause": "02 §8 'path manifest와 evidence manifest는 hash로 연결'; 03 §10 'manifest SHA'",
   "lane_sources": [
    "lane5"
   ],
   "status": "CONSISTENT",
   "divergent_case": null,
   "notes": null,
   "authority_override": null,
   "follow_up": null
  },
  {
   "id": "P-57",
   "variable": "overwrite definition",
   "c_rules": [
    {
     "lane": "lane5",
     "wording": "'manifest sha256 ≠ recomputed sha256 and artifact mtime is later than the manifest mtime or than the record's captured_at (+1 s tolerance) ⇒ overwrite. The same state identity declared twice with different hashes ⇒ overwrite … The same spine appearing under two directories, or the same observation_id/state/step identity twice ⇒ identity collision.' §7.5 'hash mismatch alone (e.g. wrong manifest) stays isolated'."
    }
   ],
   "ssotv3_clause": "02 §8 append-only",
   "lane_sources": [
    "lane5"
   ],
   "status": "CONSISTENT",
   "divergent_case": null,
   "notes": null,
   "authority_override": null,
   "follow_up": null
  },
  {
   "id": "P-58",
   "variable": "DISPLAY_NAME_AS_ID severity split",
   "c_rules": [
    {
     "lane": "lane5",
     "wording": "'systemic when in service_id/task_id/run_id (whole run's identity is not an id); isolated in observation_id/attempt_id/flow_observation_id'. §7.3 'classified systemic although the brief listed it as isolated'."
    }
   ],
   "ssotv3_clause": "02 §8",
   "lane_sources": [
    "lane5"
   ],
   "status": "CONSISTENT",
   "divergent_case": null,
   "notes": null,
   "authority_override": null,
   "follow_up": null
  },
  {
   "id": "P-59",
   "variable": "evidence defect catalogue, severities, verdict and exit codes",
   "c_rules": [
    {
     "lane": "lane5",
     "wording": "OVERWRITE_DETECTED / IDENTITY_COLLISION / MANIFEST_SHA_UNBOUND (level-2) = systemic; HASH_MISMATCH / MISSING_ARTIFACT / LINEAGE_BREAK / MISSING_FIELD (C addition) = isolated; 'no defects ⇒ COMPLETE; only isolated ⇒ COMPLETE_WITH_ISOLATED_DEFECTS; any systemic ⇒ SYSTEMIC_DEFECT'; exit 0 only when no systemic."
    }
   ],
   "ssotv3_clause": "03 §12 'evidence lineage complete'; 05 §5 'evidence defect는 structural failure로 재분류하지 않는다'",
   "lane_sources": [
    "lane5"
   ],
   "status": "CONSISTENT",
   "divergent_case": null,
   "notes": null,
   "authority_override": null,
   "follow_up": null
  },
  {
   "id": "P-60",
   "variable": "exactly-once: suppression happens before launch (no evidence dir, no browser line before the suppression line); lock files never deleted; retry semantics",
   "c_rules": [
    {
     "lane": "lane4",
     "wording": "Pass: 'runs/target all == 1; DUPLICATE_SUPPRESSED ≥ targets; proc2 rc 0; proc2 created no evidence/ dir and no Scout/L0/playwright line precedes its suppression line (launch-level, not post-hoc ledger block); lock files never deleted (S1b); after mark_done a retry is suppressed, after mark_failed_retryable exactly one retry proceeds'. Positive control on pre-W1 SHA 2281c85 must show DUPLICATE_LAUNCH_NOT_SUPPRESSED."
    }
   ],
   "ssotv3_clause": "03 §11/§12 'evidence identity / exactly-once / firewall'; 02 §8",
   "lane_sources": [
    "lane4"
   ],
   "status": "CONSISTENT",
   "divergent_case": null,
   "notes": "Key shape (task_id join) pending B's interface — a parameter, not a rule change.",
   "authority_override": null,
   "follow_up": null
  },
  {
   "id": "P-61",
   "variable": "two-layer scope fail-closed: both firewall layers independently deny every non-released scope; layer 2 holds its own manifest-sha literal and its own release-doc read; no import of layer 1",
   "c_rules": [
    {
     "lane": "lane4",
     "wording": "'Pass (today): every non-released scope denied by both; layers disagree only on V2_DIAGNOSTIC (T-B-BLK-009). Pass (after A's ruling): layers agree on every scope; layer 2 re-verifies the manifest sha with its own literal and its own release-doc read.' Proof steps: static grep, ref-swap, cross-mask monkeypatch, call-order grep."
    }
   ],
   "ssotv3_clause": "03 §12 'manifest outside access 0'; 03 §11",
   "lane_sources": [
    "lane4"
   ],
   "status": "CONSISTENT",
   "divergent_case": null,
   "notes": null,
   "authority_override": null,
   "follow_up": null
  },
  {
   "id": "P-62",
   "variable": "post-ruling scope table (E000_FAST policy; V2_DIAGNOSTIC at layer 2; V3_MAIN50 release doc)",
   "c_rules": [
    {
     "lane": "lane4",
     "wording": "'E000_FAST: ALLOW at both layers today although 00 §13 states only V2_DIAGNOSTIC REAL is permitted — a policy/runtime gap for A'; 'V2_DIAGNOSTIC L2 must PASS only if own read of V2_DIAGNOSTIC_RELEASE.json + own manifest_sha256 literal match'; 'V3_MAIN50 DENY until A freezes main-50 manifest + release doc with its sha'."
    }
   ],
   "ssotv3_clause": "03 §2/§12 (precheck before main 50); lane4 cites 00 §13 and 06 §6 (outside this document's tie-break set)",
   "lane_sources": [
    "lane4"
   ],
   "status": "PROVISIONAL_PENDING_A",
   "divergent_case": null,
   "notes": "Pending A on T-B-BLK-009 and the E000_FAST policy question. Today's fail-closed expectation (P-61) is not pending; only the post-ruling table is.",
   "authority_override": null,
   "follow_up": null
  },
  {
   "id": "P-63",
   "variable": "forbidden-action matrix invariants",
   "c_rules": [
    {
     "lane": "lane4",
     "wording": "must_never_activate: 'any control listed under never_activate must have ZERO click/fill/submit/keypress events … one event = GATE 1 FAIL'; state_match: 'a forbidden control classified SAFE is FAIL; a safe control classified FORBIDDEN_* is a precision defect (recorded, not a safety FAIL)'; cross-fixture invariants: no fill on input[type=password] or autocomplete in {current-password,new-password,one-time-code,cc-*}; no submit on form[action=/login]; no click on text matching 바로구매|장바구니|구매|결제|송금|예약|회원가입 (ADD_TO_CART is FORBIDDEN_TRANSACTION); score_forbidden_actions returns SKIPPED, never PASS, until a schema exists."
    },
    {
     "lane": "lane1",
     "wording": "Every fixture: body[data-c-forbidden-activated] must be absent; F2 '장바구니/바로구매 observed, not activated'; F3 '조회 submit not activated' (endpoint_contract forbids)."
    },
    {
     "lane": "lane3",
     "wording": "credential_check 'all [data-c-forbidden] inputs empty and body[data-c-forbidden-hit] absent; tracking-number input empty and 조회 not activated (F3 forbids the submit)'."
    }
   ],
   "ssotv3_clause": "03 §7 'credential 입력·login submit·본인인증 금지'; 03 §8 '구매/장바구니/결제/송금/예약 state-changing activation 금지 … 존재 확인은 허용하되 누르지 않는다'",
   "lane_sources": [
    "lane1",
    "lane3",
    "lane4"
   ],
   "status": "CONSISTENT",
   "divergent_case": null,
   "notes": "Effective forbidden set for a fixture = lane4 task-invariant safety matrix ∪ the frozen endpoint_contract's own prohibitions (lane1/lane3: F3 조회 submit is forbidden by contract although a generic search submit is SAFE in the matrix). Both are enforced; no contradiction.",
   "authority_override": null,
   "follow_up": null
  },
  {
   "id": "P-64",
   "variable": "login-form username field is FORBIDDEN_CREDENTIAL_INPUT by form context; guard SAFE = precision finding, fill = FAIL",
   "c_rules": [
    {
     "lane": "lane4",
     "wording": "'login forms' username fields (finance/nonfinance G-FIN-1/G-NF-1) — C expects FORBIDDEN by form context; if the guard says SAFE, C files a precision finding, and a fill event there is still a FAIL'."
    }
   ],
   "ssotv3_clause": "03 §7",
   "lane_sources": [
    "lane4"
   ],
   "status": "CONSISTENT",
   "divergent_case": null,
   "notes": null,
   "authority_override": null,
   "follow_up": null
  },
  {
   "id": "P-65",
   "variable": "CAPTCHA: only a visible, hittable active challenge is terminal; passive markup is not; never solve/interact",
   "c_rules": [
    {
     "lane": "lane4",
     "wording": "'DOM presence of captcha markup is not terminal; only a visible, hittable active challenge is terminal — and even then no solve attempt, no widget interaction, no token injection'; captcha_passive_script 'if the runner reports CAPTCHA terminal here it is over-blocking (precision defect, not safety FAIL)'."
    }
   ],
   "ssotv3_clause": "03 §8 'CAPTCHA 해결/우회 금지. active blocking challenge만 terminal'",
   "lane_sources": [
    "lane4"
   ],
   "status": "CONSISTENT",
   "divergent_case": null,
   "notes": null,
   "authority_override": null,
   "follow_up": null
  },
  {
   "id": "P-66",
   "variable": "lossless sequence check: fact_flow_step rows ordered by step_index, token-bearing rows only, compared as (state_before, action_token, state_after) triples",
   "c_rules": [
    {
     "lane": "lane3",
     "wording": "'runner's fact_flow_step rows, ordered by step_index, restricted to token-bearing rows, must equal lossless_check as (state_before,data-c-action,state_after) triples with state = body[data-c-state]'; integrity probe 'submit without the typed query is rejected by the fixture, so a runner that drops INPUT_QUERY cannot reach the endpoint'."
    },
    {
     "lane": "lane6",
     "wording": "GATE 3: 'Where a fact_flow_step table exists, rebuild the two sequences from action_token ordered by step_index first and diff them against B's stored sequences (raw-of-raw check).'"
    }
   ],
   "ssotv3_clause": "02 §4 fact_flow_step (step_index, action_token, state_before_id/state_after_id); 03 §5 Freeze 'normalized action tokens + raw selector/evidence'",
   "lane_sources": [
    "lane3",
    "lane6"
   ],
   "status": "CONSISTENT",
   "divergent_case": null,
   "notes": null,
   "authority_override": null,
   "follow_up": null
  },
  {
   "id": "P-67",
   "variable": "control-group interpretation: positive control failing = harness/evidence defect and negatives uninterpretable; negatives must fail in the predicted way; an empty result is never a pass; expectations are never edited to fit runner output",
   "c_rules": [
    {
     "lane": "lane1",
     "wording": "'Positive control failing = harness/adapter defect (EVIDENCE_DEFECT), negative controls then NOT interpretable.'"
    },
    {
     "lane": "lane2",
     "wording": "'negative controls must fail in the predicted way if the runner is wrong (unnamed→AX_NAMED, substring→MATCH, class-name→LEFT)'; 'expectations are not edited to fit runner output'."
    },
    {
     "lane": "lane3",
     "wording": "Mutation control: five planted errors → 'the walker reports FAIL on every mutated fixture'."
    },
    {
     "lane": "lane4",
     "wording": "'Without this, \"1 run/target\" could be an empty-result artefact'; 'an empty log must not look like a pass'."
    },
    {
     "lane": "lane5",
     "wording": "'Negative controls run outside fixtures (scratchpad) so that a pass is not an empty result'."
    },
    {
     "lane": "A",
     "wording": "STEP1-007 R14: 'C 는 B·D 의 fixture 를 쓰지 않고 SSOT 원문에서 자기 fixture 를 파생해 투입한다. C 의 fixture 가 B 의 fixture 와 다른 결과를 내면 그것이 해석 불일치 신호다. 이것을 GATE 1 통과 조건에 넣는다'."
    }
   ],
   "ssotv3_clause": "03 §12 (qualification exit conditions); 05 §5 'evidence defect는 structural failure로 재분류하지 않는다'; A T-A-V3-STEP1-007 R14",
   "lane_sources": [
    "A",
    "lane1",
    "lane2",
    "lane3",
    "lane4",
    "lane5"
   ],
   "status": "CONSISTENT",
   "divergent_case": null,
   "notes": "Every lane's independence note already states SSOT-only derivation and no B/D reads; R14 makes that a GATE 1 pass condition and adds: a C-fixture vs B-fixture result divergence is itself an interpretation-mismatch signal to report, not to smooth over.",
   "authority_override": "CONFIRMED_BY_A_R14",
   "follow_up": null
  },
  {
   "id": "P-68",
   "variable": "comparison normalisation and tolerances",
   "c_rules": [
    {
     "lane": "lane2",
     "wording": "categorical fields 'exact (after NFC + whitespace normalization)'; x/y_norm ±0.02; motion '|Δ| ≥ min_abs_delta_px' with the expected sign/axis; menu_dependency expected as 0/1 integers."
    },
    {
     "lane": "lane3",
     "wording": "obstruction numerics '±0.02'; menu_dependency expected as true/false booleans; 'equality after C recomputes from the runner's own sequence with the rules above (catches rule drift separately from sequence drift)'."
    },
    {
     "lane": "lane6",
     "wording": "menu_dependency returned as int 0/1; compare_with_mart_row casts B's derived ints with int(), auth_gate_stage with str()."
    }
   ],
   "ssotv3_clause": "04 §5; 02 §4 (menu_dependency typed bool in 04 §4)",
   "lane_sources": [
    "lane2",
    "lane3",
    "lane6"
   ],
   "status": "CONSISTENT",
   "divergent_case": null,
   "notes": "bool↔int coercion (true≡1) must be applied by the adapter before equality; recorded so that a type-only mismatch is never reported as a value mismatch.",
   "authority_override": null,
   "follow_up": null
  },
  {
   "id": "P-69",
   "variable": "activation_depth general criterion (three tests) and the full 18-token classification",
   "c_rules": [
    {
     "lane": "A",
     "wording": "STEP1-006 general_criterion: 'activation_depth 는 사용자가 control 을 의도적으로 활성화해 상태 전이를 일으킨 토큰의 수다'; tests '① 사용자의 의도적 조작인가 (수동 로드·리다이렉트·대기는 아니다)', '② control 활성화인가 (스크롤·타이핑은 control 활성화가 아니다)', '③ 상태가 전이되는가'. canonical_18_classification: IN = {OPEN_GLOBAL_MENU, OPEN_LOCAL_MENU, SWITCH_TAB, EXPAND_ACCORDION, SELECT_CATEGORY, SELECT_FUNCTION, SUBMIT_QUERY, SELECT_RESULT, OPEN_ITEM_DETAIL, OPEN_PLACE_DETAIL}; OUT = {INPUT_QUERY, DISMISS_OBSTRUCTION, AUTH_GATE, ENDPOINT_REACHED, ABSTAIN}; CONDITIONAL = {SELECT_ORIGIN, SELECT_DESTINATION, SELECT_DATE}."
    },
    {
     "lane": "lane3",
     "wording": "ACTIVATION_SET (13 tokens incl. the three CONDITIONAL as IN) + EXCLUDED_FROM_ACTIVATION_DEPTH (matches A's OUT)."
    },
    {
     "lane": "lane6",
     "wording": "ACTIVATION_CORE (9) + SUBMIT_QUERY (C-1) = A's IN set exactly; FORM_INTENT_TOKENS treats the three CONDITIONAL as OUT."
    }
   ],
   "ssotv3_clause": "04 §5; 03 §6; A T-A-V3-STEP1-006",
   "lane_sources": [
    "A",
    "lane3",
    "lane6"
   ],
   "status": "CONSISTENT",
   "divergent_case": null,
   "notes": "Comparison of the lanes against A's table: IN and OUT sets agree with both lanes token-for-token; the only difference is the CONDITIONAL triple (P-04). family_asymmetry_note pre-registered by A: the rule raises F2·F3·F5 depth relative to F1·F4 by construction — recorded now so it is not narrated post hoc.",
   "authority_override": "A_RULED_STEP1-006",
   "follow_up": null
  },
  {
   "id": "P-70",
   "variable": "depth_conditional_tokens recorded per observation",
   "c_rules": [
    {
     "lane": "A",
     "wording": "STEP1-006 CONDITIONAL.record: '각 관측에 depth_conditional_tokens 로 어느 토큰이 어떤 근거로 포함/제외됐는지 남긴다'; decision keyed to fixture_input_mode (STEP1-003 R5): DROPDOWN/MAP_PAN → IN, FREE_TEXT → OUT, MIXED → per means actually used."
    }
   ],
   "ssotv3_clause": "02 §4 fact_flow_observation (field addition, Δ9); A T-A-V3-STEP1-006",
   "lane_sources": [
    "A"
   ],
   "status": "CONSISTENT",
   "divergent_case": null,
   "notes": null,
   "authority_override": "A_RULED_STEP1-006",
   "follow_up": "C's derive() must accept the per-token decision and echo depth_conditional_tokens; C's GATE 1 check = for every observation containing SELECT_ORIGIN/SELECT_DESTINATION/SELECT_DATE, depth_conditional_tokens is present and each entry is consistent with fixture_input_mode."
  },
  {
   "id": "P-71",
   "variable": "terminal_reason companion field (13 values) and endpoint_status × terminal_reason allowed-combination table",
   "c_rules": [
    {
     "lane": "A",
     "wording": "STEP1-007 R11: 'endpoint_status enum 은 바꾸지 않는다. 동반 필드 terminal_reason 을 추가해 해상도를 올린다'; values [TIMEOUT, WAF_BLOCK, ACTIVE_CHALLENGE, NO_PUBLIC_MOBILE_WEB, TASK_SURFACE_ABSENT, APP_REQUIRED, CONTROL_DISABLED_OR_INERT, FORBIDDEN_ACTION_REQUIRED, AUTH_REQUIRED, EVIDENCE_DEFECT, REPLAY_BROKEN, AMBIGUOUS_MULTIPLE_CANDIDATES, OTHER]; rules '모든 terminal 관측은 endpoint_status 와 terminal_reason 을 둘 다 갖는다', 'OTHER 는 자유기술 note 필수', 'TASK_SURFACE_ABSENT 는 채널은 있으나 그 과업이 없는 경우 … precheck 교체 사유로는 여전히 NO_PUBLIC_MOBILE_WEB 하나만', 'FORBIDDEN_ACTION_REQUIRED 는 … 금지행위를 하지 않았다는 기록이지 실패가 아니다', 'CONTROL_DISABLED_OR_INERT 는 presence≠operative 결함군'; consistency 'endpoint_status × terminal_reason 의 허용 조합표를 B 가 runner 스키마에 명시하고 C 가 GATE 1 에서 검증한다. 불가능 조합(예: REACHED × TIMEOUT)은 스키마가 거부해야 한다'."
    },
    {
     "lane": "lane4",
     "wording": "State vocabulary already carries DISABLED_OR_INERT (↔ CONTROL_DISABLED_OR_INERT), 'CAPTCHA terminal (active blocking challenge)' (↔ ACTIVE_CHALLENGE), and 'Stop Condition: forbidden action attempt' (a FAIL when the runner ACTS; distinct from FORBIDDEN_ACTION_REQUIRED, which records that the path needed one and the runner stopped)."
    },
    {
     "lane": "lane1",
     "wording": "C-F5-NOSURF-01 expects endpoint_status ∈ {PUBLIC_WEB_UNOBSERVABLE, ABSTAIN} — under R11 the expected terminal_reason is TASK_SURFACE_ABSENT (channel exists, task absent)."
    }
   ],
   "ssotv3_clause": "04 §4 endpoint_status (enum unchanged); 03 §5 REPLAY_BROKEN; 03 §8 active challenge; A T-A-V3-STEP1-007 R11",
   "lane_sources": [
    "A",
    "lane1",
    "lane4"
   ],
   "status": "CONSISTENT",
   "divergent_case": null,
   "notes": null,
   "authority_override": "A_RULED_R11",
   "follow_up": "C GATE 1 check: (i) every terminal record has both fields; (ii) OTHER carries a note; (iii) every (endpoint_status, terminal_reason) pair is in B's published allowed-combination table and impossible pairs (e.g. REACHED × TIMEOUT) are rejected by the schema — C builds its own expected table from the R11 rules and diffs it against B's. lane1 EXPECTATIONS gain terminal_reason_allowed per fixture (NOSURF: TASK_SURFACE_ABSENT). lane4 must not equate FORBIDDEN_ACTION_REQUIRED with a forbidden-action FAIL."
  },
  {
   "id": "P-72",
   "variable": "auth_gate_stage gains UNDETERMINED; NONE is an affirmative, evidence-backed claim",
   "c_rules": [
    {
     "lane": "A",
     "wording": "STEP1-007 R13: 'auth_gate_stage enum 을 delta 로 확장한다 — UNDETERMINED 추가'; NONE = '관측했고, auth gate 가 없었다. 이것은 적극적 주장이며 증거를 요구한다'; UNDETERMINED = '판정할 수 없었다. 증거 부재·경로 미완·evidence defect 등'; same_rule_elsewhere '어떤 변수든 없음을 적으려면 관측했다는 증거가 있어야 한다'; aggregation 'UNDETERMINED 는 분모에서 빼지 말고 별도 범주로 보고한다'."
    },
    {
     "lane": "lane3",
     "wording": "auth_gate_stage.NONE: 'no AUTH_GATE token.' (emits NONE for any sequence without the token, including an unobserved/incomplete path)."
    },
    {
     "lane": "lane6",
     "wording": "auth_gate_stage_from_sequence: 'NONE — no AUTH_GATE.' (same; an empty sequence or a truncated BLOCKED/EVIDENCE_DEFECT path returns NONE)."
    },
    {
     "lane": "lane1",
     "wording": "C-F5-NOSURF-01 auth_gate_stage_allowed = [NONE] although no task path exists to observe."
    }
   ],
   "ssotv3_clause": "04 §4 auth_gate_stage vocabulary (extended by Δ10); 03 §7; A T-A-V3-STEP1-007 R13",
   "lane_sources": [
    "A",
    "lane1",
    "lane3",
    "lane6"
   ],
   "status": "CONSISTENT",
   "divergent_case": "Pre-ruling: task_flow_sequence = [] (or a path truncated with endpoint_status ∈ {BLOCKED, EVIDENCE_DEFECT, PUBLIC_WEB_UNOBSERVABLE}) → lane3/lane6 → auth_gate_stage=NONE; under R13 → UNDETERMINED. lane1 NOSURF fixture: expected NONE → under R13 expected UNDETERMINED.",
   "notes": "lane2's NONE values (accessible_name_source=NONE, label_relation=NONE at S0 for an unexposed control) are observed absences backed by the AX tree, so they satisfy R13's 'evidence of absence' requirement and are not superseded.",
   "authority_override": "SUPERSEDED_BY_R13",
   "follow_up": "lane3 rule and lane6 auth_gate_stage_from_sequence: NONE only when the path was observed to a terminal that is not action_token=AUTH_GATE with evidence (ENDPOINT_REACHED, or an evidence-bearing terminal on the frozen path); otherwise UNDETERMINED. lane1 C-F5-NOSURF-01 auth_gate_stage_allowed → [UNDETERMINED]. lane6 family_summary reports UNDETERMINED as its own category, never dropped from the denominator. R13's same-rule-elsewhere obligation: C reviews each categorical variable for a missing 'could not determine' value during GATE 1 and raises any found (none is added here without a case)."
  },
  {
   "id": "P-73",
   "variable": "dom_ax_divergence: DOM query vs AX tree disagreement on control existence",
   "c_rules": [
    {
     "lane": "A",
     "wording": "T-A-V3-STEP1-012 'DOM/AX divergence noted': when DOM query and AX tree disagree on control existence neither side wins; record both and set dom_ax_divergence=true."
    },
    {
     "lane": "lane2",
     "wording": "r2: dom_ax_divergence expected false on all 14 fixtures; measure_geometry.py records dom_exists / ax_exists at the observed state and flags XOR."
    }
   ],
   "ssotv3_clause": "00 §8; T-A-V3-STEP1-012",
   "lane_sources": [
    "A",
    "lane2"
   ],
   "status": "A_RULED",
   "divergent_case": null,
   "notes": "A runner reporting true on a C fixture is a finding to inspect, not an automatic FAIL.",
   "authority_override": "A_RULED_STEP1-012",
   "follow_up": null,
   "resolution_r2": "new row added in r2",
   "lane_files_r2": "lane2 EXPECTATIONS (field on all 14 fixtures) + meta.dom_ax_divergence_rule + measure_geometry.py",
   "authority_ticket": "T-A-V3-STEP1-012"
  },
  {
   "id": "P-74",
   "variable": "lane4 guard_fired 양성대조에서 '허용 행동' 의 정의",
   "status": "C_DECIDED",
   "lane_sources": [
    "lane4_safety_adapter/forbidden_action_matrix.json scoring_rule.guard_fired_positive_control",
    "comparators/grade_lane4.py grade_s4"
   ],
   "lane_files_r2": [
    "gate1/comparators/grade_lane4.py",
    "gate1/comparators/selftest.py",
    "gate1/comparators/README.md"
   ],
   "divergent_case": "매트릭스 문면은 SAFE/AUTH_ENTRY 활성화를 허용 행동으로 요구하나 login-gate fixture 2종은 SAFE 컨트롤이 없어 문면대로면 충족 불가",
   "resolution_r2": "fixture 에 SAFE 컨트롤이 있으면 SAFE/AUTH_ENTRY 활성화만 인정(navigate 불인정); 없으면 AUTH_ENTRY 활성화 또는 거부되지 않은 navigate 인정. 거부 0건=SYSTEMIC FAIL(침묵은 PASS 아님), 금지 행동 성공=HARD_STOP forbidden_action, 비대상 거부=guard_over_block ISOLATED",
   "c_rules": [
    "R9 forbidden_action",
    "Δ18-R20 lane 경계 실물 실행",
    "P-30 매트릭스 우선"
   ],
   "ssotv3_clause": "04 §safety / Δ19-R9",
   "authority_ticket": null,
   "authority_override": null,
   "notes": "worker 3 (545788d) 가 해결, C 본체 검토·등록. 결과를 본 뒤의 완화가 아니라 선행 fixture 구성 결함(SAFE 컨트롤 부재)에 대한 구현 해석",
   "follow_up": "B 어댑터 도착 후 실제 로그로 guard_events[] 별칭 매핑 확인"
  },
  {
   "id": "P-75",
   "variable": "GAP-05 off-viewport 컨트롤의 S0 occlusion 값 — null 인가 0.0 인가",
   "status": "C_DECIDED",
   "lane_sources": [
    "lane3_sequence_dismiss_auth/EXPECTATIONS.json unoccluded_but_offscreen",
    "lane3 README §9 GAP-04 null convention, §12"
   ],
   "lane_files_r2": [
    "gate1/lane3_sequence_dismiss_auth/fixtures/unoccluded_but_offscreen.html",
    "gate1/lane3_sequence_dismiss_auth/EXPECTATIONS.json"
   ],
   "divergent_case": "A 의 GAP-05 산문 예시는 '0% occluded 이나 off-viewport → not visible' 이라 S0 occlusion=0.0 으로 읽힐 수 있으나, GAP-04 필드 규칙(STEP1-012)은 관측되지 않은 수치를 null 로 둔다",
   "resolution_r2": "S0 에서 뷰포트 밖이면 task_control_occlusion_s0=null(관측 없음), visible=false, fvss=S1; 0.0 은 관측된 상태(S1)의 step occlusion 과 all_steps 에만 기록. runner 가 S0 에 0.0 을 내면 이 fixture 에서 FAIL(돌연변이 대조 3/3 검출)",
   "c_rules": [
    "GAP-04 null convention",
    "MIN-7 유사: 관측 없음에 값을 대입하지 않는다",
    "P-22"
   ],
   "ssotv3_clause": "T-A-V3-STEP1-012 GAP-04/GAP-05",
   "authority_ticket": "T-A-V3-STEP1-012",
   "authority_override": null,
   "notes": "worker 4 해결·C 본체 검토. 대안(폴드에 걸친 컨트롤)은 fvss=S0 인데 visible=false 가 되는 모순으로 기각",
   "follow_up": "A 가 GAP-05 를 '0.0' 으로 명시 판정하면 세 키만 바꾼다"
  },
  {
   "id": "P-76",
   "variable": "CI-14 r2 순서 발산 fixture 의 Δ30 1차 키 값과 발산 해석",
   "status": "C_DECIDED",
   "lane_sources": [
    "lane2_label_reveal/EXPECTATIONS.json order_controls",
    "lane2 measure_geometry.py delta30_order/min4_order/order_explaining"
   ],
   "lane_files_r2": [
    "gate1/lane2_label_reveal/fixtures/order_divergence_marked_not_first.html",
    "gate1/lane2_label_reveal/measure_geometry.py"
   ],
   "divergent_case": "Δ30 1차 키 task_binding_candidate 는 binder(lane1) 산출인데 lane2 fixture 에는 binder 가 없다",
   "resolution_r2": "lane2 에서는 [data-c-candidate] 전부를 후보로 fixture 가 선언(전원 true) → dom_order 가 결정; 3차 키 selector=tag#id; order_explaining 은 두 순서가 같으면 DELTA30, min4 1위이면서 Δ30 1위가 아닐 때만 MIN4; V3PathOrderDivergenceError 는 SEAM_REFUSED(PASS/FAIL 아님, Δ47-refusal)",
   "c_rules": [
    "Δ30-tiebreak",
    "Δ36-order",
    "Δ47-marked",
    "Δ47-refusal",
    "ruling_10"
   ],
   "ssotv3_clause": "delta Δ30/Δ36/Δ47",
   "authority_ticket": "T-A-V3-STEP1-035",
   "authority_override": null,
   "notes": "worker 6 해결·C 본체 검토. min4 키는 REFERENCE_DEFINITIONS.md(A1 line 366) 인용",
   "follow_up": "어댑터 도착 후 runner 의 SELECTED/rank 를 두 순서와 대조(CI-14)"
  }
 ],
 "revision": {
  "r2": {
   "date": "2026-08-28",
   "after_commit": "67f473b",
   "tickets": [
    "C-DECISION_REQUEST-031138",
    "T-A-V3-STEP1-011",
    "T-A-V3-STEP1-012",
    "T-A-V3-FC-001"
   ],
   "lane_files_edited": {
    "lane2": [
     "EXPECTATIONS.json",
     "measure_geometry.py",
     "README.md"
    ],
    "lane3": [
     "EXPECTATIONS.json",
     "walk_fixture.py",
     "README.md"
    ],
    "lane7": [
     "DISMISS_DEFINITION_C.md",
     "README.md",
     "impl_a.py",
     "impl_b.py",
     "converge_check.py",
     "validate_probe_like_playwright.py",
     "probe_like/f01-f06.json",
     "fixtures/f01-f05 (path-control marker)",
     "fixtures/f06 (new)"
    ],
    "lane6": "not edited (other worker)"
   },
   "reruns": {
    "lane2": "RESULT: 14/14 PASS, non-file requests aborted total=0",
    "lane3": "RESULT: ALL PASS (8/8 fixtures)",
    "lane7": "mode=CONVERGE rows compared=31 identical=31 differing=0 -> PASS; NEGCTRL_OK; checked=39 mismatches=0 -> PROBE_LIKE_VALIDATED"
   }
  }
 },
 "counts": {
  "CONSISTENT": 54,
  "A_RULED": 14,
  "C_DECIDED": 7,
  "PROVISIONAL_PENDING_A": 1
 }
}