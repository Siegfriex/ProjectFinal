"""Synthetic fixture modules for r32_inventory.py controls. Never imported by production code; parsed by AST and
exec'd only by r32_selftest.py to confirm each fixture behaves as its control claims."""
