"""must_not_flag — no structural inputs at all: zero sites, zero out-of-unit candidates."""


def add(a: int, b: int) -> int:
    return a + b


def shout(text: str) -> str:
    return text.upper() + "!"
