# RULING_INDEX_COVERAGE_C — A's canonical ruling index → C's GATE harness (coverage map)

**Plane** Claude C. **Base** `claude-c/assurance-v21 @ 04564a2` (gate1 tree `5ba23c3`). **Generated** 2026-08-28. Companion JSON: `RULING_INDEX_COVERAGE_C.json` (same rows).
**Index** `V3_RULING_INDEX.json` v1, 40 rulings, sha256 `b489400709b69f842257b9d4ecf8543cd825456d148b9bd0a460405d084592e4` (recomputed; equals the expected value and the copy at `origin/control/landing-orchestrator:research/landing_accessibility/control/v3/V3_RULING_INDEX.json`). index.authority_sha `774c81fe5183050221546b862e6d53fbc9223491`.
**Authority text** `V3_0_1_SUCCESSOR_DELTA.md` read via `git show origin/control/landing-orchestrator` (tip `adcdcb60`, file last changed `31ffd5a9`), sha256 `e35bc1afb1a8f36bbb4b5ad37ac1d565254017fd10cc7da23c0349fc26198edf` — byte-identical to the copy at the index's authority_sha. Per index.source the delta wins on conflict.
**Purpose** (index.how_to_use) C verifies GATEs against this list, not against B's delivery records. A ruling with no C check is a C gap; a ruling in the delta but not in the index is an A index-maintenance gap. Offline; no B/D worktree read.

## Coverage vocabulary

- **COVERED** — a specific C check exists and runs without B output
- **COVERED_PENDING_ADAPTER** — a C comparator/expectation exists; it turns PASS/FAIL only once the GATE1_RUNBOOK_C.md §3 adapter map binds B's field/file names (until then NOT_TESTABLE in run_gate1.py)
- **PARTIAL** — a check exists for part of the ruling, or exists without a positive control that could distinguish a correct runner from a runner hard-coding the expected value
- **UNCOVERED** — no C check found
- **NOT_C** — verified_by is not C
- **NOT_DUE_YET** — due after GATE1 (STEP2/STEP3/GATE2/GATE3); seeds noted where they exist

## Counts

| set | COVERED | COVERED_PENDING_ADAPTER | NOT_C | NOT_DUE_YET | PARTIAL | UNCOVERED | total |
|---|---|---|---|---|---|---|---|
| all 40 | 2 | 16 | 2 | 9 | 8 | 3 | 40 |
| GATE1-due 21 | 0 | 16 | 1 | 0 | 4 | 0 | 21 |

by due: GATE1 21, GATE2 1, GATE3 2, STEP2 3, STEP3 3, 상시 10

## GATE1-due rulings (21)

| id | due | requires (index verbatim) | must_appear_in (verbatim) | verified_by | C_check | coverage | gap_note |
|---|---|---|---|---|---|---|---|
| Δ6-d | GATE1 | Scout 의 결정론적 경로선택 정책 문서화·P4 이전 동결 | B 의 정책 문서 + 결정론 테스트 | C | • lane7_grain_determinism/ROUTE_POLICY_DETERMINISM_SPEC.md §2 RP-01..RP-12 (RP-02 ranking, RP-03 tie-break, RP-04 shortest-path, RP-05 stop conditions = Δ6-d's four elements; RP-11 policy sha)<br>• lane7_grain_determinism/determinism_check.py (N=3 byte-identical task/experienced sequences + ordered control_selector list; policy-doc sha bound)<br>• run_gate1.py items L7-route-policy-doc, L7-determinism-runner(-f01..f06), L7-determinism-posctrl / L7-determinism-negctrl (controls) | **COVERED_PENDING_ADAPTER** | Needs adapter map (flow.json name, runner template). Spec §Authority states the Δ6-d text was not in C's read set — re-pin the spec wording to Δ6-d (RP items already map). 'P4 이전 동결' is not checked: add a comparison of the policy-doc sha recorded in B's P3 COMPLETION against the doc at the P4 SHA. |
| Δ8-R3 | GATE1 | task_role PRIMARY/SECONDARY_REPEATED. family 집계는 PRIMARY 만 + 필터 조건 문자열 기록 | mart 스키마 + 집계 산출 | C | • lane6_stats/c_flow_derive.py::_task_role_of / split_by_task_role / PRIMARY_FILTER_CONDITION (family_summary, pairwise_matrix, denominator_chain emit filter_condition literal; missing/unknown task_role raises), compare_with_mart_row carries task_role<br>• lane6_stats/test_c_flow_derive.py::test_task_role_filter_and_secondary_summary<br>• run_gate1.py item L6-recompute-compare (schema check: task_role on every row) | **COVERED_PENDING_ADAPTER** | Needs adapter map only. Aggregate-side filter-string check on B's family report is GATE 3 (P-41 follow_up). |
| Δ8-R5 | GATE1 | fixture 는 의미 명세. fixture_input_mode 기록(FREE_TEXT/DROPDOWN/MIXED/MAP_PAN/OTHER) | 관측 행 필드 | C | • lane6_stats/c_flow_derive.py::derive(input_modes=…) / resolve_input_mode; compare_with_mart_row reads row.fixture_input_mode \| depth_input_modes<br>• lane6_stats/test_c_flow_derive.py::test_conditional_tokens_dropdown_vs_free_text<br>• run_gate1.py item L6-recompute-compare (schema check: fixture_input_mode where a conditional token occurs); gate1_adapter_spec.md §3 lists fixture_input_mode | **PARTIAL** | (1) No C browser fixture exercises a conditional token: lane3's 8 fixtures contain no SELECT_ORIGIN/SELECT_DESTINATION/SELECT_DATE (the names occur only in rule text), so B's recording of fixture_input_mode is never exercised on a fixture where DROPDOWN vs FREE_TEXT changes the answer — add a lane3 fixture pair (F5 date picker vs free-text date) with expected fixture_input_mode and activation_depth differing by 1. (2) lane1 EXPECTATIONS carries no expected fixture_input_mode (P-43 follow_up open). (3) 'service-first means, collector does not choose' has no check. |
| Δ8-R7 | GATE1 | entry_zone 임계 — y 삼등분, TOP 안에서만 x 삼등분, [a,b) 두 축 균일, y=1/3→MID · y=2/3→BOTTOM · TOP 내 x=1/3→TOP_CENTER · x=2/3→TOP_RIGHT. DRAWER>FLOATING>기하. 원좌표 항상 저장 | zone 유도 코드 + 경계 테스트 | C | • lane6_stats/c_flow_derive.py::entry_zone / entry_zone_record (ZONE_TOP_Y=1/3, ZONE_BOTTOM_Y=2/3, x thirds inside TOP only, [a,b), DRAWER>FLOATING>geometry, raw x/y retained and required even under override)<br>• lane6_stats/test_c_flow_derive.py::test_entry_zone_r7_thresholds_and_boundaries (exact boundary points y=1/3→MID, y=2/3→BOTTOM, x=1/3→TOP_CENTER, x=2/3→TOP_RIGHT)<br>• lane2_label_reveal/measure_geometry.py::zone + EXPECTATIONS (14 fixtures, zone R7 + entry_zone_band_R7 sensitivity); run_gate1.py L2-measure-geometry / L2-surface-state-compare (raw x/y ±0.02 first, zone second)<br>• compare_with_mart_row recomputes entry_zone from B's raw x/y | **COVERED_PENDING_ADAPTER** | Needs adapter map. B-side '경계 테스트' is only exercised if B rows hit a boundary — add synthetic boundary rows (x or y exactly 1/3 or 2/3) to L6-recompute-compare or require B's boundary-test names in the COMPLETION. compare_with_mart_row's DRAWER/FLOATING detection reads B fields entry_is_floating / entry_in_drawer — the adapter map must bind them (or derive from nav_container_chain). |
| Δ9 | GATE1 | activation_depth 18종 귀속 — IN 10 / OUT 5 / CONDITIONAL 3(fixture_input_mode 로 갈림). depth_conditional_tokens 근거 기록. 방향은 토큰 아님 | flow 정규화 코드 + depth_conditional_tokens 필드 | C | • lane6_stats/c_flow_derive.py::classify_token (ACTIVATION_RULE_ID STEP1-006: IN 10 / OUT 5 / CONDITIONAL 3), derive → depth_conditional_tokens (index/token/fixture_input_mode/decision/basis), raises on non-canonical tokens (direction-bearing OPEN_RIGHT_DRAWER rejected)<br>• lane6_stats/test_c_flow_derive.py::test_classify_activation_tokens / test_classify_unknown_token_raises / test_conditional_tokens_dropdown_vs_free_text / test_derive_typing_and_submit_sequence<br>• lane3 fixtures (SUBMIT_QUERY counted, INPUT_QUERY excluded: seq_typing_and_scroll_not_depth act_depth 2) + lane2 reveal_direction from geometry (direction is a variable, not a token)<br>• run_gate1.py items L3-sequence-compare (derived counts recomputed by C), L6-recompute-compare | **COVERED_PENDING_ADAPTER** | Needs adapter map. depth_conditional_tokens presence on B rows is not in the L6 schema-check list (only fixture_input_mode is) — add 'present whenever a conditional token occurs and decisions equal C's'. Conditional-token browser fixture missing (see Δ8-R5). |
| Δ10-R11 | GATE1 | terminal_reason 13값 동반 필드. 모든 terminal 이 두 필드. OTHER 는 note 필수. 허용 조합표 | 관측 행 + 조합표 | C | • lane5_evidence/EVIDENCE_CONTRACT_C.md §3b + evidence_lineage_check.py::check_flow (both fields on every flow record, 13-value enum, REACHED×non-null impossible, OTHER needs note, ALLOWED_TERMINAL table, MISSING_FIELD/SCHEMA_VIOLATION kinds); fixtures good/bad_overwrite/bad_lineage as controls<br>• lane6_stats/c_flow_derive.py::validate_terminal / TERMINAL_ALLOWED + test_c_flow_derive.py::test_validate_terminal_table; compare_with_mart_row R11 diff<br>• run_gate1.py items L5-runner-evidence-tree, L5-terminal-reason-table (B's table reconciled with C's), L6-recompute-compare | **COVERED_PENDING_ADAPTER** | C-internal defect to fix before GATE 1: lane5 ALLOWED_TERMINAL admits OTHER for every non-REACHED status, lane6 TERMINAL_ALLOWED admits OTHER only with ABSTAIN — the same B row (e.g. BLOCKED×OTHER) passes lane5 and fails lane6 (runbook §6.3 lists the pair but the code is unreconciled). B's combination table is unpublished → L5-terminal-reason-table stays NOT_TESTABLE until the COMPLETION carries it. |
| Δ10-R13 | GATE1 | auth_gate_stage 에 UNDETERMINED. NONE 은 적극적 주장. 전 변수 적용. UNDETERMINED 는 분모에서 빼지 않음 | 관측 행 + 집계 | C | • lane6_stats/c_flow_derive.py::AUTH_GATE_STAGES (incl. UNDETERMINED), auth_gate_stage_from_sequence (NONE only for a fully observed gate-free path), derive → UNDETERMINED for ABSTAIN/incomplete; family_summary keeps UNDETERMINED/NOT_OBSERVED as explicit categories (n_unobserved_explicit, never dropped from n)<br>• lane6_stats/test_c_flow_derive.py::test_auth_gate_stage_undetermined_vs_none / test_family_summary_missing_and_n_not_10 / test_gap04_null_convention<br>• lane5_evidence/evidence_lineage_check.py::check_flow — AFFIRMATIVE_WITHOUT_EVIDENCE (NONE on EVIDENCE_DEFECT/ABSTAIN) and AUTH_GATE×NONE/UNDETERMINED violations<br>• lane1 EXPECTATIONS auth_gate_stage_allowed=[NONE] only on reached fixtures; run_gate1.py L6-recompute-compare (auth_gate_stage ∈ AUTH_GATE_STAGES), L5-runner-evidence-tree | **COVERED_PENDING_ADAPTER** | Needs adapter map. '전 변수 적용': C has no inventory of B's mart variables that lack an undetermined value (ruling: raise immediately when found) — add to L6 schema checks: for every categorical column in B's fact_* schema assert an UNDETERMINED/NOT_OBSERVED member exists, list those without. |
| Δ10-R14 | GATE1 | C 는 B·D 의 fixture 를 쓰지 않고 SSOT 원문에서 자기 fixture 를 파생 | C 의 fixture 출처 기록 | A | • provenance records A verifies: lane1/lane2/lane3/lane6/lane7 README independence statements; lane5_evidence/EVIDENCE_CONTRACT_C.md header; GATE1_PREREGISTRATION_C.json inputs_read_only / not_read / P-67 (CONFIRMED_BY_A_R14); GATE1_RUNBOOK_C.md header + report §2 independence statement (run_gate1.py::write_report) | **NOT_C** | verified_by A. C keeps the provenance statements current at each lane edit. |
| Δ14-P06 | GATE1 | SWITCH_TAB 은 reveal 아님. menu_dependency_incl_tab 민감도 병기 | flow 정규화 + 민감도 산출 | C | • lane6_stats/c_flow_derive.py C-3 reveal set excludes SWITCH_TAB; derive emits menu_dependency_incl_tab (test_c_flow_derive.py:240)<br>• lane3_sequence_dismiss_auth/EXPECTATIONS.json menu_dependency (r2, REVEAL_SET without SWITCH_TAB); lane2 EXPECTATIONS meta.menu_dependency_rule (annotated)<br>• lane1 EXPECTATIONS C-F4-DECOY-01 informative sequence with SWITCH_TAB (menu_dependency 0 / incl_tab 1)<br>• run_gate1.py items L3-sequence-compare, L6-recompute-compare | **COVERED_PENDING_ADAPTER** | Needs adapter map. No lane3 browser fixture has SWITCH_TAB on the path (lane1's is informative only) — add one so B's menu_dependency=0 on a tab switch is exercised. Family-level '민감도 병기' (menu_dependency_incl_tab in family_summary) is a GATE 3 check — confirm ENTRY_STRUCTURE_VARS includes it. |
| Δ14-P09 | GATE1 | menu_dependency 는 sequence 기반. 04 가 10 glossary 를 이긴다 | flow 정규화 | C | • lane6_stats/c_flow_derive.py C-10 _before_terminal + menu_dependency (reveal token before ENDPOINT_REACHED; whole observed path if never reached), menu_dependency_alt_before_anchor sensitivity; test_derive_codebook_example / test_derive_nested_reveal_and_auth_positions<br>• lane3_sequence_dismiss_auth/EXPECTATIONS.json menu_dependency (r2, P-09 text) + fixtures 1–3 (menu_dep 1); lane2 meta.menu_dependency_rule (14 values identical under both readings)<br>• run_gate1.py items L3-sequence-compare, L6-recompute-compare | **COVERED_PENDING_ADAPTER** | Needs adapter map. The P-09 divergent case (function visible at S0, local menu opened later) exists only as a lane6 unit case — optional lane3 fixture. |
| Δ14-auth | GATE1 | auth_gate_stage 단일 위치규칙 — 과업 특이적 토큰 10종 기준 | flow 정규화 | C | • lane6_stats/c_flow_derive.py::auth_gate_stage_from_sequence (AUTH_STAGE_RULE_ID STEP1-011; task-specific 10-token set per C-6 table; AT_ENDPOINT only with endpoint_surface_rendered_before_gate=True) + test_p13_p14_auth_stage_rule_step1_011<br>• lane3_sequence_dismiss_auth/walk_fixture.py::auth_stage (derives from sequence, requires equality) + fixtures auth_gate_before_discovery / auth_gate_after_task_select / auth_gate_at_endpoint; EXPECTATIONS.auth_gate_stage rewritten to the ticket text<br>• run_gate1.py items L3-sequence-compare, L6-recompute-compare | **COVERED_PENDING_ADAPTER** | Needs adapter map. C-internal field-name mismatch: lane3 fixture flag endpoint_surface_rendered_without_auth vs lane6/adapter-spec endpoint_surface_rendered_before_gate — the adapter map must bind one B field to both. |
| Δ14-P17 | GATE1 | ABSTAIN(모른다) 과 TASK_SURFACE_ABSENT(없다고 안다) 구분 | 조합표 | C | • lane6_stats/c_flow_derive.py::validate_terminal / TERMINAL_ALLOWED (ABSTAIN→AMBIGUOUS_MULTIPLE_CANDIDATES\|OTHER vs PUBLIC_WEB_UNOBSERVABLE→TASK_SURFACE_ABSENT); q8 guard keeps action_token=ABSTAIN a separate layer; test_validate_terminal_table (:520), test lines 210–212<br>• lane5_evidence/EVIDENCE_CONTRACT_C.md §3b same split; lane1 EXPECTATIONS C-F5-NOSURF-01 endpoint_status_allowed=[PUBLIC_WEB_UNOBSERVABLE, ABSTAIN]<br>• run_gate1.py items L1-binding-and-hash, L6-recompute-compare, L5-runner-evidence-tree | **COVERED_PENDING_ADAPTER** | Needs adapter map. lane1 NOSURF admits both statuses without an expected terminal_reason (lane1 EXPECTATIONS has no terminal_reason field) — add expected (endpoint_status, terminal_reason) pairs so 'know it is absent' vs 'do not know' is asserted on the fixture, not only in lane6 unit tests. |
| Δ15-GAP07 | GATE1 | entry_observed_state 신규 필드 — 모든 행이 자기 시점 선언 | 관측 행 필드 | C | • lane2_label_reveal/measure_geometry.py:330-331 (entry_observed_state = S0 \| POST_REVEAL:<nav_type> \| NOT_OBSERVED) + EXPECTATIONS entry_observed_state on 14 fixtures (7×S0, 7×POST_REVEAL:<type>); README §How the runner adapter compares<br>• run_gate1.py item L2-surface-state-compare (entry_observed_state exact) | **COVERED_PENDING_ADAPTER** | Needs adapter map only. |
| Δ15-GAP06 | GATE1 | nav_container_type=최내곽 + nav_container_chain 병기 | 관측 행 필드 | C | • lane2_label_reveal/measure_geometry.py:333-334 (chain = [nav_type]; len(chain) == nav_container_depth check) + EXPECTATIONS nav_container_chain on 14 fixtures<br>• run_gate1.py item L2-surface-state-compare (nav_container_chain exact list) | **PARTIAL** | Every C fixture is single-level (chain length ≤ 1, innermost == outermost), so 'innermost' vs 'outermost' and chain ordering are indistinguishable — a runner reporting the outer container passes. Add a nested reveal fixture (e.g. hamburger → accordion, depth 2, expected nav_container_type=INLINE_EXPAND, chain=[<drawer type>, INLINE_EXPAND]) and extend measure_geometry chain derivation beyond one step. |
| Δ15-GAP02 | GATE1 | first_visible_scroll_state = 최초 관측 scroll state. 끝내 미관측만 NULL. **03 §3 scroll-only capture 검증 필수** | 수집기 + GATE1 보고 | C | • lane3 fixture seq_typing_and_scroll_not_depth (control below fold, fvss=S1) + walk_fixture.py:185-192 (fvss = scroll state of the first step after which the control is in viewport; null only if never observed) + EXPECTATIONS first_visible_scroll_state (fixture 6 null)<br>• run_gate1.py item L3-scroll-capture-03s3 (coverage key scroll_capture_03_s3) + evaluate: verdict cannot be PASS unless VERIFIED; write_report §4/§5 prints NOT_VERIFIED_DECLARED otherwise<br>• gate1_adapter_spec.md §3 (scroll-only states must be emitted even when the fixture fits one viewport) | **COVERED_PENDING_ADAPTER** | Needs adapter map (S1 fact_surface_state row). Single scroll fixture (n=1) — consider a second (control at S2) so S1 is not the only value the check can accept. |
| Δ15-GAP03 | GATE1 | flow_step_count 에서 ENDPOINT_REACHED·ABSTAIN 제외, AUTH_GATE 포함 | flow 정규화 | C | • lane6_stats/c_flow_derive.py C-4 flow_step_count (excludes ENDPOINT_REACHED/ABSTAIN, counts AUTH_GATE) + test_c_flow_derive.py::test_gap03_flow_step_count_terminals<br>• lane3_sequence_dismiss_auth/EXPECTATIONS.json flow_step_count (8 fixtures; auth_gate_before_discovery → 1 i.e. AUTH_GATE counted)<br>• run_gate1.py items L3-sequence-compare, L6-recompute-compare | **COVERED_PENDING_ADAPTER** | Needs adapter map only. |
| Δ15-GAP04 | GATE1 | 수치 미관측은 null(0 아님). occlusion=0.0 은 관측했고 안 가려짐. 한 행 안 결측 표현 일관 | 관측 행 | C | • lane6_stats/c_flow_derive.py::NULL_CONVENTION_ID / UNOBSERVED_CATEGORICAL; derive None-never-0; family_summary raises on empty-string categorical; entry_zone_record → NOT_OBSERVED; test_gap04_null_convention (:233), test_derive_abstain_is_not_flow_evaluable<br>• lane2 S0 rows of the 7 reveal fixtures (numeric null / NOT_OBSERVED, README fixtures table) + measure_geometry; lane3 fixtures 6/8 occlusion null vs fixture 1 = 0.0; lane7 f05 occl 0.0 observed-unoccluded<br>• run_gate1.py items L2-surface-state-compare ('GAP-04 null convention'), L6-recompute-compare | **COVERED_PENDING_ADAPTER** | Needs adapter map. '한 행 안 결측 표현 일관' is asserted only for lane2 S0 rows — add a generic row-level mixing check (null numeric alongside '' or 0-for-unobserved in the same row) to the L6 schema checks. |
| Δ15-GAP05 | GATE1 | s0_task_control_visible 과 occlusion 은 독립. 임계값 없음 | 측정 코드 | C | • lane3_sequence_dismiss_auth/walk_fixture.py:116 s0_task_control_visible = inViewport ∧ hitCentre (r1 occlusion<0.5 threshold removed); lane2 measure_geometry hit_at_centre<br>• lane3 fixture 2 (occl 1.0 at S0) and fixture 8 (below fold, occl null) | **PARTIAL** | No fixture with the discriminating case 'heavily occluded yet hit-testable → visible=true' (a runner applying an occlusion<0.5 threshold still passes all 8) nor 'occlusion 0.0 but off-viewport → visible=false' (fixture 8 has occlusion null, not 0.0). Add both to lane3. |
| Δ15-domax | GATE1 | DOM/AX 불일치 시 어느 쪽도 우선 안 함. dom_ax_divergence 플래그 | 관측 행 필드 | C | • lane2_label_reveal/measure_geometry.py:233 dom_ax_divergence = dom_exists != ax_exists; :335 compared; EXPECTATIONS false on all 14 fixtures<br>• run_gate1.py item L2-surface-state-compare | **PARTIAL** | No positive control: every fixture expects false, so a runner that hard-codes false passes. Add a fixture where DOM and AX disagree on control existence (e.g. control in DOM but aria-hidden / role=presentation → absent from AX) with expected dom_ax_divergence=true and both channels recorded. |
| Δ18-R20 | GATE1 | lane 경계를 가로지르는 경로는 양쪽 실물로 한 번 이상 실행. seam 1 수용기준은 guard 발화이지 AttributeError 소멸이 아님 | 실물 연결 테스트 + 음성대조 | C | • lane4_safety_adapter/GATE1_SAFETY_PLAN.md (b) S4 — real runner in FIXTURE mode over 13 guard fixtures scored against forbidden_action_matrix.json, positive control (≥1 logged activation on 검색 submit: zero logging ≠ zero events) + negative controls (no over-block)<br>• lane4_safety_adapter/adapter_interface_stub.py::score_forbidden_actions (SKIPPED, never PASS, until the action-log schema exists)<br>• run_gate1.py items L4-S4-forbidden-matrix-score, L4-S2-two-layer-scope-probe (imports both real firewalls); gate1_adapter_spec.md §3 action_log.jsonl with refused:true entries + log_active marker | **COVERED_PENDING_ADAPTER** | Needs runner_cmd_template + action-log schema. R20's acceptance is 'guard fires' on the real runner→guard seam: S4 asserts forbidden controls are never activated but does not assert a refusal event was logged — add to S4: ≥1 refused:true action_log entry on a fixture that tempts a forbidden action (positive control of the guard itself) with the allowed action passing in the same run (음성대조). Also request in B's COMPLETION the seam-test list (runner↔guard, runner↔evidence writer, runner↔L1/L2 firewall) with real-object test names. |
| STEP1-015 | GATE1 | AX 조인·scroll state 는 STEP 1 안. 가산적·회귀 전건 통과·collector_sha256 을 모든 v3 관측 행에 기록 | 수집기 + 관측 행 | C | • lane5_evidence/EVIDENCE_CONTRACT_C.md §2/§3/§3b — collector_sha required on every state/step/flow record; evidence_lineage_check.py STATE_REQUIRED / STEP_REQUIRED / FLOW_REQUIRED + FIELD_ALIASES collector_sha ← (collector_sha, collector_git_sha, collector_version_sha), check_required (:301)<br>• AX join: lane5 per-state 'ax' artifact required (ARTIFACT_ALIASES) + lane2 measure_geometry CDP Accessibility.getPartialAXTree; scroll state: run_gate1 L3-scroll-capture-03s3<br>• run_gate1.py item L5-runner-evidence-tree | **COVERED_PENDING_ADAPTER** | (1) FIELD_ALIASES lacks the ruling's exact name collector_sha256 — add it (else MISSING_FIELD false positive); (2) '가산적·회귀 전건 통과' is not checked by C — add a SUT_STATIC item that runs B's regression suite at the SUT SHA in the scratchpad clone and records counts (R20 caveat: lane tests passing ≠ integrated), and a schema diff of B's mart at the SHA vs 02 §3–§4 for removed fields; (3) add an equality assertion collector_sha(256) == exact_sha of the SUT. |

## 상시 rulings (10)

| id | due | requires (index verbatim) | must_appear_in (verbatim) | verified_by | C_check | coverage | gap_note |
|---|---|---|---|---|---|---|---|
| Δ4 | 상시 | promoted main 은 origin/ 접두 또는 exact SHA 로만 참조. bare 브랜치명 금지 | 코드·티켓·결정문 전수 | C | • (no check script) grep over assurance/: 2 origin-prefixed refs; qa_base.py:25/38 remote_tip() uses `git ls-remote origin refs/heads/research/landing-accessibility-main` (remote tip, compliant in substance, bare string); bus_mirror_c/acks/T-E-FACT_CORRECTION-001.C.json applied Δ4 by hand | **UNCOVERED** | Add ref_discipline_lint (new script or c_bus_scan rule): scan C tree + v3-era tickets/ACKs/completions for 'research/landing-accessibility-main' not preceded by 'origin/' or 'refs/remotes/origin/' and not accompanied by a 40-hex SHA; whitelist `ls-remote origin refs/heads/…`; report hits as findings. Manual compliance is not a check. |
| Δ5 | 상시 | v3 이후 티켓은 15 스키마. legacy type 매핑표. 소급 개정 금지 | 버스 티켓 | C | • bus.py::emit (v3 fields ticket_id/type/from/to/created_at_kst/priority/claim_kind/scope/status; legacy kind→v3 type map incl. HARD_STOP_CANDIDATE→BLOCKER; legacy_type retained) — C's own issuance<br>• c_bus_scan.py::scan content_changed_after_ack (ticket bytes changed after C's ACK = retroactive-edit detection, C-acked subset only) | **PARTIAL** | (1) bus.py::emit omits schema-required base_sha unless supplied in payload — add default; (2) c_bus_scan does not validate incoming v3-era tickets against 15 schema (required fields, type enum, legacy type used after cutoff without the mapping) — add a schema-validation pass with the Δ5 mapping table; (3) retroactive-edit detection covers only tickets C acked — extend to a mirror-wide hash ledger. |
| Δ6-a | 상시 | E 를 enum 에 포함. E 의 REAL 티켓은 5필드 필수 | 버스 티켓 | C | • c_bus_scan.py regex (A\|B\|C\|D\|E) strips E plane suffixes on ACK filenames (recognises E ACKs) — not an enum or field check; bus.py::is_for_c only | **UNCOVERED** | Add c_bus_scan v3-era rule: from/to/cc ⊆ {A,B,C,D,E,DIRECTOR}; when E ∈ from∪to and the ticket is REAL-bearing (scope/mode REAL or REAL request claim) require real_scope_id, release_doc, target_manifest_sha256, allowlist_ref, task_contract_sha256; any missing → SCHEMA_VIOLATION finding to A. |
| Δ6-b | 상시 | E REAL scope 0. 어느 평면도 E 에 REAL 요청 불가 | 릴리스 문서 부재 | C | • lane4_safety_adapter/two_layer_scope_probe.py (V3_MAIN50 / unknown / scope-less REAL denied at both layers regardless of requester; run_gate1.py L4-S2-two-layer-scope-probe) — mechanism level only | **PARTIAL** | Add bus-level check: any v3-era ticket with to∋E and a REAL scope is a violation while no E release doc exists; extend lane4 probe to enumerate release docs on control/landing-orchestrator and assert none binds an E scope. |
| Δ7 | 상시 | phase 자율전이는 REAL release 를 대체하지 않는다. hard stop 어휘 정본은 R9 8종 | 릴리스 발행 기록 | C | • run_gate1.py::HARD_STOP_8 + evaluate (hard-stop vocabulary = STEP1-004 R9 canonical 8; any trigger ⇒ HARD_STOP verdict)<br>• lane4_safety_adapter/two_layer_scope_probe.py + GATE1_SAFETY_PLAN.md (c) (REAL only when release doc RELEASED and manifest sha bound at both layers; run_gate1 L4-S2-*) | **PARTIAL** | (1) Vocabulary conflict to A: delta Δ7 text says the canonical hard-stop set is T-A-V3-P0-001's 6, the index says 'R9 8종', C's harness uses STEP1-004 canonical 8 — index.source says delta wins, so either the index row or STEP1-004 supersession must be stated; (2) no bus-side check that a REAL start cites a RELEASED release doc + manifest sha + explicit A GO rather than a phase transition — add a c_bus_scan rule for REAL-start claims. |
| Δ12-R15 | 상시 | A 의 판정은 근거 원문을 축자 인용 | A 결정문 | B·C·D·E | • GATE1_PREREGISTRATION_C.json rows with authority_override cite ssotv3_clause next to the A ticket (P-06/P-09/P-13/P-14/P-17/P-21/P-22/P-25/P-33/P-35/P-38/P-39/P-73 …) = C re-reads the quoted clause<br>• GATE1_RUNBOOK_C.md §5 (C-vs-B divergence → interpretation-mismatch finding to A, R14) | **PARTIAL** | No automated check that an A ruling ticket contains a verbatim quotation. Add c_bus_scan rule for A DIRECTIVE/ruling tickets after the v3 cutoff: require a non-empty verbatim-quote field with doc §; list rulings lacking one (delta itself flags Δ8-R1/Δ8-R2 as thin). |
| Δ12-R16 | 상시 | A 판정 반론은 FACT_CORRECTION. 근거는 같은 원문 다른 독해 | 버스 | A | • capability only: bus.py::emit(kind='FACT_CORRECTION') maps to v3 type FACT_CORRECTION; GATE1_PREREGISTRATION_C.json fact_correction_r2 fields record received FCs | **NOT_C** | verified_by A. No C action. |
| Δ13-R17 | 상시 | 철회는 주장과 같은 근거를 요구. 발행 시 어떻게 알았는가, 수신 시 원 기록 대조 | 버스 FACT_CORRECTION | C | • (none) c_bus_scan.py has no content rule for FACT_CORRECTION/retraction tickets; bus.py::ack records ticket_sha256 only | **UNCOVERED** | Add c_bus_scan rule: FACT_CORRECTION tickets (incl. self-retractions) must carry a how_known field (원 기록 path + sha/line); C's ACK of a retraction must record verified_against (원 기록 대조 result) before acceptance; list retractions lacking either. |
| Δ17-R18 | 상시 | 모든 ACK 은 ticket_sha256 포함. 발행 전 경로 존재 확인 | 버스 ACK | C | • bus.py::ack — ticket_sha256 = sha256 of the ticket file bytes at ACK time<br>• bus.py::emit — path-existence check before writing (os.link exclusive create; sequence suffix on collision)<br>• c_bus_scan.py::scan — content_changed_after_ack (acked ticket_sha256 vs current bytes) + acked_sha_unrecorded_n | **COVERED** | None for C's own ACKs. Other planes' hash-less ACKs are counted, not failed (legacy 116+ remain by ruling). |
| Δ17-R19 | 상시 | 재ACK 은 덮지 않고 <ticket>.<plane>-N.json | 버스 ACK | C | • bus.py::ack — writes <tid>.C.json, then <tid>.C-N.json on re-ACK (never overwrites)<br>• c_bus_scan.py::scan — glob {tid}.{plane}*.json reads every re-ACK when binding hashes | **COVERED** | None. |

## Rulings due after GATE1 (9: STEP2 / STEP3 / GATE2 / GATE3)

| id | due | requires (index verbatim) | must_appear_in (verbatim) | verified_by | C_check | coverage | gap_note |
|---|---|---|---|---|---|---|---|
| Δ1-a | STEP2 | F5 날짜 fixture — T+1 상대일자 + target 별 collection_date_kst·요일 기록 + 날짜 분산 사전등록 민감도 | FINAL_MAIN50_MANIFEST.f5_date_rule · 관측 행의 collection_date_kst/query_date/dow/is_holiday | C | • (none) grep collection_date_kst/query_date/dow/is_holiday over gate1/ and assurance/*.py = 0 hits | **NOT_DUE_YET** | STEP2 prep: add a manifest/observation check (new lane6 or manifest_check script): FINAL_MAIN50_MANIFEST.f5_date_rule in {A,B}; under B all F5 rows share one collection_date_kst; query_date/dow/is_holiday present on every F5 row; date-variance sensitivity pre-registered. |
| Δ1-b | STEP3 | F1 시중7/지방3 층 사전등록. primary 분모 불변 | manifest.strata · 분석 산출의 층별 병기 | C | • (none) grep strata over gate1/ = 0 hits | **NOT_DUE_YET** | STEP3 prep: lane6 family_summary(F1) needs a strata sensitivity block keyed to manifest.strata (시중7/지방3) reported beside, never instead of, the primary denominators. |
| Δ2 | STEP2 | replacement 순서명부 precheck 이전 동결·hash 포함·층 유지·소진 시 n<10 | manifest.replacement_reserve · replacement_rule | C | • lane6_stats/c_flow_derive.py::_validate_replacement / denominator_chain(replaced=…) (reserve_rank positive int, 5 item keys, reason set closed) — seed only<br>• lane6_stats/test_c_flow_derive.py::test_denominator_chain_replaced_k_explicit_and_reasons_closed | **NOT_DUE_YET** | STEP2 prep: add checks that (i) replacement follows reserve order (reserve_rank monotone; a skipped rank requires a recorded reason C reviews), (ii) the reserve list is inside the manifest hash and its freeze precedes the first precheck timestamp, (iii) exhausted reserve → family reported n<10, no ad-hoc addition. |
| Δ6-c | GATE2 | B 는 E route 를 실행 대본으로 재생하지 않는다. 갈리면 B 것이 task_flow_sequence | B replay 입력이 task_id+endpoint_contract 뿐임을 보이는 artifact · E_ROUTE_ID 는 lineage hint 로만 | C | • GATE1_RUNBOOK_C.md §2(b) (locate where task_id/contract enter TargetSpec) and gate1_adapter_spec.md §2 (contract input = task_id + endpoint_contract only) — seeds<br>• lane7_grain_determinism/ROUTE_POLICY_DETERMINISM_SPEC.md RP-10 (replay consumes frozen manifest only)<br>• (none) grep E_ROUTE_ID over gate1/ = 0 hits | **NOT_DUE_YET** | GATE2 prep: add a check that B's replay input schema carries no route/selector/action fields beyond task_id + endpoint_contract; E_ROUTE_ID may exist only as a lineage-hint field; E-vs-B route divergence is filed as a FINDING, not a defect. |
| Δ8-R1 | STEP2 | replacement 5번째 사유 없음. 과업 의미 불일치는 TASK_COMPARABILITY_CONCERN finding + ABSTAIN | precheck 기록 | C | • lane6_stats/c_flow_derive.py::REPLACEMENT_REASONS (4 values; _validate_replacement raises on a 5th)<br>• lane6_stats/test_c_flow_derive.py:787 (TASK_COMPARABILITY_CONCERN rejected as a replacement reason) | **NOT_DUE_YET** | STEP2 prep: precheck-record check that TASK_COMPARABILITY_CONCERN appears only as a FINDING ticket + endpoint_status=ABSTAIN row, never in a replacement record. |
| Δ8-R2 | STEP3 | 진입 flow 지표는 AUTH_GATE 여부와 무관하게 산출. endpoint 의존 지표만 flow-evaluable n | 분석 산출의 두 분모 분리 | C | • lane6_stats/c_flow_derive.py::family_summary (denominators.entry_structure_n / endpoint_dependent_n, AUTH_GATE_IS_ENDPOINT_BY_FAMILY, ENTRY_STRUCTURE_VARS / ENDPOINT_DEPENDENT_VARS)<br>• lane6_stats/test_c_flow_derive.py::test_family_summary_two_denominators_f1_vs_f2 | **NOT_DUE_YET** | STEP3: compare B's analysis output for both denominators per family; C's reading 'F2–F5 AUTH_GATE stays inside the reach-rate denominator as not-reached' (lane6 README R2 row) must be confirmed by A before GATE 3. |
| Δ8-R4 | STEP3 | 분모 사슬에 교체 이력 노출. k=0 도 명시 | 분석 산출 | C | • lane6_stats/c_flow_derive.py::denominator_chain (CHAIN_STAGES incl. 'replaced', k=0 explicit, per-item 5 fields, by_reason)<br>• lane6_stats/test_c_flow_derive.py::test_denominator_chain_replaced_k_explicit_and_reasons_closed<br>• run_gate1.py item L6-denominator-chain (runs at GATE 1 on any runner-emitted chain; hard_stop=denominator_corruption) | **NOT_DUE_YET** | STEP3: feed A's freeze-manifest replacements into denominator_chain(replaced=…, rows=…) per family and diff against B's reported chain. |
| Δ8-R6 | GATE3 | F1 auth 절단 사전등록 민감도. AUTH_GATE/ABSTAIN 필드 한정 의무 | 분석 산출·mart 컬럼 | C | • lane6_stats/c_flow_derive.py::q8_bare_mentions / assert_field_qualified (R6 Q8 guard; self-checked by derive/family_summary/pairwise_matrix/denominator_chain/validate_terminal)<br>• lane6_stats/test_c_flow_derive.py::test_q8_field_qualification_guard<br>• family_summary by_endpoint_reached split (R6 Q1 truncation visibility) | **NOT_DUE_YET** | GATE3: run q8_bare_mentions over B's mart columns/reports (GATE1_PREREGISTRATION_C P-44 follow_up notes C's own lane READMEs still use bare AUTH_GATE in places); F1 auth-truncation sensitivity: family_summary(F1) must co-report the auth_gate_stage distribution and an F1 marker in cross-family depth tables (P-45 follow_up open). |
| Δ10-R12 | GATE3 | sequence 거리 정규화 primary=max(len), sum(len)·Yujian-Bo 저장, 군집 시 YB 병기 | **STEP 3 mart/분석** | C | • lane6_stats/c_flow_derive.py::seq_distance (levenshtein_norm = lev/max len PRIMARY; levenshtein_norm_sum; yujian_bo always stored), pairwise_matrix(for_clustering_or_mds=True → clustering_companion='yujian_bo')<br>• lane6_stats/test_c_flow_derive.py::test_seq_distance_r12_three_normalisations | **NOT_DUE_YET** | GATE3: cell-by-cell comparison of all three matrices against B's family report; any clustering/MDS artifact from B must carry the Yujian-Bo companion. |

## UNCOVERED — concrete next tasks

- **Δ4** (상시): Add ref_discipline_lint (new script or c_bus_scan rule): scan C tree + v3-era tickets/ACKs/completions for 'research/landing-accessibility-main' not preceded by 'origin/' or 'refs/remotes/origin/' and not accompanied by a 40-hex SHA; whitelist `ls-remote origin refs/heads/…`; report hits as findings. Manual compliance is not a check.
- **Δ6-a** (상시): Add c_bus_scan v3-era rule: from/to/cc ⊆ {A,B,C,D,E,DIRECTOR}; when E ∈ from∪to and the ticket is REAL-bearing (scope/mode REAL or REAL request claim) require real_scope_id, release_doc, target_manifest_sha256, allowlist_ref, task_contract_sha256; any missing → SCHEMA_VIOLATION finding to A.
- **Δ13-R17** (상시): Add c_bus_scan rule: FACT_CORRECTION tickets (incl. self-retractions) must carry a how_known field (원 기록 path + sha/line); C's ACK of a retraction must record verified_against (원 기록 대조 result) before acceptance; list retractions lacking either.

## PARTIAL — what closes each

- **Δ5** (상시): (1) bus.py::emit omits schema-required base_sha unless supplied in payload — add default; (2) c_bus_scan does not validate incoming v3-era tickets against 15 schema (required fields, type enum, legacy type used after cutoff without the mapping) — add a schema-validation pass with the Δ5 mapping table; (3) retroactive-edit detection covers only tickets C acked — extend to a mirror-wide hash ledger.
- **Δ6-b** (상시): Add bus-level check: any v3-era ticket with to∋E and a REAL scope is a violation while no E release doc exists; extend lane4 probe to enumerate release docs on control/landing-orchestrator and assert none binds an E scope.
- **Δ7** (상시): (1) Vocabulary conflict to A: delta Δ7 text says the canonical hard-stop set is T-A-V3-P0-001's 6, the index says 'R9 8종', C's harness uses STEP1-004 canonical 8 — index.source says delta wins, so either the index row or STEP1-004 supersession must be stated; (2) no bus-side check that a REAL start cites a RELEASED release doc + manifest sha + explicit A GO rather than a phase transition — add a c_bus_scan rule for REAL-start claims.
- **Δ8-R5** (GATE1): (1) No C browser fixture exercises a conditional token: lane3's 8 fixtures contain no SELECT_ORIGIN/SELECT_DESTINATION/SELECT_DATE (the names occur only in rule text), so B's recording of fixture_input_mode is never exercised on a fixture where DROPDOWN vs FREE_TEXT changes the answer — add a lane3 fixture pair (F5 date picker vs free-text date) with expected fixture_input_mode and activation_depth differing by 1. (2) lane1 EXPECTATIONS carries no expected fixture_input_mode (P-43 follow_up open). (3) 'service-first means, collector does not choose' has no check.
- **Δ12-R15** (상시): No automated check that an A ruling ticket contains a verbatim quotation. Add c_bus_scan rule for A DIRECTIVE/ruling tickets after the v3 cutoff: require a non-empty verbatim-quote field with doc §; list rulings lacking one (delta itself flags Δ8-R1/Δ8-R2 as thin).
- **Δ15-GAP06** (GATE1): Every C fixture is single-level (chain length ≤ 1, innermost == outermost), so 'innermost' vs 'outermost' and chain ordering are indistinguishable — a runner reporting the outer container passes. Add a nested reveal fixture (e.g. hamburger → accordion, depth 2, expected nav_container_type=INLINE_EXPAND, chain=[<drawer type>, INLINE_EXPAND]) and extend measure_geometry chain derivation beyond one step.
- **Δ15-GAP05** (GATE1): No fixture with the discriminating case 'heavily occluded yet hit-testable → visible=true' (a runner applying an occlusion<0.5 threshold still passes all 8) nor 'occlusion 0.0 but off-viewport → visible=false' (fixture 8 has occlusion null, not 0.0). Add both to lane3.
- **Δ15-domax** (GATE1): No positive control: every fixture expects false, so a runner that hard-codes false passes. Add a fixture where DOM and AX disagree on control existence (e.g. control in DOM but aria-hidden / role=presentation → absent from AX) with expected dom_ax_divergence=true and both channels recorded.

## C-internal defects found while mapping (fix before GATE 1 run)

- R11 table split inside C: lane5 ALLOWED_TERMINAL admits OTHER for every non-REACHED status; lane6 TERMINAL_ALLOWED admits OTHER only with ABSTAIN (Δ10-R11 row)
- Field-name mismatch inside C: lane3 endpoint_surface_rendered_without_auth vs lane6/adapter-spec endpoint_surface_rendered_before_gate (Δ14-auth row)
- lane5 FIELD_ALIASES lacks collector_sha256, the exact name in STEP1-015
- bus.py::emit omits schema-required base_sha unless supplied (Δ5 row)
- Δ7 hard-stop vocabulary: C uses STEP1-004 canonical 8, delta text names P0-001's 6 — resolution belongs to A

## Index ↔ delta cross-check

### Δ items present in V3_0_1_SUCCESSOR_DELTA.md but absent from the index (→ A, index maintenance)

| delta | content | severity | note |
|---|---|---|---|
| Δ3 | 08 baseline SHA table (3 stale heads corrected) | low | informational; C pins exact SHAs anyway |
| Δ6-e | C·D 의 E 사용 — E is an evidence-acquisition operator only; C recomputes E artifact hashes/raw evidence independently; B–E agreement is not independence | medium | C-facing operative rule; propose index row verified_by C, due GATE2/상시 |
| Δ11 | 버스 구조 사실 — decisions live in ACK bodies (tools must inspect ACK content); v3-era ACK/completion without a ticket file is forbidden and C verifies; lane citations carry a timestamp | high | delta says 'C 가 검증한다' yet no index row; C check exists: c_bus_scan.py::scan dangling_refs_v3_era (v3-era ACK/completion without ticket) — ACK-body inspection is not yet a c_bus_scan rule |
| Δ12 (C 추가 지시) | GATE 1 에서 B 와 갈린 항목은 구현 결함이 아니라 해석 불일치 후보로 먼저 분류 | medium | covered by GATE1_RUNBOOK_C.md §5 (R14 interpretation-mismatch routing) but absent as an index row |
| Δ16 | SSOTV3 provenance snapshot (control/v3/ssot_snapshot/, 22 files) + honest limit | low | A action; C re-verified 0/22 mismatch at cad8ad45 (GATE1_RUNBOOK_C.md §1.5) |
| Δ5 sub-rule | finding_class: VALIDITY_RISK is issued at P1 or higher only | low | not in the Δ5 index text |
| Δ2 item 3 | reserve order skipped ⇒ reason recorded and C verifies | low | only implied by the Δ2 index text |

### Index items without a delta anchor, or conflicting with the delta text

- **STEP1-015** — not a Δ item; authority is ticket T-A-V3-STEP1-015 while index.source names the delta as 정본 — add a per-row authority field so non-delta rulings are traceable
- **Δ7** — index says 'hard stop 어휘 정본은 R9 8종'; delta Δ7 text says '정본은 T-A-V3-P0-001 의 6종'; C harness (run_gate1.py::HARD_STOP_8, runbook) uses STEP1-004 'R9 canonical 8'. index.source says delta wins on conflict — A must state whether STEP1-004 supersedes P0-001 (then the delta text is stale) or the index row is wrong

Present in both and consistent: Δ1-a, Δ1-b, Δ2, Δ4, Δ5, Δ6-a, Δ6-b, Δ6-c, Δ6-d, Δ8-R1…R7, Δ9, Δ10-R11…R14, Δ12-R15/R16, Δ13-R17, Δ14-P06/P09/auth/P17, Δ15-GAP07/06/02/03/04/05/domax, Δ17-R18/R19, Δ18-R20 (Δ8-R7's index text already carries the T-A-V3-FC-002 y=1/3→MID correction, as does the delta).

## How this map is used at GATE 1

1. `run_gate1.py` item ids cited in C_check are the execution units; a ruling whose items are NOT_TESTABLE after the adapter map is filled stays COVERED_PENDING_ADAPTER → verdict capped at METHOD_QUALIFIED_WITH_LIMITATIONS (runbook §5).
2. PARTIAL rows list the missing positive control or fixture; until added, a PASS on those items does not discharge the ruling and the report §4 (검증하지 않은 것) must name the ruling id.
3. UNCOVERED rows are C-side work items (bus scanner rules, ref-discipline lint); they are 상시 rulings and do not gate GATE 1 but are reported to A as open C coverage gaps.
4. The index↔delta discrepancy list goes to A as an index-maintenance finding (Δ11 is the material one: a C-verified rule with no index row).
